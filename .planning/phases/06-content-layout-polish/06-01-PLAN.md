---
phase: 06-content-layout-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/Section/SectionFAQ.vue
  - app/components/Section/SectionLeadForm.vue
  - app/components/Section/SectionAbout.vue
  - app/components/Section/SectionMethod.vue
autonomous: true
requirements:
  - CTRST-01
  - CTRST-02
  - CONT-04
  - CONT-05

must_haves:
  truths:
    - "FAQ accordion labels are dark readable text (#1a1a1a) against the light background, not muted/grey"
    - "Lead form inputs have white backgrounds with visible borders; labels are white on navy"
    - "SectionAbout heading reads 'Sua estrategia de milhas comeca aqui' instead of 'Sobre a Fly Up Milhas'"
    - "Step 4 in Como Funciona is titled 'Autonomia' with description about post-consultancy independence"
  artifacts:
    - path: "app/components/Section/SectionFAQ.vue"
      provides: "FAQ with high-contrast labels"
      contains: "color-brand-text"
    - path: "app/components/Section/SectionLeadForm.vue"
      provides: "Form with legible inputs on navy"
      contains: "bg-white"
    - path: "app/components/Section/SectionAbout.vue"
      provides: "Impactful value prop heading"
      contains: "Sua estrat"
    - path: "app/components/Section/SectionMethod.vue"
      provides: "Updated step 4 content"
      contains: "Autonomia"
  key_links:
    - from: "app/components/Section/SectionFAQ.vue"
      to: "UAccordion"
      via: "slot override for label styling"
      pattern: "#leading.*color-brand-text"
---

<objective>
Fix contrast issues in FAQ and form, update SectionAbout headline and SectionMethod step 4 content.

Purpose: Ensure all text is readable (WCAG AA contrast) and section content accurately reflects the consultancy offering.
Output: Four updated Vue components with corrected contrast, heading, and step content.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-content-layout-polish/06-CONTEXT.md

<interfaces>
<!-- Design tokens from app/assets/css/main.css -->
--color-brand-primary: #1a3a5c
--color-brand-bg: #F9FAFB
--color-brand-cta: #e67e22
--color-brand-text: #1a1a1a
--color-brand-text-muted: #6b7280

<!-- Established patterns -->
- UAccordion #content slot override: already done in SectionFAQ.vue (v1.1)
- template #label slot on UFormField: already done in SectionLeadForm.vue (v1.1)
- import.meta.client guard for browser APIs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix FAQ label contrast and form input legibility</name>
  <files>app/components/Section/SectionFAQ.vue, app/components/Section/SectionLeadForm.vue</files>
  <read_first>app/components/Section/SectionFAQ.vue, app/components/Section/SectionLeadForm.vue, app/assets/css/main.css</read_first>
  <action>
**SectionFAQ.vue (per D-01, CTRST-01):**
- The UAccordion currently uses default muted styling for label text. Add a `#leading` slot (or `#label` slot — check Nuxt UI v4 UAccordion slot API) to override the label text color.
- The slot should render the item label text with class `text-[var(--color-brand-text)]` which resolves to #1a1a1a (dark, WCAG AA compliant against #F9FAFB background).
- Per D-02, do NOT change the `#content` slot — it was already fixed in v1.1.
- If UAccordion v4 does not have a `#leading` slot, use the `ui` prop to override label text color via the component's theme configuration. Check the UAccordion component API: the trigger label styling may be overridable via `ui` prop like `ui="{ trigger: 'text-[var(--color-brand-text)]' }"` or similar. Try the `#default` slot with `item` and `open` params if slot-based override is needed.

**SectionLeadForm.vue (per D-13, D-14, D-15, D-16, CTRST-02):**
- Keep the navy card wrapper as-is (per D-13).
- Add explicit `class="w-full bg-white"` to all 3 UInput components (lines ~114, ~127, ~142). The `w-full` is already there; append `bg-white` if not present.
- Add explicit `class="w-full bg-white"` to the USelect component (line ~155).
- If Nuxt UI v4 UInput/USelect do not respond to `bg-white` class on the root element, use the `ui` prop: `ui="{ base: 'bg-white' }"` or similar override.
- Add a light gray border if inputs lack visible borders: try `border border-gray-300` on each input, or via the `ui` prop.
- Labels are already white via `<template #label><span class="text-white">` — verify they remain unchanged (per D-16).
  </action>
  <verify>
    <automated>cd /home/michel/projects/codebase/agencia201/flyupmilhas/site-flyupmilhas && grep -n "color-brand-text" app/components/Section/SectionFAQ.vue | head -5 && grep -n "bg-white" app/components/Section/SectionLeadForm.vue | head -10</automated>
  </verify>
  <acceptance_criteria>
- SectionFAQ.vue: UAccordion label text uses `color-brand-text` (#1a1a1a) — grep for `color-brand-text` in SectionFAQ.vue returns a match in the label/trigger area (not just the #content slot)
- SectionLeadForm.vue: All 3 UInput and 1 USelect have `bg-white` in their class or ui prop — grep for `bg-white` returns at least 4 matches
- SectionLeadForm.vue: `<span class="text-white">` still present for all 4 label slots
  </acceptance_criteria>
  <done>FAQ labels are dark readable text; form inputs have white backgrounds with visible borders on navy card; labels remain white</done>
</task>

<task type="auto">
  <name>Task 2: Update SectionAbout headline and SectionMethod step 4</name>
  <files>app/components/Section/SectionAbout.vue, app/components/Section/SectionMethod.vue</files>
  <read_first>app/components/Section/SectionAbout.vue, app/components/Section/SectionMethod.vue</read_first>
  <action>
**SectionAbout.vue (per D-03, D-04, CONT-04):**
- Change the h2 text on line 9 from "Sobre a Fly Up Milhas" to "Sua estrategia de milhas comeca aqui" (with proper accents: "Sua estrategia de milhas comeca aqui" — use the exact Portuguese: `Sua estratégia de milhas começa aqui`)
- Update the subtitle paragraph (line 12) to match the new heading tone. Per D-04, Claude's discretion. Suggested: "Descubra como transformar seus gastos do dia a dia em viagens executivas e renda extra" (or similar value-prop focused subtitle).

**SectionMethod.vue (per D-05, D-06, D-07, CONT-05):**
- Change step 4 object (index 3 in the `steps` array, around line 22-26):
  - `title`: change from `'Voo'` to `'Autonomia'`
  - `description`: change to exactly `'Apos o plano, voce tem autonomia para acumular e emitir milhas sozinho. O conhecimento fica com voce para sempre.'` (with proper accents: `'Após o plano, você tem autonomia para acumular e emitir milhas sozinho. O conhecimento fica com você para sempre.'`)
  - `icon`: change from `'i-heroicons-paper-airplane'` to `'i-heroicons-academic-cap'` (per D-07, representing knowledge/independence)
  </action>
  <verify>
    <automated>cd /home/michel/projects/codebase/agencia201/flyupmilhas/site-flyupmilhas && grep -n "Sua estrat" app/components/Section/SectionAbout.vue && grep -n "Autonomia" app/components/Section/SectionMethod.vue && grep -n "academic-cap" app/components/Section/SectionMethod.vue</automated>
  </verify>
  <acceptance_criteria>
- SectionAbout.vue: h2 contains "Sua estrat" (beginning of the new heading) — no occurrence of "Sobre a Fly Up Milhas"
- SectionMethod.vue: step 4 title is "Autonomia" — grep returns a match
- SectionMethod.vue: step 4 icon is "i-heroicons-academic-cap" — grep returns a match
- SectionMethod.vue: step 4 description contains "autonomia para acumular" — grep returns a match
  </acceptance_criteria>
  <done>SectionAbout has impactful value-prop heading; SectionMethod step 4 describes post-consultancy autonomy with academic-cap icon</done>
</task>

</tasks>

<verification>
- `pnpm build` completes without errors
- Visual check: FAQ labels are dark text, not muted grey
- Visual check: Form inputs are white with visible borders on navy background
- Visual check: SectionAbout heading is value-prop focused
- Visual check: Step 4 shows "Autonomia" with academic cap icon
</verification>

<success_criteria>
- All 4 components updated with correct content and contrast values
- No build errors
- CTRST-01, CTRST-02, CONT-04, CONT-05 requirements addressed
</success_criteria>

<output>
After completion, create `.planning/phases/06-content-layout-polish/06-01-SUMMARY.md`
</output>

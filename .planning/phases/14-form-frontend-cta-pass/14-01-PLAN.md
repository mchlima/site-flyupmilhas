---
phase: 14-form-frontend-cta-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/Section/SectionLeadForm.vue
  - app/components/Section/SectionProgramContent.vue
  - app/components/Section/SectionPrice.vue
autonomous: true
requirements:
  - FORM-02
  - FORM-03
  - FORM-04
  - CTA-01
  - CTA-02
  - CTA-03

must_haves:
  truths:
    - "Security badge 'Seus dados estao seguros' with lock icon visible below submit button"
    - "ProgramContent CTA reads 'Quero entender melhor'"
    - "Price card CTA reads 'Quero entrar na mentoria'"
    - "Form submit button reads 'Quero comecar minha mentoria'"
  artifacts:
    - path: "app/components/Section/SectionLeadForm.vue"
      provides: "Submit button text + security badge"
      contains: "Seus dados"
    - path: "app/components/Section/SectionProgramContent.vue"
      provides: "Mid-page CTA text"
      contains: "Quero entender melhor"
    - path: "app/components/Section/SectionPrice.vue"
      provides: "Final CTA text"
      contains: "Quero entrar na mentoria"
  key_links:
    - from: "app/components/Section/SectionLeadForm.vue"
      to: "security badge div"
      via: "sibling of UForm, not child"
      pattern: "</UForm>.*Seus dados"
---

<objective>
Update CTA button text across 3 sections and add security badge below the lead form.

Purpose: Complete the progressive CTA copy strategy (different text per scroll position) and add trust signal below the form submit button for conversion uplift.

Output: 3 modified Vue components with updated button text and 1 new security badge element.

Note on traceability: FORM-02 is OBSOLETE (objetivo field removed in Phase 11 — form is now nome+email+whatsapp). FORM-03 was completed in Phase 12 (form subtitle already reads "Preencha o formulario..."). CTA-01 was completed in Phase 12 (Hero CTA already reads "Quero comecar agora"). These requirements are listed for traceability only — no code changes needed for them.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-form-frontend-cta-pass/14-CONTEXT.md

<interfaces>
<!-- Current CTA button patterns in the 3 files to modify -->

From app/components/Section/SectionProgramContent.vue (line 30-34):
```vue
<button
  class="bg-[var(--color-brand-cta)] hover:bg-[var(--color-brand-cta-hover)] text-white font-semibold px-8 py-4 rounded-lg cursor-pointer transition-colors"
  @click="scrollTo('formulario')"
>
  Quero dar o primeiro passo
</button>
```

From app/components/Section/SectionPrice.vue (line 45-50):
```vue
<button
  class="w-full bg-[var(--color-brand-cta)] hover:bg-[var(--color-brand-cta-hover)] text-white font-semibold px-8 py-4 rounded-lg text-lg cursor-pointer transition-colors"
  @click="scrollTo('formulario')"
>
  Quero dar o primeiro passo
</button>
```

From app/components/Section/SectionLeadForm.vue (line 157-164):
```vue
<button type="submit" :disabled="isLoading" class="w-full py-4 bg-[var(--color-brand-cta)] ...">
  <span v-if="isLoading">Enviando...</span>
  <span v-else>Quero dar o primeiro passo</span>
</button>
```
UForm closing tag is at line 165: `</UForm>`
WhatsApp CTA div starts at line 168: `<div class="mt-4">`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CTA text in ProgramContent and Price sections</name>
  <files>app/components/Section/SectionProgramContent.vue, app/components/Section/SectionPrice.vue</files>
  <read_first>
    - app/components/Section/SectionProgramContent.vue
    - app/components/Section/SectionPrice.vue
  </read_first>
  <action>
In SectionProgramContent.vue, change the button text from "Quero dar o primeiro passo" to "Quero entender melhor" (per D-08, CTA-02). The button element is around line 33. Only the text changes — keep all classes and @click handler identical.

In SectionPrice.vue, change the button text from "Quero dar o primeiro passo" to "Quero entrar na mentoria" (per D-09, CTA-03). The button element is around line 49. Only the text changes — keep all classes and @click handler identical.

Do NOT touch any other text, classes, or structure in either file.
  </action>
  <verify>
    <automated>grep -n "Quero entender melhor" app/components/Section/SectionProgramContent.vue && grep -n "Quero entrar na mentoria" app/components/Section/SectionPrice.vue && ! grep "Quero dar o primeiro passo" app/components/Section/SectionProgramContent.vue app/components/Section/SectionPrice.vue</automated>
  </verify>
  <acceptance_criteria>
    - grep "Quero entender melhor" SectionProgramContent.vue returns exactly 1 match
    - grep "Quero entrar na mentoria" SectionPrice.vue returns exactly 1 match
    - grep "Quero dar o primeiro passo" in both files returns 0 matches
  </acceptance_criteria>
  <done>ProgramContent CTA reads "Quero entender melhor" and Price CTA reads "Quero entrar na mentoria"</done>
</task>

<task type="auto">
  <name>Task 2: Update form submit button text and add security badge</name>
  <files>app/components/Section/SectionLeadForm.vue</files>
  <read_first>
    - app/components/Section/SectionLeadForm.vue
  </read_first>
  <action>
Two changes in SectionLeadForm.vue:

1. SUBMIT BUTTON TEXT (per D-10): Change the `<span v-else>` text inside the submit button from "Quero dar o primeiro passo" to "Quero comecar minha mentoria". The button is around line 157-164. Keep all classes, v-if/v-else logic, and disabled binding identical.

2. SECURITY BADGE (per D-01 through D-05, FORM-04): Add a security badge div AFTER the closing `</UForm>` tag (line 165) and BEFORE the WhatsApp CTA div (line 168). The badge must be OUTSIDE the UForm component (D-03, D-05 — UForm expects UFormField children only).

Badge markup:
```html
<!-- Security badge — outside UForm per D-03/D-05 -->
<div class="flex items-center justify-center gap-2 mt-4 text-white/60 text-sm">
  <UIcon name="i-heroicons-lock-closed" class="w-4 h-4" />
  <span>Seus dados estao seguros</span>
</div>
```

Use UIcon with "i-heroicons-lock-closed" (per D-02). Use text-sm and text-white/60 for muted appearance (per D-04). Use flex centering to align icon and text horizontally.

Do NOT modify anything else — not the form fields, not the success state, not the WhatsApp CTA, not the script section.
  </action>
  <verify>
    <automated>grep -n "Quero comecar minha mentoria" app/components/Section/SectionLeadForm.vue && grep -n "Seus dados" app/components/Section/SectionLeadForm.vue && grep -n "lock-closed" app/components/Section/SectionLeadForm.vue && ! grep "Quero dar o primeiro passo" app/components/Section/SectionLeadForm.vue</automated>
  </verify>
  <acceptance_criteria>
    - grep "Quero comecar minha mentoria" SectionLeadForm.vue returns exactly 1 match
    - grep "Seus dados" SectionLeadForm.vue returns exactly 1 match
    - grep "i-heroicons-lock-closed" SectionLeadForm.vue returns exactly 1 match
    - grep "Quero dar o primeiro passo" SectionLeadForm.vue returns 0 matches
    - The security badge div appears AFTER </UForm> and BEFORE the WhatsApp CTA div (verified by line numbers in grep output)
  </acceptance_criteria>
  <done>Form submit button reads "Quero comecar minha mentoria" and security badge with lock icon appears below submit button, outside UForm</done>
</task>

</tasks>

<verification>
All 6 requirement IDs verified:
- FORM-02: Verify-only (objetivo field removed in Phase 11 — confirmed by checking form has exactly 3 fields: nome, email, whatsapp)
- FORM-03: Verify-only (subtitle "Preencha o formulario..." already present — grep confirms)
- FORM-04: Security badge with lock icon below submit button, outside UForm
- CTA-01: Verify-only (Hero CTA "Quero comecar agora" already present — grep confirms in SectionHero)
- CTA-02: ProgramContent CTA reads "Quero entender melhor"
- CTA-03: Price CTA reads "Quero entrar na mentoria"

Post-execution verification commands:
```bash
# Verify no "Quero dar o primeiro passo" remains in modified files
grep -r "Quero dar o primeiro passo" app/components/Section/SectionProgramContent.vue app/components/Section/SectionPrice.vue app/components/Section/SectionLeadForm.vue

# Verify progressive CTA text
grep "Quero entender melhor" app/components/Section/SectionProgramContent.vue
grep "Quero entrar na mentoria" app/components/Section/SectionPrice.vue
grep "Quero comecar minha mentoria" app/components/Section/SectionLeadForm.vue

# Verify security badge outside UForm
grep -A5 "</UForm>" app/components/Section/SectionLeadForm.vue | grep "Seus dados"

# Verify prior completions still intact
grep "Quero comecar agora" app/components/Section/SectionHero.vue
grep "Preencha o formulario" app/components/Section/SectionLeadForm.vue
```
</verification>

<success_criteria>
1. SectionProgramContent CTA button text is "Quero entender melhor"
2. SectionPrice CTA button text is "Quero entrar na mentoria"
3. SectionLeadForm submit button text is "Quero comecar minha mentoria"
4. Security badge "Seus dados estao seguros" with lock icon is visible below submit button, outside UForm
5. No occurrence of "Quero dar o primeiro passo" in any of the 3 modified files
6. All prior Phase 12 completions (Hero CTA, form subtitle) remain unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/14-form-frontend-cta-pass/14-01-SUMMARY.md`
</output>

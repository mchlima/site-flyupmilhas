---
phase: 07-content-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/Section/SectionHero.vue
  - app/components/App/AppHeader.vue
  - app/components/App/AppFooter.vue
  - app/app.vue
autonomous: true
requirements: [COPY-01, COPY-02, COPY-03]

must_haves:
  truths:
    - "Hero section displays 3 pain point chips above the headline"
    - "Hero CTA reads 'Quero dar o primeiro passo'"
    - "Hero subheadline mentions mentoria, not consultoria"
    - "AppHeader desktop and mobile CTAs read 'Quero dar o primeiro passo'"
    - "AppFooter WhatsApp link text says mentoria, not consultoria"
    - "All SEO meta tags (title, og, twitter) say mentoria, not consultoria"
    - "schema.org description says mentoria, not consultoria"
    - "No occurrence of the word 'consultoria' in any modified file"
  artifacts:
    - path: "app/components/Section/SectionHero.vue"
      provides: "Pain point chips + mentoria copy + new CTA"
      contains: "Quero dar o primeiro passo"
    - path: "app/components/App/AppHeader.vue"
      provides: "Updated CTA labels"
      contains: "Quero dar o primeiro passo"
    - path: "app/app.vue"
      provides: "SEO meta and schema.org with mentoria"
      contains: "mentoria"
  key_links:
    - from: "app/components/Section/SectionHero.vue"
      to: "useScroll composable"
      via: "scrollTo('formulario')"
      pattern: "scrollTo.*formulario"
    - from: "app/app.vue"
      to: "useSeoMeta"
      via: "SEO meta tags"
      pattern: "useSeoMeta"
---

<objective>
Update the hero section, header, footer, and SEO meta to replace "consultoria" with "mentoria" and add pain point badges to the hero.

Purpose: The LP must reflect the real product (mentorship) starting from the first touchpoint (hero, header, SEO).
Output: Updated SectionHero with pain point chips, all CTAs saying "Quero dar o primeiro passo", SEO meta using "mentoria".
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-content-overhaul/07-CONTEXT.md
@docs/OLD_SITE_CONTENT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pain point chips to SectionHero and update copy</name>
  <files>app/components/Section/SectionHero.vue</files>
  <read_first>
    - app/components/Section/SectionHero.vue
    - docs/OLD_SITE_CONTENT.md (section 1 pain points)
  </read_first>
  <action>
    Edit SectionHero.vue to:

    1. Add 3 pain point chips ABOVE the h1 headline (per D-06, D-07, D-08). Use a flex-wrap container with 3 inline spans:
       - "Viajar de aviao e caro"
       - "Milhas e so para quem tem cartao black"
       - "Nao sei por onde comecar"
       Style each chip as: `text-xs md:text-sm px-3 py-1 rounded-full border border-[var(--color-brand-primary)]/30 text-[var(--color-brand-text-muted)]` — muted outline style per D-08, leading into the headline without competing visually. Wrap in a `div class="flex flex-wrap justify-center gap-2 mb-6"`.

    2. Update the subheadline (p tag) from:
       "Em 1 hora de consultoria personalizada, montamos sua estrategia completa de milhas -- do diagnostico a emissao da passagem."
       TO:
       "Na mentoria Fly Up Milhas, voce aprende a acumular e usar milhas com estrategia -- do diagnostico ao seu primeiro voo executivo."
       (per D-01, D-09 — adjust wording for mentoria while keeping existing structure)

    3. Change CTA button text from "Quero minha Consultoria" to "Quero dar o primeiro passo" (per D-03).
  </action>
  <verify>
    <automated>grep -c "Quero dar o primeiro passo" app/components/Section/SectionHero.vue | grep -q "1" && grep -c "consultoria" app/components/Section/SectionHero.vue | grep -q "^0$" && grep -c "Viajar de" app/components/Section/SectionHero.vue | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "Quero dar o primeiro passo" app/components/Section/SectionHero.vue returns 1 match
    - grep -ic "consultoria" app/components/Section/SectionHero.vue returns 0
    - grep "Viajar de" app/components/Section/SectionHero.vue returns 1 match (pain point chip)
    - grep "rounded-full border" app/components/Section/SectionHero.vue returns matches (chip styling)
  </acceptance_criteria>
  <done>SectionHero shows 3 pain point chips above headline, subheadline says mentoria, CTA says "Quero dar o primeiro passo", zero consultoria occurrences</done>
</task>

<task type="auto">
  <name>Task 2: Update AppHeader CTAs, AppFooter WhatsApp text, and app.vue SEO meta</name>
  <files>app/components/App/AppHeader.vue, app/components/App/AppFooter.vue, app/app.vue</files>
  <read_first>
    - app/components/App/AppHeader.vue
    - app/components/App/AppFooter.vue
    - app/app.vue
  </read_first>
  <action>
    **AppHeader.vue** (per D-03, D-04):
    1. Change desktop CTA button text from "Quero minha Consultoria" to "Quero dar o primeiro passo"
    2. Change mobile CTA button text from "Quero minha Consultoria" to "Quero dar o primeiro passo"

    **AppFooter.vue** (per D-01):
    1. Change WhatsApp href text parameter from "consultoria%20de%20milhas" to "mentoria%20Fly%20Up%20Milhas" (URL-encoded in the wa.me link)

    **app.vue** (per D-27, D-28):
    1. Update useSeoMeta:
       - title: "Fly Up Milhas -- Mentoria em Milhas Aereas"
       - description: "Mentoria para aprender a acumular milhas e voar em classe executiva gastando menos."
       - ogTitle: same as title
       - ogDescription: same as description
       - twitterTitle: same as title
       - twitterDescription: "Mentoria para aprender a acumular milhas e voar em classe executiva."
    2. Update useSchemaOrg defineLocalBusiness:
       - description: "Mentoria em acumulo e emissao estrategica de milhas aereas"
       - Remove priceRange: 'R$200' (price is hidden per EDIT-02/D-16)
    3. Change aria-label on formulario section from "Formulario de consultoria" to "Formulario de mentoria"
  </action>
  <verify>
    <automated>grep -ic "consultoria" app/components/App/AppHeader.vue app/components/App/AppFooter.vue app/app.vue | grep -v ":0$" | wc -l | grep -q "^0$" && grep -c "Quero dar o primeiro passo" app/components/App/AppHeader.vue | grep -q "2" && grep -c "Mentoria" app/app.vue | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep -ic "consultoria" app/components/App/AppHeader.vue returns 0
    - grep -ic "consultoria" app/components/App/AppFooter.vue returns 0
    - grep -ic "consultoria" app/app.vue returns 0
    - grep -c "Quero dar o primeiro passo" app/components/App/AppHeader.vue returns 2 (desktop + mobile)
    - grep "mentoria" app/app.vue returns matches in SEO meta and schema.org
    - grep "priceRange" app/app.vue returns 0 (removed)
  </acceptance_criteria>
  <done>Header CTAs say "Quero dar o primeiro passo", footer WhatsApp says mentoria, all SEO meta and schema.org say mentoria, zero consultoria in any of the 3 files</done>
</task>

</tasks>

<verification>
Run: `grep -ric "consultoria" app/components/Section/SectionHero.vue app/components/App/AppHeader.vue app/components/App/AppFooter.vue app/app.vue`
Expected: All lines show `:0`

Run: `grep -c "Quero dar o primeiro passo" app/components/Section/SectionHero.vue app/components/App/AppHeader.vue`
Expected: SectionHero:1, AppHeader:2

Run: `grep "Viajar de" app/components/Section/SectionHero.vue`
Expected: 1 match (pain point chip)
</verification>

<success_criteria>
- Zero occurrences of "consultoria" in SectionHero.vue, AppHeader.vue, AppFooter.vue, app.vue
- Pain point chips visible in SectionHero above the headline
- All 3 CTA buttons (hero + header desktop + header mobile) say "Quero dar o primeiro passo"
- SEO meta tags reference "mentoria"
- schema.org no longer shows priceRange
</success_criteria>

<output>
After completion, create `.planning/phases/07-content-overhaul/07-01-SUMMARY.md`
</output>

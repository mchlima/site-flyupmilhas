---
phase: 07-content-overhaul
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/Section/SectionLeadForm.vue
  - app/components/Section/SectionSocialProof.vue
  - app/components/Section/SectionFAQ.vue
  - app/components/Section/SectionExpert.vue
  - app/components/Section/SectionPlaceholder.vue
autonomous: true
requirements: [COPY-01, COPY-02, COPY-04]

must_haves:
  truths:
    - "Form heading says mentoria, not 'Consultoria VIP'"
    - "Form success message mentions primeiro encontro, not consultoria"
    - "WhatsApp pre-filled message says 'mentoria Fly Up Milhas'"
    - "Form submit button says 'Quero dar o primeiro passo'"
    - "Testimonial text uses 'mentoria' instead of 'consultoria'"
    - "FAQ label and content use 'mentoria' instead of 'consultoria'"
    - "No occurrence of 'consultoria' in any visible copy across the entire app/ directory"
  artifacts:
    - path: "app/components/Section/SectionLeadForm.vue"
      provides: "Mentoria-focused form heading, success message, WhatsApp text, CTA"
      contains: "Quero dar o primeiro passo"
    - path: "app/components/Section/SectionSocialProof.vue"
      provides: "Testimonials with mentoria references"
      contains: "mentoria"
    - path: "app/components/Section/SectionFAQ.vue"
      provides: "FAQ with mentoria references"
      contains: "mentoria"
  key_links:
    - from: "app/components/Section/SectionLeadForm.vue"
      to: "useLeadForm composable"
      via: "submit function"
      pattern: "useLeadForm"
    - from: "app/components/Section/SectionLeadForm.vue"
      to: "WhatsApp URL"
      via: "WHATSAPP_URL constant"
      pattern: "WHATSAPP_URL"
---

<objective>
Update SectionLeadForm (heading, success message, WhatsApp text, submit CTA), SectionSocialProof (testimonials), SectionFAQ (Q&A), and legacy files to replace all remaining "consultoria" with "mentoria".

Purpose: Complete the consultoria-to-mentoria migration across every remaining component so zero occurrences remain site-wide.
Output: All form touchpoints, testimonials, FAQ, and legacy files use "mentoria" consistently.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-content-overhaul/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SectionLeadForm heading, success message, WhatsApp text, and submit CTA</name>
  <files>app/components/Section/SectionLeadForm.vue</files>
  <read_first>
    - app/components/Section/SectionLeadForm.vue
  </read_first>
  <action>
    Edit SectionLeadForm.vue with these specific changes (per D-25, D-26, D-03, D-05):

    1. **WHATSAPP_URL constant** (line 5): Change the pre-filled text parameter from
       `consultoria%20VIP%20de%20milhas` to `mentoria%20Fly%20Up%20Milhas`
       So the full URL becomes: `https://wa.me/55XXXXXXXXXXX?text=Ola%20Marcio%2C%20quero%20saber%20mais%20sobre%20a%20mentoria%20Fly%20Up%20Milhas.`

    2. **Form heading h2** (line 62): Change from "Quero minha Consultoria VIP" to "Comece sua mentoria agora" (per D-05, Claude's discretion on wording)

    3. **Form subheading p** (line 65): Keep "Preencha e Marcio entra em contato em ate 24h pelo WhatsApp." unchanged (still accurate)

    4. **Success message** (line 76): Change from "Recebi! Marcio vai te chamar no WhatsApp em ate 24h." to "Recebemos! Entraremos em contato pelo WhatsApp para agendar seu primeiro encontro." (per D-25)

    5. **Submit button text** (line 189): Change from "Quero minha Consultoria" to "Quero dar o primeiro passo" (per D-03, D-04)
  </action>
  <verify>
    <automated>grep -ic "consultoria" app/components/Section/SectionLeadForm.vue | grep -q "^0$" && grep -c "Quero dar o primeiro passo" app/components/Section/SectionLeadForm.vue | grep -q "1" && grep -c "primeiro encontro" app/components/Section/SectionLeadForm.vue | grep -q "1" && grep -c "mentoria%20Fly%20Up" app/components/Section/SectionLeadForm.vue | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep -ic "consultoria" returns 0
    - grep "Quero dar o primeiro passo" returns 1 match (submit button)
    - grep "primeiro encontro" returns 1 match (success message)
    - grep "mentoria%20Fly%20Up" returns 1 match (WhatsApp URL)
    - grep "Comece sua mentoria" returns 1 match (form heading)
  </acceptance_criteria>
  <done>Form heading says "Comece sua mentoria agora", success message mentions primeiro encontro, WhatsApp pre-filled text says mentoria, submit button says "Quero dar o primeiro passo", zero consultoria</done>
</task>

<task type="auto">
  <name>Task 2: Update SocialProof, FAQ, and legacy files to eliminate all remaining consultoria</name>
  <files>app/components/Section/SectionSocialProof.vue, app/components/Section/SectionFAQ.vue, app/components/Section/SectionExpert.vue, app/components/Section/SectionPlaceholder.vue</files>
  <read_first>
    - app/components/Section/SectionSocialProof.vue
    - app/components/Section/SectionFAQ.vue
    - app/components/Section/SectionExpert.vue
    - app/components/Section/SectionPlaceholder.vue
  </read_first>
  <action>
    **SectionSocialProof.vue**:
    1. In testimonial 1 (Ana Paula), change "Com a consultoria do Marcio" to "Com a mentoria do Marcio" and "Valeu cada centavo da consultoria" to "Valeu cada centavo da mentoria"

    **SectionFAQ.vue**:
    1. Change FAQ item label "E se eu nao gostar da consultoria?" to "E se eu nao gostar da mentoria?"
    2. In the same FAQ answer, the text references "reuniao" and "diagnostico detalhado" which are still accurate for the mentorship format — keep as-is (the word consultoria does not appear in the content, only in the label)

    **SectionExpert.vue** (legacy file, may not be in app.vue but still in codebase):
    1. Find and replace any "Quero minha Consultoria" with "Quero dar o primeiro passo"
    2. Replace any other "consultoria" occurrences with "mentoria"

    **SectionPlaceholder.vue** (legacy file):
    1. Change "consultoria VIP em milhas aereas" to "mentoria em milhas aereas"

    After all edits, run a final sweep:
    `grep -ric "consultoria" app/` — must return 0 total across all files.
    If any file still has "consultoria", fix it.
  </action>
  <verify>
    <automated>grep -ric "consultoria" app/ | grep -v ":0$" | wc -l | grep -q "^0$" && echo "PASS: zero consultoria site-wide" || (echo "FAIL: remaining occurrences:" && grep -ric "consultoria" app/ | grep -v ":0$")</automated>
  </verify>
  <acceptance_criteria>
    - grep -ric "consultoria" app/ returns only :0 lines (zero total occurrences site-wide)
    - grep "mentoria do Marcio" SectionSocialProof.vue returns 1 match
    - grep "mentoria" SectionFAQ.vue returns at least 1 match
  </acceptance_criteria>
  <done>Zero occurrences of "consultoria" across the entire app/ directory. Every visible text reference now says "mentoria". The full COPY-01 requirement is satisfied site-wide.</done>
</task>

</tasks>

<verification>
CRITICAL FINAL CHECK — run after all plans complete:
`grep -ric "consultoria" app/`
Expected: Every line shows `:0`. This is the definitive proof of COPY-01 completion.

`grep -rc "Quero dar o primeiro passo" app/`
Expected: Matches in SectionHero (1), AppHeader (2), SectionAbout (1), SectionPrice (1), SectionLeadForm (1) = 6 total
</verification>

<success_criteria>
- Zero "consultoria" occurrences across all files in app/
- Form heading, success message, and WhatsApp pre-filled text all reference mentoria
- Submit button says "Quero dar o primeiro passo"
- Testimonials and FAQ use "mentoria" where they previously said "consultoria"
- Legacy files (SectionExpert, SectionPlaceholder) cleaned up
</success_criteria>

<output>
After completion, create `.planning/phases/07-content-overhaul/07-03-SUMMARY.md`
</output>

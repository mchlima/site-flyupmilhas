---
phase: 01-foundation-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - nuxt.config.ts
  - package.json
  - tsconfig.json
  - .env
  - .env.example
  - .gitignore
  - .eslint.config.mjs
  - app/app.vue
  - app/assets/css/main.css
  - app/components/App/AppHeader.vue
  - app/components/App/AppFooter.vue
  - app/components/Section/SectionPlaceholder.vue
  - app/composables/useLeadForm.ts
  - public/favicon.ico
autonomous: true
requirements:
  - INFR-01

must_haves:
  truths:
    - "Running `pnpm build && pnpm preview` produces a working SSR page with no hydration warnings in the browser console"
    - "The page uses the `app/` directory structure with `app/app.vue` as root — no `pages/` directory exists"
    - "Brand colors (azul aviacao, branco gelo, laranja CTA) are available as CSS custom properties in `@theme` block"
    - "Inter font loads via @nuxt/fonts (bundled with @nuxt/ui) with no external DNS lookup required"
    - "`nuxt.config.ts` uses `cloudflare_pages` preset (underscore) and `future.compatibilityVersion: 4`"
    - "`runtimeConfig.public.apiBase` and `runtimeConfig.public.r2BaseUrl` are defined and overrideable via env vars"
  artifacts:
    - path: "nuxt.config.ts"
      provides: "Nuxt 4 configuration with Cloudflare Pages preset, modules, runtimeConfig"
      contains: "cloudflare_pages"
    - path: "app/assets/css/main.css"
      provides: "Tailwind v4 CSS-first design tokens"
      contains: "@theme"
    - path: "app/app.vue"
      provides: "Application root shell with header, main, footer"
    - path: "app/composables/useLeadForm.ts"
      provides: "Stub composable for Phase 3 wiring"
  key_links:
    - from: "nuxt.config.ts"
      to: "app/assets/css/main.css"
      via: "css array"
      pattern: "css:.*main\\.css"
    - from: "app/app.vue"
      to: "app/components/App/AppHeader.vue"
      via: "auto-import"
      pattern: "<AppHeader"
    - from: "nuxt.config.ts"
      to: "nitro.preset"
      via: "config key"
      pattern: "cloudflare_pages"
---

<objective>
Scaffold the Nuxt 4 project with SSR, Nuxt UI v4, Tailwind v4 design tokens, and all configuration required for Cloudflare Pages deployment. Produces a building, previewing, and type-checking project with the correct directory structure, brand tokens, Inter font, and stub components — ready for Phase 2 content sections.

Purpose: Every subsequent plan and phase depends on this scaffold. Getting the directory structure, preset name, module order, and design tokens right here prevents compounding errors later.
Output: A running Nuxt 4 project at `nuxt build && nuxt preview` with no console errors, correct `app/` directory layout, and CSS design tokens defined.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Nuxt 4 project and install all dependencies</name>
  <files>package.json, nuxt.config.ts, tsconfig.json, .gitignore, .eslint.config.mjs, .env, .env.example</files>

  <read_first>
    - .planning/phases/01-foundation-infrastructure/01-RESEARCH.md (Pattern 1: nuxt.config.ts, Standard Stack table, Anti-Patterns)
    - .planning/phases/01-foundation-infrastructure/01-CONTEXT.md (decisions D-01 through D-05, D-14)
  </read_first>

  <action>
    This is a greenfield project. The working directory already exists. Execute the following steps:

    **Step 1 — Initialize Nuxt 4 scaffold:**
    ```bash
    pnpm dlx nuxi@latest init . --force --package-manager pnpm
    ```
    Accept defaults. This produces `nuxt.config.ts`, `app/app.vue`, `package.json`, `tsconfig.json`.

    **Step 2 — Install all dependencies:**
    ```bash
    pnpm add @nuxt/ui @nuxt/image @nuxtjs/seo zod
    pnpm add -D @nuxt/eslint eslint
    ```
    Do NOT install tailwindcss separately — @nuxt/ui v4 manages it as a peer dependency.

    **Step 3 — Replace `nuxt.config.ts` with the exact content below** (per D-01, D-02, D-03, D-14):
    ```typescript
    // nuxt.config.ts
    export default defineNuxtConfig({
      compatibilityDate: '2025-07-15',

      future: {
        compatibilityVersion: 4,
      },

      modules: [
        '@nuxt/ui',
        '@nuxt/image',
        '@nuxtjs/seo',
        '@nuxt/eslint',
      ],

      ssr: true,

      nitro: {
        preset: 'cloudflare_pages',
        prerender: {
          autoSubfolderIndex: false,
        },
      },

      site: {
        url: 'https://flyupmilhas.com.br',
        name: 'Fly Up Milhas — Consultoria VIP em Milhas Aéreas',
        defaultLocale: 'pt-BR',
        description: 'Consultoria personalizada em milhas aéreas. Voe em classe executiva gastando menos.',
      },

      runtimeConfig: {
        public: {
          apiBase: '',
          r2BaseUrl: '',
        },
      },

      css: ['~/assets/css/main.css'],

      image: {
        quality: 85,
        format: ['webp'],
      },
    })
    ```
    Critical: the preset is `cloudflare_pages` (underscore, NOT hyphen). Both `compatibilityDate` and `future.compatibilityVersion: 4` are required together.

    **Step 4 — Create `.env` and `.env.example`:**
    ```bash
    # .env (local dev — never commit)
    NUXT_PUBLIC_API_BASE=http://localhost:3001
    NUXT_PUBLIC_R2_BASE_URL=https://cdn.flyupmilhas.com.br
    ```
    ```bash
    # .env.example (commit this)
    NUXT_PUBLIC_API_BASE=
    NUXT_PUBLIC_R2_BASE_URL=
    ```

    **Step 5 — Update `.gitignore`** to include `.env` but NOT `.env.example`:
    Ensure `.gitignore` contains at minimum: `.env`, `node_modules/`, `.nuxt/`, `.output/`, `dist/`

    **Step 6 — Create `eslint.config.mjs`** (or let @nuxt/eslint auto-generate via `pnpm nuxt prepare`):
    Run `pnpm nuxt prepare` to generate `.nuxt/` type stubs and auto-configure ESLint.
  </action>

  <verify>
    <automated>cd /home/michel/projects/codebase/agencia201/flyupmilhas/site-flyupmilhas && grep -q "cloudflare_pages" nuxt.config.ts && grep -q "compatibilityVersion: 4" nuxt.config.ts && grep -q "apiBase" nuxt.config.ts && grep -q "r2BaseUrl" nuxt.config.ts && echo "nuxt.config.ts OK" && grep -q "NUXT_PUBLIC_API_BASE" .env.example && echo ".env.example OK" && test -f package.json && grep -q "@nuxt/ui" package.json && echo "package.json OK"</automated>
  </verify>

  <acceptance_criteria>
    - `nuxt.config.ts` contains the string `cloudflare_pages` (grep confirms)
    - `nuxt.config.ts` contains `compatibilityVersion: 4` (grep confirms)
    - `nuxt.config.ts` contains `apiBase:` and `r2BaseUrl:` in runtimeConfig (grep confirms)
    - `package.json` lists `@nuxt/ui`, `@nuxt/image`, `@nuxtjs/seo`, `zod` as dependencies
    - `.env.example` contains `NUXT_PUBLIC_API_BASE=` and `NUXT_PUBLIC_R2_BASE_URL=`
    - `.env` is listed in `.gitignore` (grep `.gitignore` for `.env`)
    - `.env.example` is NOT in `.gitignore`
    - `tailwindcss` does NOT appear as a direct dependency in `package.json` (it is managed by @nuxt/ui)
  </acceptance_criteria>

  <done>Nuxt 4 project scaffolded with correct configuration, all dependencies installed, env vars defined, gitignore configured.</done>
</task>

<task type="auto">
  <name>Task 2: Create app shell, design tokens, and stub components</name>
  <files>
    app/app.vue,
    app/assets/css/main.css,
    app/components/App/AppHeader.vue,
    app/components/App/AppFooter.vue,
    app/components/Section/SectionPlaceholder.vue,
    app/composables/useLeadForm.ts,
    public/favicon.ico
  </files>

  <read_first>
    - .planning/phases/01-foundation-infrastructure/01-RESEARCH.md (Pattern 2: Tailwind v4 CSS, Pattern 3: app.vue shell)
    - .planning/phases/01-foundation-infrastructure/01-CONTEXT.md (decisions D-02, D-04, D-05)
    - nuxt.config.ts (verify `css` array points to correct path before creating the file)
  </read_first>

  <action>
    **Step 1 — Create `app/assets/css/main.css`** with Tailwind v4 CSS-first design tokens (per D-04):
    ```css
    /* app/assets/css/main.css */
    @import "tailwindcss";
    @import "@nuxt/ui";

    @theme {
      /* Brand colors */
      --color-brand-primary: #1a3a5c;       /* azul aviacao — headings, nav, trust elements */
      --color-brand-bg: #f8f9fa;            /* branco gelo / cinza ultra claro — page background */
      --color-brand-cta: #e67e22;           /* laranja/dourado — CTA buttons and accents */
      --color-brand-cta-hover: #d35400;     /* CTA hover state */
      --color-brand-text: #1a1a1a;          /* body text */
      --color-brand-text-muted: #6b7280;    /* secondary text */

      /* Typography (per D-05) */
      --font-family-sans: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    }

    /* Base styles */
    html {
      background-color: var(--color-brand-bg);
      color: var(--color-brand-text);
      font-family: var(--font-family-sans);
      -webkit-font-smoothing: antialiased;
    }
    ```
    Note: There is NO `tailwind.config.js`. All tokens are CSS custom properties inside `@theme {}`.

    **Step 2 — Create `app/app.vue`** as the root shell (per D-02 — no pages/ directory):
    ```vue
    <script setup lang="ts">
    // SEO meta — populated in Plan 03 with full useSeoMeta + useSchemaOrg
    // Placeholder title for scaffold validation
    useHead({
      title: 'Fly Up Milhas — Consultoria VIP em Milhas Aéreas',
      meta: [
        { name: 'viewport', content: 'width=device-width, initial-scale=1' },
        { charset: 'utf-8' },
      ],
    })
    </script>

    <template>
      <UApp>
        <AppHeader />
        <main id="main-content">
          <SectionPlaceholder />
        </main>
        <AppFooter />
      </UApp>
    </template>
    ```

    **Step 3 — Create `app/components/App/AppHeader.vue`:**
    ```vue
    <template>
      <header class="py-4 px-6 bg-white border-b border-gray-100">
        <div class="max-w-5xl mx-auto flex items-center justify-between">
          <span class="font-semibold text-[var(--color-brand-primary)] text-lg">
            Fly Up Milhas
          </span>
        </div>
      </header>
    </template>
    ```

    **Step 4 — Create `app/components/App/AppFooter.vue`:**
    ```vue
    <template>
      <footer class="py-6 px-6 text-center text-sm text-[var(--color-brand-text-muted)]">
        <p>&copy; {{ new Date().getFullYear() }} Fly Up Milhas. Todos os direitos reservados.</p>
      </footer>
    </template>
    ```

    **Step 5 — Create `app/components/Section/SectionPlaceholder.vue`:**
    ```vue
    <template>
      <section class="min-h-screen flex items-center justify-center bg-[var(--color-brand-bg)]">
        <div class="text-center">
          <h1 class="text-2xl font-bold text-[var(--color-brand-primary)] mb-2">
            Fly Up Milhas
          </h1>
          <p class="text-[var(--color-brand-text-muted)]">
            Em breve — consultoria VIP em milhas aéreas
          </p>
        </div>
      </section>
    </template>
    ```

    **Step 6 — Create `app/composables/useLeadForm.ts`** stub for Phase 3:
    ```typescript
    // app/composables/useLeadForm.ts
    // Stub — fully implemented in Phase 3
    export function useLeadForm() {
      const isLoading = ref(false)
      const isSuccess = ref(false)
      const error = ref<string | null>(null)

      // Wire up to POST /leads in Phase 3
      async function submit(_data: unknown) {
        // TODO: Phase 3 implementation
      }

      return { isLoading, isSuccess, error, submit }
    }
    ```

    **Step 7 — Verify the build runs:**
    ```bash
    pnpm build && pnpm preview
    ```
    Check browser console for hydration warnings. The page must load with no errors.
  </action>

  <verify>
    <automated>cd /home/michel/projects/codebase/agencia201/flyupmilhas/site-flyupmilhas && test -f app/app.vue && test -f app/assets/css/main.css && test -f app/components/App/AppHeader.vue && test -f app/components/App/AppFooter.vue && test -f app/components/Section/SectionPlaceholder.vue && test -f app/composables/useLeadForm.ts && grep -q "@theme" app/assets/css/main.css && grep -q -- "--color-brand-primary" app/assets/css/main.css && grep -q -- "--color-brand-cta" app/assets/css/main.css && grep -q "AppHeader" app/app.vue && echo "All files OK"</automated>
  </verify>

  <acceptance_criteria>
    - `app/assets/css/main.css` contains `@theme {` block (grep confirms)
    - `app/assets/css/main.css` contains `--color-brand-primary: #1a3a5c` (grep confirms)
    - `app/assets/css/main.css` contains `--color-brand-cta: #e67e22` (grep confirms)
    - `app/assets/css/main.css` contains `--font-family-sans:` with Inter (grep confirms)
    - `app/app.vue` contains `<UApp>` wrapper (grep confirms)
    - `app/app.vue` contains `<AppHeader />` and `<AppFooter />` (grep confirms)
    - `app/composables/useLeadForm.ts` exports `useLeadForm` function (grep confirms)
    - NO `tailwind.config.js` file exists in the project root (test -f tailwind.config.js returns non-zero)
    - NO `pages/` directory exists (test -d app/pages returns non-zero)
    - `pnpm build` completes without errors (exit code 0)
  </acceptance_criteria>

  <done>App shell with design tokens, brand colors, Inter font via @nuxt/fonts, stub components, and useLeadForm composable created. `pnpm build` succeeds.</done>
</task>

</tasks>

<verification>
End-to-end check for Plan 01:

```bash
# 1. Build and preview work
cd /home/michel/projects/codebase/agencia201/flyupmilhas/site-flyupmilhas
pnpm build && echo "BUILD OK"

# 2. Correct Nuxt 4 structure enforced
grep -q "cloudflare_pages" nuxt.config.ts && echo "Preset OK"
grep -q "compatibilityVersion: 4" nuxt.config.ts && echo "Nuxt 4 mode OK"
test ! -d app/pages && echo "No pages/ dir OK"
test ! -f tailwind.config.js && echo "No tailwind.config.js OK"

# 3. Design tokens present
grep -q "@theme" app/assets/css/main.css && echo "Tokens OK"
grep -q "#1a3a5c" app/assets/css/main.css && echo "Brand primary color OK"
grep -q "#e67e22" app/assets/css/main.css && echo "CTA color OK"

# 4. Env vars defined
grep -q "apiBase" nuxt.config.ts && echo "runtimeConfig OK"
grep -q "NUXT_PUBLIC_API_BASE" .env.example && echo ".env.example OK"
```
</verification>

<success_criteria>
- `pnpm build` exits 0 with no errors
- `pnpm preview` serves the scaffold page at localhost:3000 with no browser console errors
- `nuxt.config.ts` uses `cloudflare_pages` preset and `compatibilityVersion: 4`
- CSS `@theme {}` block defines all 6 brand tokens (primary, bg, cta, cta-hover, text, text-muted)
- No `pages/` directory, no `tailwind.config.js`, no separately-installed `tailwindcss` in package.json
- `.env.example` committed with both env var keys (empty values)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-infrastructure/01-01-SUMMARY.md` using the summary template.
</output>

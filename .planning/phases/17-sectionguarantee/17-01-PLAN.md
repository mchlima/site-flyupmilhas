---
phase: 17-sectionguarantee
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/Section/SectionGuarantee.vue
  - app/app.vue
  - app/components/Section/SectionPrice.vue
autonomous: true
requirements:
  - GAR-01
  - GAR-02
  - GAR-03

must_haves:
  truths:
    - "Ao scrollar a pagina, o visitante encontra uma secao com selo dourado entre SectionPrice e SectionFAQ"
    - "O copy comunica claramente 7 dias, 100% do valor devolvido, sem ambiguidade"
    - "A secao usa fundo escuro (gradient-form) onde o selo PNG com cantos pretos fica invisivel"
    - "O inline guarantee block em SectionPrice esta reduzido a uma linha, sem duplicar o copy completo"
  artifacts:
    - path: "app/components/Section/SectionGuarantee.vue"
      provides: "Dedicated guarantee section with golden seal and trust copy"
      min_lines: 20
    - path: "app/app.vue"
      provides: "SectionGuarantee wired between SectionPrice and SectionFAQ"
      contains: "SectionGuarantee"
    - path: "app/components/Section/SectionPrice.vue"
      provides: "Reduced inline guarantee (one-liner only)"
  key_links:
    - from: "app/app.vue"
      to: "app/components/Section/SectionGuarantee.vue"
      via: "Nuxt auto-import of <SectionGuarantee />"
      pattern: "<SectionGuarantee"
    - from: "app/components/Section/SectionGuarantee.vue"
      to: "app/assets/img/selo-garantia7-dias.png"
      via: "img src with ~/assets/ alias"
      pattern: "selo-garantia7-dias"
---

<objective>
Create SectionGuarantee.vue — a dedicated dark-background guarantee section with the golden 7-day seal and trust copy — and wire it into app.vue between SectionPrice and SectionFAQ. Reduce the existing inline guarantee in SectionPrice to a one-liner to avoid redundancy.

Purpose: Place a high-visibility risk-reversal element at the decision point in the conversion funnel (after price, before objection handling in FAQ).
Output: One new component, two edited files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-sectionguarantee/17-CONTEXT.md
@.planning/phases/17-sectionguarantee/17-RESEARCH.md
@app/components/Section/SectionPrice.vue
@app/app.vue
@app/assets/css/main.css

<interfaces>
<!-- Existing patterns the executor needs -->

From app/app.vue (insertion point — lines 46-47):
```html
<SectionPrice />
<!-- INSERT SectionGuarantee HERE -->
<SectionFAQ />
```

From app/components/Section/SectionPrice.vue (inline guarantee block to reduce — lines 53-60):
```html
<!-- Guarantee block -->
<div class="mt-6 flex items-center gap-3 text-white/80">
  <UIcon name="i-heroicons-shield-check" class="w-8 h-8 text-[var(--color-brand-cta)] shrink-0" />
  <div>
    <p class="font-semibold text-white">Garantia de 7 dias</p>
    <p class="text-sm text-white/70">Se nao fizer sentido pra voce, devolvemos 100% do valor.</p>
  </div>
</div>
```

From app/assets/css/main.css (gradient utility):
```css
@utility gradient-form {
  background: linear-gradient(135deg, #0F172A 0%, #1E3A8A 100%);
}
```

Design tokens (Phase 15):
- `--color-brand-dark`: #0F172A
- `--color-brand-primary`: #1D4ED8
- `--color-brand-cta`: #0891B2

Typography convention (Phase 16):
- h2: `text-2xl md:text-3xl font-semibold tracking-[-0.015em]`
- Body on dark: `text-white/80 text-base leading-relaxed`

Copy constants (STATE.md):
- Guarantee: "7 dias — 100% devolvido"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SectionGuarantee.vue component</name>
  <files>app/components/Section/SectionGuarantee.vue</files>
  <read_first>
    - app/components/Section/SectionPrice.vue (dark section pattern reference)
    - app/assets/css/main.css (confirm gradient-form utility exists)
    - app/assets/img/ (confirm selo-garantia7-dias.png exists)
  </read_first>
  <action>
Create `app/components/Section/SectionGuarantee.vue` as a pure-display SFC (no script block needed).

**Structure (per D-01, D-02):**
- `<section id="garantia" class="gradient-form py-12 md:py-20 px-6">` — dark gradient background per D-08, D-09
- Inner `<div class="max-w-4xl mx-auto flex flex-col md:flex-row items-center gap-8 md:gap-12">` — stacks on mobile (D-02), side-by-side on md+ (D-01)

**Left column — Seal (per D-03, D-13, D-14):**
- `<div class="shrink-0 flex justify-center">`
- `<img src="~/assets/img/selo-garantia7-dias.png" alt="Selo de garantia incondicional de 7 dias" class="w-48 md:w-64">` — 192px mobile, 256px desktop (within D-03 range of 200-280px)
- Use plain `<img>` NOT `<NuxtImg>` (R2 not confirmed for local assets — see Pitfall 2 in research)
- The PNG has black corners that are hidden by the dark gradient background (D-14)

**Right column — Copy (per D-04, D-05, D-06, D-07, D-10):**
- `<div class="flex flex-col gap-4 text-center md:text-left">`
- h2: `class="text-2xl md:text-3xl font-semibold tracking-[-0.015em] text-white"` with text "Sua tranquilidade e nossa prioridade" (D-05, Claude may adjust slightly)
- Body paragraph: `class="text-white/80 text-base leading-relaxed"` — communicate "7 dias, 100% do valor investido, sem perguntas, sem burocracia" (D-06)
- Reinforcement line: `class="text-white/60 text-sm"` with exact copy constant "7 dias — 100% devolvido" (D-07)

**DO NOT:**
- Add any `<script>` block — this is a pure display component
- Use `<NuxtImg>` — use plain `<img>` with `~/assets/` alias
- Hardcode hex colors — use `gradient-form` utility and `text-white` variants
- Add this section to AppHeader nav links — guarantee is not a navigable destination
- Add animations or interactive elements — pure static trust signal
  </action>
  <verify>
    <automated>ls -la app/components/Section/SectionGuarantee.vue && grep -q 'selo-garantia7-dias' app/components/Section/SectionGuarantee.vue && grep -q 'gradient-form' app/components/Section/SectionGuarantee.vue && grep -q '7 dias' app/components/Section/SectionGuarantee.vue && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "selo-garantia7-dias" app/components/Section/SectionGuarantee.vue returns match
    - grep "gradient-form" app/components/Section/SectionGuarantee.vue returns match
    - grep "7 dias" app/components/Section/SectionGuarantee.vue returns match
    - grep "text-white" app/components/Section/SectionGuarantee.vue returns match
    - grep "md:flex-row" app/components/Section/SectionGuarantee.vue returns match (responsive split layout)
    - grep "md:w-64\|md:w-60" app/components/Section/SectionGuarantee.vue returns match (desktop seal size)
    - File does NOT contain "<script" (pure display, no script block)
    - File does NOT contain "NuxtImg" (plain img tag only)
  </acceptance_criteria>
  <done>SectionGuarantee.vue exists with golden seal image, trust copy mentioning "7 dias" and "100%", dark gradient background, responsive split layout (stacked mobile, side-by-side desktop)</done>
</task>

<task type="auto">
  <name>Task 2: Wire SectionGuarantee into app.vue and reduce SectionPrice inline guarantee</name>
  <files>app/app.vue, app/components/Section/SectionPrice.vue</files>
  <read_first>
    - app/app.vue (current section order, find SectionPrice line)
    - app/components/Section/SectionPrice.vue (locate inline guarantee block lines 53-60)
  </read_first>
  <action>
**app.vue (per D-11, D-12):**
Add `<SectionGuarantee />` on a new line immediately after `<SectionPrice />` and before `<SectionFAQ />`. No import needed — Nuxt auto-imports from `app/components/Section/`.

The section order after edit must be:
```
<SectionPrice />
<SectionGuarantee />
<SectionFAQ />
```

**SectionPrice.vue (per Pitfall 5 in research):**
Reduce the inline guarantee block (currently lines 53-60) to a minimal one-liner. Replace the current two-line copy block with a single reinforcement line. Keep the shield icon and "Garantia de 7 dias" text. Remove the descriptive paragraph `<p class="text-sm text-white/70">Se nao fizer sentido...</p>` since SectionGuarantee now carries the full explanation.

The reduced block should be approximately:
```html
<div class="mt-6 flex items-center gap-3 text-white/80">
  <UIcon name="i-heroicons-shield-check" class="w-6 h-6 text-[var(--color-brand-cta)] shrink-0" />
  <p class="font-medium text-white text-sm">Garantia incondicional de 7 dias</p>
</div>
```
Changes from original: icon shrunk from w-8 h-8 to w-6 h-6, inner `<div>` flattened to single `<p>`, descriptive paragraph removed, font-semibold changed to font-medium, text-sm for subtlety.

**DO NOT:**
- Change any other sections in app.vue
- Add "Garantia" to the AppHeader navigation
- Remove the inline guarantee block entirely — keep the one-liner as immediate reassurance next to the price card
  </action>
  <verify>
    <automated>grep -A1 'SectionPrice' app/app.vue | grep -q 'SectionGuarantee' && grep -c 'SectionGuarantee' app/app.vue | grep -q '1' && grep -c 'text-white/70' app/components/Section/SectionPrice.vue | grep -q '0' && cd /home/michel/projects/codebase/agencia201/flyupmilhas/site-flyupmilhas && pnpm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "SectionGuarantee" app/app.vue returns exactly 1 match
    - grep -A1 "SectionPrice" app/app.vue shows SectionGuarantee on the next line
    - grep -A1 "SectionGuarantee" app/app.vue shows SectionFAQ on the next line
    - grep "text-white/70" app/components/Section/SectionPrice.vue returns 0 matches (descriptive paragraph removed)
    - grep "Garantia" app/components/Section/SectionPrice.vue returns match (one-liner kept)
    - pnpm run build completes without errors
  </acceptance_criteria>
  <done>SectionGuarantee appears between SectionPrice and SectionFAQ in app.vue. SectionPrice inline guarantee reduced to one-liner. Production build succeeds.</done>
</task>

</tasks>

<verification>
1. `pnpm run build` completes without errors (component resolves via auto-import)
2. `grep -n 'SectionGuarantee' app/app.vue` shows exactly one occurrence between SectionPrice and SectionFAQ
3. `grep 'selo-garantia7-dias' app/components/Section/SectionGuarantee.vue` confirms seal image reference
4. `grep '7 dias' app/components/Section/SectionGuarantee.vue` confirms guarantee copy
5. `grep 'gradient-form' app/components/Section/SectionGuarantee.vue` confirms dark gradient background
6. `grep -c 'text-white/70' app/components/Section/SectionPrice.vue` returns 0 (inline guarantee reduced)
</verification>

<success_criteria>
- SectionGuarantee.vue exists as a pure-display component with golden seal + trust copy on dark gradient
- The section appears between Price and FAQ in the page flow
- SectionPrice inline guarantee is reduced to a one-liner (no content duplication)
- Production build passes
</success_criteria>

<output>
After completion, create `.planning/phases/17-sectionguarantee/17-01-SUMMARY.md`
</output>

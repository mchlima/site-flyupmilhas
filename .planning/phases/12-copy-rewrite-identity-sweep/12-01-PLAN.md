---
phase: 12-copy-rewrite-identity-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/Section/SectionHero.vue
  - app/components/Section/SectionAbout.vue
autonomous: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - HERO-04
  - ABOUT-01
  - ABOUT-02
  - ABOUT-03
  - ABOUT-04
  - ABOUT-05

must_haves:
  truths:
    - "Hero displays the exact headline from client PDF"
    - "Hero displays the exact subheadline from client PDF"
    - "Hero CTA reads 'Quero comecar agora'"
    - "Microcopy appears below the Hero CTA button"
    - "SectionAbout title reads 'Viaje mais. Gaste menos. Use milhas do jeito certo.'"
    - "SectionAbout has structured body text (desire + objection + proof)"
    - "SectionAbout has a visually distinct social proof phrase"
    - "SectionAbout shows 3 cards (conforto, economia, estrategia)"
    - "No 'renda extra' text appears in SectionAbout"
  artifacts:
    - path: "app/components/Section/SectionHero.vue"
      provides: "Updated hero copy with new headline, subheadline, CTA, microcopy"
      contains: "Quero começar agora"
    - path: "app/components/Section/SectionAbout.vue"
      provides: "Rewritten About section with title, body, proof, 3 cards"
      contains: "Viaje mais. Gaste menos. Use milhas do jeito certo."
  key_links:
    - from: "app/components/Section/SectionHero.vue"
      to: "#formulario"
      via: "scrollTo('formulario') on CTA click"
      pattern: "scrollTo.*formulario"
    - from: "app/components/Section/SectionAbout.vue"
      to: "#formulario"
      via: "scrollTo('formulario') on CTA click"
      pattern: "scrollTo.*formulario"
---

<objective>
Rewrite Hero and About sections with client-approved copy from the PDF feedback.

Purpose: Align the LP's primary conversion sections (Hero + About) with the client's positioning — selling transformation and real savings, not generic promises.
Output: Updated SectionHero.vue and SectionAbout.vue with exact copy from client PDF.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-copy-rewrite-identity-sweep/12-CONTEXT.md
@app/components/Section/SectionHero.vue
@app/components/Section/SectionAbout.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SectionHero copy (HERO-01, HERO-02, HERO-03, HERO-04)</name>
  <files>app/components/Section/SectionHero.vue</files>
  <read_first>
    - app/components/Section/SectionHero.vue (current state)
    - .planning/phases/12-copy-rewrite-identity-sweep/12-CONTEXT.md (decisions D-01 through D-05)
  </read_first>
  <action>
In SectionHero.vue, make these exact changes:

1. **Headline (h1 tag, currently line 31):** Replace "Sua próxima viagem de classe executiva custa menos que um jantar fora." with:
   "Aprenda a viajar de classe executiva pagando até 80% menos, mesmo começando do zero."
   (per D-01, HERO-01)

2. **Subheadline (p tag, currently lines 34-38):** Replace "Na mentoria Fly Up Milhas, você aprende a acumular e usar milhas com estratégia — do diagnóstico ao seu primeiro voo executivo." with:
   "Um método simples e prático para transformar seus gastos do dia a dia em milhas e viajar mais, gastando menos."
   (per D-02, HERO-02)

3. **CTA button text (currently line 45):** Replace "Quero dar o primeiro passo" with:
   "Quero começar agora"
   (per D-03, HERO-03)

4. **Microcopy (new element):** Add a `<p>` tag below the CTA button `</div>`, inside the content container. Text:
   "Comece a economizar já no primeiro mês"
   Style: `class="text-sm text-white/60 text-center mt-3"`
   (per D-04, HERO-04)

5. **Pain point badges (lines 22-26):** Leave unchanged — per D-05 they are already good.

Do NOT change the background image, overlay, layout, scrollTo behavior, or any structural HTML/CSS beyond the copy changes above.
  </action>
  <verify>
    <automated>grep -n "pagando até 80% menos" app/components/Section/SectionHero.vue && grep -n "Quero começar agora" app/components/Section/SectionHero.vue && grep -n "Comece a economizar já no primeiro mês" app/components/Section/SectionHero.vue && grep -n "transformar seus gastos do dia a dia em milhas" app/components/Section/SectionHero.vue</automated>
  </verify>
  <acceptance_criteria>
    - grep "pagando até 80% menos, mesmo começando do zero" app/components/Section/SectionHero.vue returns 1 match
    - grep "Um método simples e prático para transformar seus gastos do dia a dia" app/components/Section/SectionHero.vue returns 1 match
    - grep "Quero começar agora" app/components/Section/SectionHero.vue returns 1 match
    - grep "Quero dar o primeiro passo" app/components/Section/SectionHero.vue returns 0 matches
    - grep "Comece a economizar já no primeiro mês" app/components/Section/SectionHero.vue returns 1 match
    - grep "Viajar de avião é caro" app/components/Section/SectionHero.vue returns 1 match (badges unchanged)
    - grep "scrollTo" app/components/Section/SectionHero.vue returns 1 match (CTA still scrolls to form)
  </acceptance_criteria>
  <done>Hero displays the exact headline, subheadline, CTA, and microcopy from client PDF. Pain point badges unchanged. CTA scroll behavior preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite SectionAbout layout and copy (ABOUT-01 through ABOUT-05)</name>
  <files>app/components/Section/SectionAbout.vue</files>
  <read_first>
    - app/components/Section/SectionAbout.vue (current state — bento grid with hero card + 2 value cards)
    - .planning/phases/12-copy-rewrite-identity-sweep/12-CONTEXT.md (decisions D-06 through D-12)
  </read_first>
  <action>
Rewrite SectionAbout.vue. Remove the current bento grid (hero card + 2 value cards) and replace with a structured layout: title / body text / social proof phrase / 3 equal cards.

**Section title (per D-06, ABOUT-01):**
```
Viaje mais. Gaste menos. Use milhas do jeito certo.
```
Use the existing h2 styling: `class="text-2xl md:text-3xl font-bold text-[var(--color-brand-primary)] text-center mb-4"`

**Body text (per D-07, D-08, ABOUT-02):**
Below the title, add a paragraph with max-w-3xl mx-auto, centered, text-[var(--color-brand-text-muted)]:
```
A maioria das pessoas acumula milhas, mas não sabe como usar e acaba pagando caro em passagens. Na Fly Up Milhas, você aprende de forma simples, como transformar seus gastos do dia a dia em viagens reais, pagando muito menos.
```

**Social proof phrase (per D-09, ABOUT-03):**
Below the body text, add a visually distinct block. Use a `<p>` with `class="text-center text-base md:text-lg font-semibold text-[var(--color-brand-primary)] bg-[var(--color-brand-primary)]/5 rounded-lg py-4 px-6 max-w-3xl mx-auto mt-6 mb-10"`:
```
Alunos já economizaram milhares de reais em passagens mesmo sem cartão black ou renda alta.
```

**3 cards (per D-10, ABOUT-04):**
Replace the current bento grid with a `grid grid-cols-1 md:grid-cols-3 gap-4 md:gap-6` containing 3 equal cards:

Card 1:
- Icon: `i-heroicons-paper-airplane`
- Title: "Viaje com mais conforto pagando menos"
- Description: "Voe de classe executiva para Europa, Ásia e Américas pagando apenas uma fração do valor original."

Card 2:
- Icon: `i-heroicons-banknotes`
- Title: "Economize nas viagens do dia a dia"
- Description: "Transforme seus gastos comuns em milhas e reduza drasticamente o custo das suas passagens."

Card 3:
- Icon: `i-heroicons-light-bulb`
- Title: "Use suas milhas com estratégia"
- Description: "Aprenda a acumular, organizar e usar seus pontos da forma certa para nunca mais perder oportunidades."

Each card: `class="rounded-xl bg-white shadow-sm border border-gray-100 p-6"` with UIcon `class="w-8 h-8 text-[var(--color-brand-primary)] mb-3"`, h3 `class="text-base font-bold text-[var(--color-brand-primary)] mb-2"`, p `class="text-sm text-[var(--color-brand-text-muted)] leading-relaxed"`.

**Remove (per D-11):** The navy hero card ("Viaje mais. Gaste menos. Ganhe com as suas milhas.") must be completely removed.

**CTA below cards:** Keep the existing CTA button with "Quero dar o primeiro passo" text and scrollTo('formulario') behavior. (Phase 14 will change CTA text later.)

**Verify no "renda extra" (per D-12, ABOUT-05):** Already clean from Phase 11, but do not introduce any new "renda extra" text.

Keep the `<script setup>` with `const { scrollTo } = useScroll()`. Keep `id="sobre"` on the section. Keep the existing section background class `bg-white`.
  </action>
  <verify>
    <automated>grep -n "Viaje mais. Gaste menos. Use milhas do jeito certo." app/components/Section/SectionAbout.vue && grep -n "Alunos já economizaram milhares de reais" app/components/Section/SectionAbout.vue && grep -n "Viaje com mais conforto pagando menos" app/components/Section/SectionAbout.vue && grep -n "Economize nas viagens do dia a dia" app/components/Section/SectionAbout.vue && grep -n "Use suas milhas com estratégia" app/components/Section/SectionAbout.vue && grep -c "renda extra" app/components/Section/SectionAbout.vue; true</automated>
  </verify>
  <acceptance_criteria>
    - grep "Viaje mais. Gaste menos. Use milhas do jeito certo." app/components/Section/SectionAbout.vue returns 1 match
    - grep "A maioria das pessoas acumula milhas" app/components/Section/SectionAbout.vue returns 1 match
    - grep "Alunos já economizaram milhares de reais em passagens mesmo sem cartão black ou renda alta" app/components/Section/SectionAbout.vue returns 1 match
    - grep "Viaje com mais conforto pagando menos" app/components/Section/SectionAbout.vue returns 1 match
    - grep "Economize nas viagens do dia a dia" app/components/Section/SectionAbout.vue returns 1 match
    - grep "Use suas milhas com estratégia" app/components/Section/SectionAbout.vue returns 1 match
    - grep "md:grid-cols-3" app/components/Section/SectionAbout.vue returns 1 match (3-column grid)
    - grep "Ganhe com as suas milhas" app/components/Section/SectionAbout.vue returns 0 matches (old hero card removed)
    - grep "md:col-span-2" app/components/Section/SectionAbout.vue returns 0 matches (no hero card spanning)
    - grep -c "renda extra" app/components/Section/SectionAbout.vue returns 0
    - grep "scrollTo" app/components/Section/SectionAbout.vue returns 1 match (CTA preserved)
    - grep 'id="sobre"' app/components/Section/SectionAbout.vue returns 1 match (anchor preserved)
  </acceptance_criteria>
  <done>SectionAbout displays the new title, structured body text, highlighted social proof phrase, and 3 equal cards (conforto, economia, estrategia). Old hero card removed. No "renda extra" present. CTA and section anchor preserved.</done>
</task>

</tasks>

<verification>
- `grep -n "pagando até 80% menos" app/components/Section/SectionHero.vue` returns a match
- `grep -n "Quero começar agora" app/components/Section/SectionHero.vue` returns a match
- `grep -n "Viaje mais. Gaste menos. Use milhas do jeito certo." app/components/Section/SectionAbout.vue` returns a match
- `grep -c "renda extra" app/components/Section/SectionAbout.vue` returns 0
- `pnpm run build` completes without errors (if available)
</verification>

<success_criteria>
Hero and About sections display the exact copy from the client PDF. Hero CTA says "Quero comecar agora" with microcopy below. About has structured title/body/proof/3-cards layout. No "renda extra" in About. Both sections preserve scroll-to-form CTA behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/12-copy-rewrite-identity-sweep/12-01-SUMMARY.md`
</output>

---
phase: 16-tipografia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - nuxt.config.ts
  - app/assets/css/main.css
  - app/components/Section/SectionHero.vue
  - app/components/Section/SectionAbout.vue
  - app/components/Section/SectionProgramContent.vue
  - app/components/Section/SectionForWhom.vue
  - app/components/Section/SectionMethod.vue
  - app/components/Section/SectionSocialProof.vue
  - app/components/Section/SectionExpert.vue
  - app/components/Section/SectionPrice.vue
  - app/components/Section/SectionFAQ.vue
  - app/components/Section/SectionLeadForm.vue
autonomous: true
requirements:
  - TIPO-01
  - TIPO-02
  - TIPO-03

must_haves:
  truths:
    - "A fonte carregada no browser para todo texto e Plus Jakarta Sans — Inter nao aparece no painel Network"
    - "Headings h1 usam font-bold com tracking-tight; h2 usam font-semibold com tracking personalizado; h3 usam font-medium"
    - "Body text mantém font-weight 400 e tracking normal — sem alteracao de legibilidade"
    - "CLS < 0.1 em nuxt preview (build de producao)"
  artifacts:
    - path: "nuxt.config.ts"
      provides: "fonts.families config for Plus Jakarta Sans"
      contains: "Plus Jakarta Sans"
    - path: "app/assets/css/main.css"
      provides: "Updated --font-family-sans token"
      contains: "Plus Jakarta Sans"
  key_links:
    - from: "nuxt.config.ts"
      to: "app/assets/css/main.css"
      via: "fonts.families declares font, @theme token references it"
      pattern: "Plus Jakarta Sans"
    - from: "app/assets/css/main.css"
      to: "html base rule"
      via: "font-family: var(--font-family-sans)"
      pattern: "font-family-sans"
---

<objective>
Substituir Inter por Plus Jakarta Sans em toda a LP e aprimorar a hierarquia tipografica com pesos e tracking diferenciados por nivel de heading.

Purpose: A tipografia atual (Inter, pesos uniformes) nao transmite a identidade premium desejada. Plus Jakarta Sans com hierarquia refinada cria impacto visual nos headings sem sacrificar legibilidade do body text.

Output: Font swap completo (config + CSS token), heading hierarchy aplicada em todos os componentes, CLS < 0.1 verificado.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-tipografia/16-CONTEXT.md
@.planning/phases/16-tipografia/16-RESEARCH.md

<interfaces>
<!-- Current state of the two config files that Task 1 modifies -->

From nuxt.config.ts (full file — no fonts section exists yet):
```typescript
export default defineNuxtConfig({
  compatibilityDate: '2025-07-15',
  future: { compatibilityVersion: 4 },
  modules: ['@nuxt/ui', '@nuxt/image', '@nuxtjs/seo', '@nuxt/eslint'],
  ssr: true,
  site: { url: 'https://flyupmilhas.com.br', name: 'Fly Up Milhas...', defaultLocale: 'pt-BR', description: '...' },
  runtimeConfig: { public: { apiBase: '', r2BaseUrl: '' } },
  css: ['~/assets/css/main.css'],
  image: { quality: 85, format: ['webp'] },
})
```

From app/assets/css/main.css line 16-17 (current font token):
```css
/* Typography (per D-05) */
--font-family-sans: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
```

Current heading classes across components (grep audit):
- h1 (SectionHero): `text-3xl md:text-5xl font-bold text-white leading-tight mb-6`
- h2 (8 sections): `text-2xl md:text-3xl font-bold text-[var(--color-brand-primary)]` (+ variants with text-white, text-center)
- h2 (SectionExpert): `text-2xl md:text-3xl font-bold text-[var(--color-brand-primary)] mb-4`
- h3 (SectionAbout): `text-base font-bold text-[var(--color-brand-primary)] mb-2`
- h3 (SectionMethod): `text-lg font-semibold mb-2` (already semibold)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atomic font swap — nuxt.config.ts + main.css</name>
  <files>nuxt.config.ts, app/assets/css/main.css</files>
  <read_first>
    - nuxt.config.ts
    - app/assets/css/main.css
    - .planning/phases/16-tipografia/16-RESEARCH.md (Pattern 1: Atomic Font Swap section)
  </read_first>
  <action>
    CRITICAL: Both files MUST be updated in the same operation to avoid font double-load (Pitfall V3 per D-03).

    1. **nuxt.config.ts** — Add `fonts` key to the config object (after `image` block, before closing `}`):
    ```typescript
    fonts: {
      families: [
        {
          name: 'Plus Jakarta Sans',
          provider: 'google',
          weights: [400, 500, 600, 700],
          styles: ['normal'],
        }
      ]
    },
    ```
    Do NOT add weight 800 — deferred per scope. Do NOT keep Inter in any form.

    2. **app/assets/css/main.css** — Replace line 17 (the `--font-family-sans` value inside `@theme {}`):
    - OLD: `--font-family-sans: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;`
    - NEW: `--font-family-sans: 'Plus Jakarta Sans', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;`
    - Update the comment above it from `(per D-05)` to `Plus Jakarta Sans (D-01, D-02)`
    - Leave ALL other tokens (colors, gradients, utilities, html rule) unchanged.

    Per D-04: Inter must be completely removed. Since Inter was only loaded implicitly via Nuxt UI defaults and the @theme token, these two changes are sufficient for complete removal.
    Per D-11: @nuxt/fonts generates fallback metrics automatically — do NOT add manual font-display rules.
  </action>
  <verify>
    <automated>cd /home/michel/projects/codebase/agencia201/flyupmilhas/site-flyupmilhas && grep -c "Plus Jakarta Sans" nuxt.config.ts app/assets/css/main.css && grep -c "Inter" nuxt.config.ts app/assets/css/main.css</automated>
  </verify>
  <acceptance_criteria>
    - grep "Plus Jakarta Sans" nuxt.config.ts returns match (fonts.families config present)
    - grep "Plus Jakarta Sans" app/assets/css/main.css returns match (token updated)
    - grep "Inter" nuxt.config.ts returns 0 matches (no Inter reference in config)
    - grep "'Inter'" app/assets/css/main.css returns 0 matches (no Inter in font token)
    - fonts.families array contains weights [400, 500, 600, 700] only — no 800
    - provider is 'google'
  </acceptance_criteria>
  <done>nuxt.config.ts has fonts.families declaring Plus Jakarta Sans with weights 400/500/600/700 via google provider. main.css --font-family-sans token starts with 'Plus Jakarta Sans'. Zero references to 'Inter' in either file.</done>
</task>

<task type="auto">
  <name>Task 2: Heading hierarchy — weight and tracking across all components</name>
  <files>
    app/components/Section/SectionHero.vue,
    app/components/Section/SectionAbout.vue,
    app/components/Section/SectionProgramContent.vue,
    app/components/Section/SectionForWhom.vue,
    app/components/Section/SectionMethod.vue,
    app/components/Section/SectionSocialProof.vue,
    app/components/Section/SectionExpert.vue,
    app/components/Section/SectionPrice.vue,
    app/components/Section/SectionFAQ.vue,
    app/components/Section/SectionLeadForm.vue
  </files>
  <read_first>
    - app/components/Section/SectionHero.vue
    - app/components/Section/SectionAbout.vue
    - app/components/Section/SectionProgramContent.vue
    - app/components/Section/SectionForWhom.vue
    - app/components/Section/SectionMethod.vue
    - app/components/Section/SectionSocialProof.vue
    - app/components/Section/SectionExpert.vue
    - app/components/Section/SectionPrice.vue
    - app/components/Section/SectionFAQ.vue
    - app/components/Section/SectionLeadForm.vue
    - .planning/phases/16-tipografia/16-RESEARCH.md (Pattern 3: Component Heading Inventory)
  </read_first>
  <action>
    Apply heading hierarchy per decisions D-05 through D-09. Each heading level gets specific weight and tracking:

    **h1 elements (D-05): font-bold + tracking-tight**
    - SectionHero.vue h1: ADD `tracking-tight` to existing classes. Keep `font-bold` (weight 700). Result: `text-3xl md:text-5xl font-bold tracking-tight text-white leading-tight mb-6`

    **h2 elements (D-06): font-bold -> font-semibold + tracking-[-0.015em]**
    Apply to ALL h2 elements — change `font-bold` to `font-semibold` and ADD `tracking-[-0.015em]`:
    - SectionAbout.vue h2: `text-2xl md:text-3xl font-semibold tracking-[-0.015em] text-[var(--color-brand-primary)] text-center mb-4`
    - SectionProgramContent.vue h2: `text-2xl md:text-3xl font-semibold tracking-[-0.015em] text-[var(--color-brand-primary)]`
    - SectionForWhom.vue h2: `text-2xl md:text-3xl font-semibold tracking-[-0.015em] text-[var(--color-brand-primary)] text-center mb-10`
    - SectionMethod.vue h2: `text-2xl md:text-3xl font-semibold tracking-[-0.015em] text-[var(--color-brand-primary)] text-center mb-4`
    - SectionSocialProof.vue h2: `text-2xl md:text-3xl font-semibold tracking-[-0.015em] text-[var(--color-brand-primary)] text-center mb-4`
    - SectionExpert.vue h2: `text-2xl md:text-3xl font-semibold tracking-[-0.015em] text-[var(--color-brand-primary)] mb-4`
    - SectionPrice.vue h2: `text-2xl md:text-3xl font-semibold tracking-[-0.015em] text-white`
    - SectionFAQ.vue h2: `text-2xl md:text-3xl font-semibold tracking-[-0.015em] text-[var(--color-brand-primary)] text-center mb-4`
    - SectionLeadForm.vue h2: `text-2xl font-semibold tracking-[-0.015em] text-white mb-2`

    **h3 elements (D-07): font-bold -> font-medium, tracking normal**
    - SectionAbout.vue h3 (3 instances, lines ~27, 36, 45): Change `font-bold` to `font-medium`. Result: `text-base font-medium text-[var(--color-brand-primary)] mb-2`
    - SectionMethod.vue h3: Already `font-semibold` — change to `font-medium` per D-07. Result: `text-lg font-medium mb-2`

    **DO NOT change:**
    - Body text (p elements) — D-08 locks body at weight 400, tracking normal (already default)
    - CTA buttons — keep `font-semibold` as established pattern
    - Non-heading font-bold elements (e.g., SectionMethod stat values "30 dias", "3 encontros") — these are data emphasis, not headings
    - AppHeader.vue — no heading elements; mobile menu title `font-bold` is navigation, not content hierarchy
    - SectionPrice.vue price value `text-4xl md:text-5xl font-bold` — this is a price display, not a heading
  </action>
  <verify>
    <automated>cd /home/michel/projects/codebase/agencia201/flyupmilhas/site-flyupmilhas && echo "=== h1 tracking ===" && grep -n "tracking-tight" app/components/Section/SectionHero.vue && echo "=== h2 font-semibold ===" && grep -rn "h2.*font-semibold" app/components/Section/ && echo "=== h2 tracking ===" && grep -rn "h2.*tracking-\[-0.015em\]" app/components/Section/ | wc -l && echo "=== h3 font-medium ===" && grep -rn "h3.*font-medium" app/components/Section/ && echo "=== NO h2 font-bold ===" && grep -rn "<h2.*font-bold" app/components/Section/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep "tracking-tight" SectionHero.vue returns match on the h1 element
    - grep "<h2.*font-bold" across all Section components returns 0 matches (all h2s changed to font-semibold)
    - grep "tracking-\[-0.015em\]" across Section components returns 9 matches (one per h2)
    - grep "<h3.*font-medium" in SectionAbout.vue returns 3 matches
    - grep "h3.*font-medium" in SectionMethod.vue returns 1 match
    - grep "<h3.*font-bold" across Section components returns 0 matches
    - No tracking classes added to p elements or body text
    - CTA buttons retain font-semibold unchanged
  </acceptance_criteria>
  <done>All h1 elements have tracking-tight. All h2 elements use font-semibold with tracking-[-0.015em]. All h3 elements use font-medium with no tracking. Body text and non-heading elements unchanged. Typography hierarchy is visually distinct across 3 heading levels.</done>
</task>

</tasks>

<verification>
1. **Font swap verification:** `pnpm run build` succeeds without errors. On `pnpm run preview`, browser DevTools Network tab shows only `plus-jakarta-sans-*.woff2` requests — zero `inter-*.woff2` requests.
2. **CLS verification (TIPO-03 exit gate):** Run Lighthouse on the preview server URL (`http://localhost:3000`) in Chrome DevTools. CLS metric must be < 0.1. This is the phase exit gate per D-10.
3. **Hierarchy verification:** Visual inspection confirms h1 > h2 > h3 weight progression is clear. Headings appear more impactful than before while body text readability is unchanged.
</verification>

<success_criteria>
- Plus Jakarta Sans is the only web font downloaded (TIPO-01)
- h1 bold+tight, h2 semibold+custom tracking, h3 medium+normal — clear visual hierarchy (TIPO-02)
- CLS < 0.1 on `nuxt preview` Lighthouse audit (TIPO-03)
- Zero references to 'Inter' as a font family in the codebase config
</success_criteria>

<output>
After completion, create `.planning/phases/16-tipografia/16-01-SUMMARY.md`
</output>

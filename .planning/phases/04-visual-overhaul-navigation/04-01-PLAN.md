---
phase: 04-visual-overhaul-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/assets/css/main.css
  - app/components/App/AppHeader.vue
autonomous: true
requirements:
  - VISL-01
  - NAV-01
  - NAV-02
  - NAV-03

must_haves:
  truths:
    - "Page background is #F9FAFB with no yellow tint visible"
    - "Desktop header shows anchor links (Sobre, Como Funciona, Depoimentos, FAQ) and an orange CTA button"
    - "Mobile header shows a hamburger icon that opens a fullscreen overlay with centered nav links and CTA"
    - "Header hides on scroll down and reappears on scroll up (smart sticky)"
    - "Clicking an anchor link scrolls to the correct section without the fixed header obscuring content"
    - "Mobile menu closes on anchor link tap, Escape key, or X icon tap"
  artifacts:
    - path: "app/assets/css/main.css"
      provides: "Updated background token and scroll-margin-top rule"
      contains: "--color-brand-bg: #F9FAFB"
    - path: "app/components/App/AppHeader.vue"
      provides: "Smart sticky header with desktop nav and mobile hamburger menu"
      min_lines: 60
  key_links:
    - from: "app/components/App/AppHeader.vue"
      to: "app/composables/useScroll.ts"
      via: "useScroll() composable import"
      pattern: "useScroll\\(\\)"
    - from: "app/components/App/AppHeader.vue"
      to: "app/assets/css/main.css"
      via: "CSS custom properties"
      pattern: "var\\(--color-brand-primary\\)"
---

<objective>
Fix page background color token and rebuild AppHeader with smart sticky navigation, desktop anchor links, mobile hamburger menu, and highlighted CTA button.

Purpose: The header is currently a minimal placeholder with only a logo. This plan transforms it into a fully functional navigation component with smart-sticky scroll behavior and responsive mobile menu, while also fixing the background color from the slightly warm #f8f9fa to the cool neutral #F9FAFB (Tailwind gray-50).

Output: Updated main.css with new background token and scroll-margin-top rule; fully rebuilt AppHeader.vue with smart sticky header, desktop nav links, mobile fullscreen overlay menu, and orange CTA button.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-visual-overhaul-navigation/04-CONTEXT.md
@.planning/phases/04-visual-overhaul-navigation/04-RESEARCH.md
@.planning/phases/04-visual-overhaul-navigation/04-UI-SPEC.md

<interfaces>
<!-- useScroll composable — already exists, reuse for anchor navigation -->
From app/composables/useScroll.ts:
```typescript
export function useScroll() {
  function scrollTo(id: string) {
    if (import.meta.client) {
      const el = document.getElementById(id)
      el?.scrollIntoView({ behavior: 'smooth', block: 'start' })
    }
  }
  return { scrollTo }
}
```

<!-- Current main.css tokens — the source of truth for design tokens -->
From app/assets/css/main.css:
```css
@theme {
  --color-brand-primary: #1a3a5c;
  --color-brand-bg: #f8f9fa;          /* TO CHANGE -> #F9FAFB */
  --color-brand-cta: #e67e22;
  --color-brand-cta-hover: #d35400;
  --color-brand-text: #1a1a1a;
  --color-brand-text-muted: #6b7280;
  --font-family-sans: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
}
```

<!-- Current AppHeader — minimal placeholder to be completely replaced -->
From app/components/App/AppHeader.vue:
```vue
<template>
  <header class="py-4 px-6 bg-white border-b border-gray-100">
    <div class="max-w-5xl mx-auto flex items-center justify-between">
      <span class="font-semibold text-[var(--color-brand-primary)] text-lg">
        Fly Up Milhas
      </span>
    </div>
  </header>
</template>
```

<!-- Anchor IDs used across section components -->
Section anchor IDs: sobre (SectionAbout), como-funciona (SectionMethod), depoimentos (SectionSocialProof), faq (SectionFAQ), formulario (app.vue wrapper)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CSS tokens and add scroll-margin-top rule</name>
  <files>app/assets/css/main.css</files>
  <read_first>
    - app/assets/css/main.css
  </read_first>
  <action>
    In `app/assets/css/main.css`, make two changes:

    1. Per D-01: Inside the `@theme {}` block, change `--color-brand-bg: #f8f9fa;` to `--color-brand-bg: #F9FAFB;` (Tailwind gray-50, neutral cool tone). The `html` rule already uses `var(--color-brand-bg)` so it updates automatically (D-02).

    2. Per 04-RESEARCH.md Pattern 5: Add a new CSS rule AFTER the `html {}` block:
    ```css
    /* Fixed header anchor offset — prevents content hiding behind 64px header */
    section[id] {
      scroll-margin-top: 72px;
    }
    ```
    This ensures anchor navigation from the smart sticky header does not obscure section content (72px = 64px header height + 8px breathing room).

    Do NOT change any other tokens or rules in the file. The `html` background-color already references `var(--color-brand-bg)` and will pick up the new value automatically.
  </action>
  <verify>
    <automated>grep -q "brand-bg: #F9FAFB" app/assets/css/main.css && grep -q "scroll-margin-top: 72px" app/assets/css/main.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - app/assets/css/main.css contains `--color-brand-bg: #F9FAFB;` (not #f8f9fa)
    - app/assets/css/main.css contains `section[id] { scroll-margin-top: 72px; }`
    - No other tokens changed (--color-brand-primary still #1a3a5c, --color-brand-cta still #e67e22)
    - html background-color still uses `var(--color-brand-bg)` (unchanged reference)
  </acceptance_criteria>
  <done>Background token updated to #F9FAFB and scroll-margin-top rule added for anchored sections</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild AppHeader with smart sticky nav and mobile hamburger menu</name>
  <files>app/components/App/AppHeader.vue</files>
  <read_first>
    - app/components/App/AppHeader.vue
    - app/composables/useScroll.ts
    - app/assets/css/main.css
    - .planning/phases/04-visual-overhaul-navigation/04-RESEARCH.md
    - .planning/phases/04-visual-overhaul-navigation/04-UI-SPEC.md
  </read_first>
  <action>
    Completely replace the contents of `app/components/App/AppHeader.vue` with a full rebuild implementing smart sticky header, desktop anchor navigation, and mobile hamburger menu. Use the exact code pattern from 04-RESEARCH.md "Smart Sticky Header (complete AppHeader structure)" section as the base, with these specifics:

    **Script setup:**
    - Import `useScroll` composable: `const { scrollTo } = useScroll()`
    - Reactive refs: `isVisible = ref(true)`, `isMenuOpen = ref(false)`, `lastScrollY = ref(0)`, `SCROLL_THRESHOLD = 64`
    - Smart sticky scroll listener inside `if (import.meta.client) {}` block with `{ passive: true }` option:
      - If `currentY < SCROLL_THRESHOLD`: always visible
      - Else: visible only when `currentY < lastScrollY.value` (scrolling up)
    - Escape key listener inside same `import.meta.client` guard: closes menu on Escape
    - `navLinks` array: `[{ label: 'Sobre', anchor: 'sobre' }, { label: 'Como Funciona', anchor: 'como-funciona' }, { label: 'Depoimentos', anchor: 'depoimentos' }, { label: 'FAQ', anchor: 'faq' }]`
    - `navigate(anchor: string)` function: sets `isMenuOpen.value = false` then calls `scrollTo(anchor)`

    **Template — header element (per D-11, D-12):**
    - `<header>` with classes: `fixed top-0 left-0 right-0 z-50 px-6 py-4 bg-[var(--color-brand-primary)] transition-transform duration-300`
    - Dynamic class: `isVisible ? 'translate-y-0' : '-translate-y-full'`
    - Inner `<div class="max-w-5xl mx-auto flex items-center justify-between">`
    - Logo: `<span class="font-semibold text-white text-lg">Fly Up Milhas</span>`

    **Template — desktop nav (per D-13, NAV-01, NAV-02):**
    - `<nav class="hidden md:flex items-center gap-6">`
    - `v-for="link in navLinks"` rendering `<button>` elements with classes: `text-white/80 hover:text-white text-sm font-medium transition-colors cursor-pointer`
    - Each button calls `navigate(link.anchor)` on click
    - CTA button after nav links: `<button class="bg-[var(--color-brand-cta)] hover:bg-[var(--color-brand-cta-hover)] text-white font-semibold px-5 py-2 rounded-lg text-sm cursor-pointer transition-colors min-h-[44px]" @click="scrollTo('formulario')">Quero minha Consultoria</button>`

    **Template — mobile hamburger button (per D-14, D-17, NAV-03):**
    - `<button class="md:hidden text-white min-h-[44px] min-w-[44px] flex items-center justify-center cursor-pointer">`
    - `aria-label` toggles: `isMenuOpen ? 'Fechar menu' : 'Abrir menu'`
    - `:aria-expanded="isMenuOpen"`
    - Contains `<UIcon :name="isMenuOpen ? 'i-heroicons-x-mark' : 'i-heroicons-bars-3'" class="w-6 h-6" />`
    - Click handler: `@click="isMenuOpen = !isMenuOpen"`

    **Template — fullscreen mobile overlay (per D-14, D-15):**
    - `<div class="fixed inset-0 z-40 flex flex-col items-center justify-center bg-[var(--color-brand-primary)] transition-opacity duration-200">`
    - Dynamic class: `isMenuOpen ? 'opacity-100 pointer-events-auto' : 'opacity-0 pointer-events-none'`
    - `role="dialog"`, `aria-modal="true"`, `:aria-hidden="!isMenuOpen"`
    - Inner `<nav class="flex flex-col items-center gap-8">`
    - `v-for="link in navLinks"` rendering `<button class="text-white text-xl font-bold cursor-pointer" @click="navigate(link.anchor)">{{ link.label }}</button>`
    - CTA button at bottom: `<button class="mt-4 bg-[var(--color-brand-cta)] hover:bg-[var(--color-brand-cta-hover)] text-white font-semibold px-8 py-4 rounded-lg text-lg cursor-pointer transition-colors w-full max-w-xs" @click="navigate('formulario')">Quero minha Consultoria</button>`

    **Important constraints:**
    - Do NOT use `process.client` — use `import.meta.client` only (Nuxt 4 convention per Phase 1)
    - Do NOT use `v-if` on the overlay — use opacity/pointer-events pattern (avoids DOM create/destroy, SSR-safe)
    - Do NOT use any icon prefix other than `i-heroicons-*` (only installed set)
    - Hamburger touch target must be at least 44px (WCAG 2.5.5 AA): enforce with `min-h-[44px] min-w-[44px]`
  </action>
  <verify>
    <automated>grep -q "import.meta.client" app/components/App/AppHeader.vue && grep -q "useScroll" app/components/App/AppHeader.vue && grep -q "translate-y-full" app/components/App/AppHeader.vue && grep -q "Quero minha Consultoria" app/components/App/AppHeader.vue && grep -q "i-heroicons-bars-3" app/components/App/AppHeader.vue && grep -q "i-heroicons-x-mark" app/components/App/AppHeader.vue && grep -q "pointer-events-none" app/components/App/AppHeader.vue && grep -q "aria-modal" app/components/App/AppHeader.vue && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - AppHeader.vue contains `import.meta.client` (NOT process.client)
    - AppHeader.vue contains `useScroll()` composable usage
    - AppHeader.vue contains smart sticky logic with `-translate-y-full` class binding
    - AppHeader.vue contains `bg-[var(--color-brand-primary)]` on header element
    - AppHeader.vue contains 4 nav links: Sobre, Como Funciona, Depoimentos, FAQ
    - AppHeader.vue contains CTA button with text "Quero minha Consultoria" and `bg-[var(--color-brand-cta)]`
    - AppHeader.vue contains hamburger icon `i-heroicons-bars-3` and close icon `i-heroicons-x-mark`
    - AppHeader.vue contains fullscreen overlay with `opacity-0 pointer-events-none` / `opacity-100 pointer-events-auto` pattern (NOT v-if)
    - AppHeader.vue contains `aria-modal="true"` on overlay and `aria-label` on hamburger button
    - AppHeader.vue contains `min-h-[44px]` on hamburger button (WCAG touch target)
    - AppHeader.vue contains `{ passive: true }` on scroll event listener
    - AppHeader.vue does NOT contain `process.client`
  </acceptance_criteria>
  <done>AppHeader fully rebuilt with smart sticky behavior, desktop anchor nav with 4 links + CTA, mobile hamburger with fullscreen overlay, SSR-safe scroll detection, and WCAG-compliant touch targets</done>
</task>

</tasks>

<verification>
- `grep "brand-bg: #F9FAFB" app/assets/css/main.css` returns a match
- `grep "scroll-margin-top" app/assets/css/main.css` returns a match
- `grep -c "import.meta.client" app/components/App/AppHeader.vue` returns at least 1
- `grep "process.client" app/components/App/AppHeader.vue` returns NO match
- `pnpm nuxt build` completes without errors (SSR build validates no hydration issues at build time)
</verification>

<success_criteria>
- Page background renders as #F9FAFB (Tailwind gray-50) with no yellow tint
- Desktop header at 768px+ shows logo, 4 anchor links, and orange CTA button on navy background
- Mobile header below 768px shows logo and hamburger icon; tapping hamburger opens fullscreen navy overlay with centered links and CTA
- Header hides when scrolling down past 64px and reappears when scrolling up
- Tapping any nav link scrolls smoothly to the target section and closes mobile menu
- Escape key closes mobile menu
- No hydration warnings in `nuxt build && nuxt preview` console output
</success_criteria>

<output>
After completion, create `.planning/phases/04-visual-overhaul-navigation/04-01-SUMMARY.md`
</output>

---
phase: 04-visual-overhaul-navigation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/Section/SectionAbout.vue
  - app/components/Section/SectionSocialProof.vue
  - app/components/Section/SectionFAQ.vue
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - VISL-03

must_haves:
  truths:
    - "SectionAbout component exists with company-focused content (not personal bio)"
    - "SectionAbout presents three value props at equal billing: viagens executivas, economia familiar, renda extra com milhas"
    - "SectionAbout uses bento grid layout with varied card sizes and rounded-xl corners"
    - "SectionSocialProof no longer shows the comprovante de resultado screenshot"
    - "SectionSocialProof still shows both testimonial cards unchanged"
    - "FAQ accordion content text uses #1a1a1a (not muted gray) passing WCAG AA 4.5:1"
  artifacts:
    - path: "app/components/Section/SectionAbout.vue"
      provides: "Company story section with 3 value prop bento cards"
      min_lines: 40
    - path: "app/components/Section/SectionSocialProof.vue"
      provides: "Testimonial cards without comprovante screenshot"
    - path: "app/components/Section/SectionFAQ.vue"
      provides: "FAQ with WCAG AA compliant text contrast"
      contains: "color-brand-text"
  key_links:
    - from: "app/components/Section/SectionAbout.vue"
      to: "app/assets/css/main.css"
      via: "CSS custom properties"
      pattern: "var\\(--color-brand-primary\\)"
    - from: "app/components/Section/SectionFAQ.vue"
      to: "app/assets/css/main.css"
      via: "CSS custom property override"
      pattern: "var\\(--color-brand-text\\)"
---

<objective>
Create SectionAbout component (replacing SectionExpert concept), remove comprovante screenshot from SectionSocialProof, and fix FAQ text contrast to WCAG AA compliance.

Purpose: Transform the "specialist bio" into a company-focused value proposition section with equal billing for renda extra com milhas, remove the placeholder comprovante screenshot that adds no trust value, and ensure all FAQ text is readable with proper contrast.

Output: New SectionAbout.vue component with bento grid layout; cleaned SectionSocialProof.vue without comprovante block; updated SectionFAQ.vue with WCAG AA compliant text color.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-visual-overhaul-navigation/04-CONTEXT.md
@.planning/phases/04-visual-overhaul-navigation/04-RESEARCH.md
@.planning/phases/04-visual-overhaul-navigation/04-UI-SPEC.md

<interfaces>
<!-- SectionExpert.vue — reference for what we're replacing (company section, NOT bio) -->
From app/components/Section/SectionExpert.vue:
```vue
<script setup lang="ts">
const config = useRuntimeConfig()
const r2Base = config.public.r2BaseUrl
const { scrollTo } = useScroll()
</script>
<template>
  <section id="especialista" class="bg-white py-12 md:py-24 px-6">
    <!-- 2-col grid with photo + bio + stats + CTA -->
  </section>
</template>
```
Section ID: "especialista" (changes to "sobre" in new component per D-24)

<!-- SectionSocialProof.vue — current structure showing comprovante block to remove -->
From app/components/Section/SectionSocialProof.vue:
```vue
<!-- Lines 55-68: comprovante block to REMOVE per D-25 -->
<div class="text-center">
  <p class="text-sm text-[var(--color-brand-text-muted)] mb-4 uppercase tracking-wide font-medium">
    Comprovante de resultado
  </p>
  <NuxtImg
    :src="`${r2Base}/resultado-placeholder.webp`"
    width="800" height="450"
    alt="Print de passagem executiva..."
    class="rounded-xl shadow-lg mx-auto max-w-2xl w-full"
  />
</div>
```

<!-- SectionFAQ.vue — current UAccordion without content slot -->
From app/components/Section/SectionFAQ.vue:
```vue
<UAccordion :items="faqItems" />
```
Needs #content slot override for text color per D-03.

<!-- Design tokens from main.css -->
--color-brand-primary: #1a3a5c (navy)
--color-brand-bg: #F9FAFB (off-white, updated in Plan 01)
--color-brand-cta: #e67e22 (orange)
--color-brand-text: #1a1a1a (body text — WCAG AA target for FAQ fix)
--color-brand-text-muted: #6b7280 (gray — current FAQ color, insufficient contrast)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SectionAbout component with bento grid value props</name>
  <files>app/components/Section/SectionAbout.vue</files>
  <read_first>
    - app/components/Section/SectionExpert.vue
    - app/components/Section/SectionMethod.vue
    - app/assets/css/main.css
    - .planning/phases/04-visual-overhaul-navigation/04-UI-SPEC.md
    - .planning/phases/04-visual-overhaul-navigation/04-CONTEXT.md
  </read_first>
  <action>
    Create a NEW file `app/components/Section/SectionAbout.vue` implementing the company-focused "Sobre a Fly Up Milhas" section with bento grid layout. Per D-18, D-19, D-20, D-21, D-22, D-24.

    **Script setup:**
    - Import `useScroll`: `const { scrollTo } = useScroll()`
    - No `useRuntimeConfig()` needed (no R2 images — icons only per D-21)

    **Template structure:**
    - `<section id="sobre" class="bg-white py-12 md:py-24 px-6">` (anchor id is "sobre" per D-24)
    - Inner `<div class="max-w-5xl mx-auto">`

    **Bento grid (per D-22, UI-SPEC SectionAbout bento card structure):**
    ```html
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 md:gap-6">
      <!-- Hero card: spans 2 cols on md+, navy background per D-06 -->
      <div class="md:col-span-2 rounded-xl bg-[var(--color-brand-primary)] text-white p-8">
        <h2 class="text-2xl md:text-3xl font-bold mb-4">
          Viaje mais. Gaste menos. Ganhe com as suas milhas.
        </h2>
        <p class="text-white/80 leading-relaxed">
          <!-- TODO: replace with final copy from Marcio -->
          A Fly Up Milhas existe para transformar seus gastos do dia a dia em passagens executivas e renda extra.
        </p>
      </div>

      <!-- Value prop 1: Viagens Executivas — 1x1 white card per D-07 -->
      <div class="rounded-xl bg-white shadow-sm p-6">
        <UIcon name="i-heroicons-paper-airplane" class="w-8 h-8 text-[var(--color-brand-primary)] mb-3" />
        <h3 class="text-base font-bold text-[var(--color-brand-primary)] mb-2">Viagens Executivas</h3>
        <p class="text-sm text-[var(--color-brand-text-muted)] leading-relaxed">
          Voe de classe executiva para Europa, Asia e Americas pagando uma fracao do preco de tabela.
        </p>
      </div>

      <!-- Value prop 2: Economia Familiar — 1x1 white card -->
      <div class="rounded-xl bg-white shadow-sm p-6">
        <UIcon name="i-heroicons-banknotes" class="w-8 h-8 text-[var(--color-brand-primary)] mb-3" />
        <h3 class="text-base font-bold text-[var(--color-brand-primary)] mb-2">Economia Familiar</h3>
        <p class="text-sm text-[var(--color-brand-text-muted)] leading-relaxed">
          Transforme gastos do dia a dia em milhas e economize milhares de reais em passagens para toda a familia.
        </p>
      </div>

      <!-- Value prop 3: Renda Extra — 1x1 white card, SAME SIZE as other two per CONT-02 D-20 -->
      <div class="rounded-xl bg-white shadow-sm p-6">
        <UIcon name="i-heroicons-currency-dollar" class="w-8 h-8 text-[var(--color-brand-primary)] mb-3" />
        <h3 class="text-base font-bold text-[var(--color-brand-primary)] mb-2">Renda Extra com Milhas</h3>
        <p class="text-sm text-[var(--color-brand-text-muted)] leading-relaxed">
          Aprenda a gerar renda extra comprando e vendendo milhas de forma estrategica e segura.
        </p>
      </div>
    </div>

    <!-- CTA below grid -->
    <div class="text-center mt-8">
      <button
        class="bg-[var(--color-brand-cta)] hover:bg-[var(--color-brand-cta-hover)] text-white font-semibold px-8 py-4 rounded-lg text-lg cursor-pointer transition-colors"
        @click="scrollTo('formulario')"
      >
        Quero minha Consultoria
      </button>
    </div>
    ```

    **Important constraints:**
    - Do NOT include personal photo or personal bio (per D-18, D-19 — company focus, not individual)
    - Do NOT use `<NuxtImg>` — no R2 images in this section (icons only per D-21)
    - "Renda Extra com Milhas" card must be SAME SIZE as other two value prop cards — equal billing per CONT-02, D-20
    - All three value prop cards must be `rounded-xl` per D-08
    - Only use `i-heroicons-*` icon names (only installed icon set). If `i-heroicons-currency-dollar` renders empty at runtime, use `i-heroicons-banknotes` as fallback (per RESEARCH open question 2)
    - Section anchor id MUST be `sobre` (not `especialista`) per D-24
    - Do NOT delete SectionExpert.vue — it stays in the repo as reference (D-18 note)
  </action>
  <verify>
    <automated>test -f app/components/Section/SectionAbout.vue && grep -q 'id="sobre"' app/components/Section/SectionAbout.vue && grep -q "Renda Extra com Milhas" app/components/Section/SectionAbout.vue && grep -q "Viagens Executivas" app/components/Section/SectionAbout.vue && grep -q "Economia Familiar" app/components/Section/SectionAbout.vue && grep -q "i-heroicons-paper-airplane" app/components/Section/SectionAbout.vue && grep -q "rounded-xl" app/components/Section/SectionAbout.vue && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File app/components/Section/SectionAbout.vue exists
    - Contains `id="sobre"` (NOT "especialista")
    - Contains all three value props: "Viagens Executivas", "Economia Familiar", "Renda Extra com Milhas"
    - Contains bento grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3`
    - Contains navy hero card: `bg-[var(--color-brand-primary)]` with `md:col-span-2`
    - Contains UIcon components with `i-heroicons-` prefix
    - Contains `rounded-xl` on cards (D-08)
    - Contains `useScroll()` composable usage for CTA button
    - Does NOT contain `<NuxtImg>` (no photos per D-21)
    - Does NOT contain personal bio text like "Quem e o Marcio"
    - SectionExpert.vue still exists (NOT deleted)
  </acceptance_criteria>
  <done>SectionAbout.vue created with company-focused bento grid layout, three equal-billing value props including renda extra, navy hero card, and CTA button</done>
</task>

<task type="auto">
  <name>Task 2: Remove comprovante screenshot from SectionSocialProof</name>
  <files>app/components/Section/SectionSocialProof.vue</files>
  <read_first>
    - app/components/Section/SectionSocialProof.vue
    - .planning/phases/04-visual-overhaul-navigation/04-CONTEXT.md
  </read_first>
  <action>
    In `app/components/Section/SectionSocialProof.vue`, per D-25 and D-26:

    1. Remove the entire "Result screenshot block" — the `<div class="text-center">` block at the bottom of the section that contains:
       - The `<p>` with text "Comprovante de resultado"
       - The `<NuxtImg>` with src `resultado-placeholder.webp`
       This is lines 55-68 in the current file.

    2. Since `r2Base` was only used for that NuxtImg, also remove:
       - `const config = useRuntimeConfig()` from script setup
       - `const r2Base = config.public.r2BaseUrl` from script setup
       - The `useRuntimeConfig` import is auto-imported by Nuxt, so no explicit import to remove.

    3. Keep the `mb-12` class on the testimonial cards grid div (line 37) — it provided spacing before the comprovante block. Without the block, `mb-12` is no longer needed. Change `mb-12` to nothing (remove it) since there's no content below the grid now.

    4. Do NOT change the testimonial cards themselves — they remain exactly as-is (D-26).
    5. Do NOT remove the `const testimonials` array or any testimonial data.
  </action>
  <verify>
    <automated>grep -q "Comprovante de resultado" app/components/Section/SectionSocialProof.vue && echo "FAIL — comprovante still present" || (grep -q "testimonials" app/components/Section/SectionSocialProof.vue && echo "PASS" || echo "FAIL — testimonials missing")</automated>
  </verify>
  <acceptance_criteria>
    - SectionSocialProof.vue does NOT contain "Comprovante de resultado"
    - SectionSocialProof.vue does NOT contain "resultado-placeholder.webp"
    - SectionSocialProof.vue does NOT contain `useRuntimeConfig` or `r2BaseUrl`
    - SectionSocialProof.vue still contains `const testimonials` array with both entries
    - SectionSocialProof.vue still contains `v-for="t in testimonials"` loop
    - SectionSocialProof.vue still contains `id="depoimentos"`
  </acceptance_criteria>
  <done>Comprovante de resultado screenshot removed from SectionSocialProof; testimonial cards remain unchanged</done>
</task>

<task type="auto">
  <name>Task 3: Fix FAQ text contrast with UAccordion content slot override</name>
  <files>app/components/Section/SectionFAQ.vue</files>
  <read_first>
    - app/components/Section/SectionFAQ.vue
    - .planning/phases/04-visual-overhaul-navigation/04-RESEARCH.md
    - .planning/phases/04-visual-overhaul-navigation/04-UI-SPEC.md
  </read_first>
  <action>
    In `app/components/Section/SectionFAQ.vue`, per D-03 and VISL-03:

    Replace the current plain `<UAccordion :items="faqItems" />` (line 40) with a slotted version that overrides the content text color:

    ```vue
    <UAccordion :items="faqItems">
      <template #content="{ item }">
        <p class="text-[var(--color-brand-text)] leading-relaxed pb-4 px-4">
          {{ item.content }}
        </p>
      </template>
    </UAccordion>
    ```

    This ensures the accordion content text uses `--color-brand-text` (#1a1a1a) instead of Nuxt UI's default muted gray. The contrast ratio of #1a1a1a on white background is 16.5:1, far exceeding WCAG AA's 4.5:1 requirement.

    Do NOT change the `faqItems` array, the section heading, or the section layout. Only the UAccordion rendering changes.
  </action>
  <verify>
    <automated>grep -q "color-brand-text" app/components/Section/SectionFAQ.vue && grep -q '#content=' app/components/Section/SectionFAQ.vue && grep -q "item.content" app/components/Section/SectionFAQ.vue && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - SectionFAQ.vue contains `#content=` slot template on UAccordion
    - SectionFAQ.vue contains `text-[var(--color-brand-text)]` class on the content `<p>` element
    - SectionFAQ.vue contains `{{ item.content }}` rendering inside the slot
    - SectionFAQ.vue still contains the `faqItems` array with 5 items
    - SectionFAQ.vue still contains `id="faq"` on the section element
  </acceptance_criteria>
  <done>FAQ accordion content text overridden with --color-brand-text (#1a1a1a) for WCAG AA compliant contrast (16.5:1 ratio)</done>
</task>

</tasks>

<verification>
- `test -f app/components/Section/SectionAbout.vue` — new component exists
- `grep "Renda Extra" app/components/Section/SectionAbout.vue` — renda extra value prop present
- `grep -v "Comprovante" app/components/Section/SectionSocialProof.vue | wc -l` equals total line count (no comprovante text)
- `grep "color-brand-text" app/components/Section/SectionFAQ.vue` — text contrast fix applied
- `test -f app/components/Section/SectionExpert.vue` — original file NOT deleted (kept as reference)
</verification>

<success_criteria>
- SectionAbout.vue exists with company-focused bento grid showing three equal-sized value prop cards including "Renda Extra com Milhas"
- SectionSocialProof.vue shows only testimonial cards — no comprovante screenshot visible
- SectionFAQ.vue accordion content text is #1a1a1a (not muted gray), passing WCAG AA 4.5:1 contrast
- SectionExpert.vue is preserved in repo (not deleted)
</success_criteria>

<output>
After completion, create `.planning/phases/04-visual-overhaul-navigation/04-02-SUMMARY.md`
</output>

---
phase: 04-visual-overhaul-navigation
plan: 03
type: execute
wave: 2
depends_on:
  - 04-02
files_modified:
  - app/components/Section/SectionMethod.vue
  - app/app.vue
autonomous: false
requirements:
  - VISL-02

must_haves:
  truths:
    - "SectionMethod displays 4 steps as bento grid cards with rounded-xl corners and shadows"
    - "Step 4 (Voo) card has navy accent background for conversion emphasis"
    - "Bento grid is single column on mobile, multi-column on desktop"
    - "app.vue uses SectionAbout instead of SectionExpert"
    - "Section order in app.vue is Hero, About, Method, SocialProof, Price, FAQ, Form"
    - "All sections render correctly on mobile (375px) and desktop (1280px)"
  artifacts:
    - path: "app/components/Section/SectionMethod.vue"
      provides: "Bento grid layout for the 4-step method section"
      contains: "rounded-xl"
    - path: "app/app.vue"
      provides: "Updated section assembly with SectionAbout replacing SectionExpert"
      contains: "SectionAbout"
  key_links:
    - from: "app/app.vue"
      to: "app/components/Section/SectionAbout.vue"
      via: "Nuxt auto-import component"
      pattern: "<SectionAbout"
    - from: "app/components/Section/SectionMethod.vue"
      to: "app/assets/css/main.css"
      via: "CSS custom properties"
      pattern: "var\\(--color-brand-primary\\)"
---

<objective>
Refactor SectionMethod to bento grid card layout and wire app.vue to use SectionAbout instead of SectionExpert, completing the visual overhaul assembly.

Purpose: Transform the flat 4-column steps grid into a modern bento card layout with varied visual emphasis, and connect all Phase 4 changes by swapping SectionExpert for SectionAbout in the main app shell.

Output: Refactored SectionMethod.vue with bento grid cards; updated app.vue with SectionAbout replacing SectionExpert.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-visual-overhaul-navigation/04-CONTEXT.md
@.planning/phases/04-visual-overhaul-navigation/04-RESEARCH.md
@.planning/phases/04-visual-overhaul-navigation/04-UI-SPEC.md
@.planning/phases/04-visual-overhaul-navigation/04-02-SUMMARY.md

<interfaces>
<!-- Current SectionMethod.vue — flat grid to be refactored to bento cards -->
From app/components/Section/SectionMethod.vue:
```vue
<!-- Steps grid: grid-cols-1 md:grid-cols-4 gap-8 -->
<!-- Each step: text-center with number, UIcon, title, description -->
<!-- Offer block: bg-white rounded-2xl shadow p-8 with 3 check-circle items -->
```
Steps data array structure:
```typescript
const steps = [
  { icon: 'i-heroicons-magnifying-glass', number: '01', title: 'Diagnostico', description: '...' },
  { icon: 'i-heroicons-light-bulb', number: '02', title: 'Estrategia', description: '...' },
  { icon: 'i-heroicons-rocket-launch', number: '03', title: 'Execucao', description: '...' },
  { icon: 'i-heroicons-paper-airplane', number: '04', title: 'Voo', description: '...' },
]
```

<!-- Current app.vue section order -->
From app/app.vue:
```vue
<SectionHero />
<SectionExpert />    <!-- REPLACE with <SectionAbout /> -->
<SectionMethod />
<SectionSocialProof />
<SectionPrice />
<SectionFAQ />
<section id="formulario">...</section>
```

<!-- SectionAbout.vue created in Plan 02 — auto-imported by Nuxt -->
Component name: SectionAbout
Section ID: "sobre"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor SectionMethod to bento grid card layout</name>
  <files>app/components/Section/SectionMethod.vue</files>
  <read_first>
    - app/components/Section/SectionMethod.vue
    - .planning/phases/04-visual-overhaul-navigation/04-RESEARCH.md
    - .planning/phases/04-visual-overhaul-navigation/04-UI-SPEC.md
  </read_first>
  <action>
    Refactor the steps grid in `app/components/Section/SectionMethod.vue` from flat text-center divs to bento grid cards. Per D-04, D-05, D-06, D-07, D-08, D-09, D-10.

    **Do NOT change the `<script setup>` block** — the `steps` array stays exactly as-is.

    **Changes to template only:**

    1. Replace the steps grid container (currently `class="grid grid-cols-1 md:grid-cols-4 gap-8 mb-16"`) with:
    ```html
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4 md:gap-6 mb-8">
    ```
    Key change: add `lg:grid-cols-4` breakpoint (was missing `lg:` prefix — Pitfall 3), reduce gap from `gap-8` to `gap-4 md:gap-6` (UI-SPEC spacing scale), reduce mb from 16 to 8.

    2. Replace each step's `<div>` wrapper (currently `class="text-center"`) with a bento card wrapper. For steps 01-03 (white cards per D-07):
    ```html
    <div class="rounded-xl bg-white shadow-sm p-6">
    ```
    For step 04 "Voo" (navy accent per D-06, UI-SPEC SectionMethod bento card structure):
    ```html
    <div class="rounded-xl bg-[var(--color-brand-primary)] text-white p-6">
    ```

    3. Inside each card, restructure the content layout (remove `text-center`, align left for card reading flow):
    ```html
    <div class="rounded-xl bg-white shadow-sm p-6">
      <span class="text-4xl font-bold text-[var(--color-brand-cta)] opacity-30 block mb-2">
        {{ step.number }}
      </span>
      <UIcon
        :name="step.icon"
        class="w-10 h-10 text-[var(--color-brand-primary)] mb-3"
      />
      <h3 class="text-lg font-semibold text-[var(--color-brand-primary)] mb-2">
        {{ step.title }}
      </h3>
      <p class="text-sm text-[var(--color-brand-text-muted)] leading-relaxed">
        {{ step.description }}
      </p>
    </div>
    ```
    For the navy step 04 card, adjust text colors: icon class uses `text-white/70` instead of `text-[var(--color-brand-primary)]`, title uses `text-white`, description uses `text-white/80`, step number uses `text-white opacity-30` instead of `text-[var(--color-brand-cta)] opacity-30`.

    4. Use a conditional check in the v-for to apply the navy background to step 04. The cleanest approach: use `:class` binding based on `step.number === '04'`:
    ```html
    <div
      v-for="step in steps"
      :key="step.number"
      class="rounded-xl p-6"
      :class="step.number === '04'
        ? 'bg-[var(--color-brand-primary)] text-white'
        : 'bg-white shadow-sm'"
    >
      <span
        class="text-4xl font-bold opacity-30 block mb-2"
        :class="step.number === '04' ? 'text-white' : 'text-[var(--color-brand-cta)]'"
      >{{ step.number }}</span>
      <UIcon
        :name="step.icon"
        class="w-10 h-10 mb-3"
        :class="step.number === '04' ? 'text-white/70' : 'text-[var(--color-brand-primary)]'"
      />
      <h3
        class="text-lg font-semibold mb-2"
        :class="step.number === '04' ? 'text-white' : 'text-[var(--color-brand-primary)]'"
      >{{ step.title }}</h3>
      <p
        class="text-sm leading-relaxed"
        :class="step.number === '04' ? 'text-white/80' : 'text-[var(--color-brand-text-muted)]'"
      >{{ step.description }}</p>
    </div>
    ```

    5. Update the offer block wrapper: change `rounded-2xl` to `rounded-xl` (D-08 consistency) and keep other classes. Remove `text-center` and `max-w-2xl mx-auto` — let it span full width for bento consistency:
    ```html
    <div class="bg-white rounded-xl shadow p-8">
    ```

    **Constraints:**
    - Do NOT change the `steps` array data
    - Do NOT remove the offer block or its check-circle list items
    - Ensure `grid-cols-1 md:grid-cols-2 lg:grid-cols-4` — must specify all three breakpoints (Pitfall 3)
    - All cards use `rounded-xl` per D-08
    - Remove `mx-auto` from UIcon (was for text-center, no longer needed)
  </action>
  <verify>
    <automated>grep -q "rounded-xl" app/components/Section/SectionMethod.vue && grep -q "lg:grid-cols-4" app/components/Section/SectionMethod.vue && grep -q "shadow-sm" app/components/Section/SectionMethod.vue && grep -q "color-brand-primary.*text-white" app/components/Section/SectionMethod.vue && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - SectionMethod.vue grid container has `grid-cols-1 md:grid-cols-2 lg:grid-cols-4` (all three breakpoints)
    - SectionMethod.vue step cards have `rounded-xl` class (D-08)
    - Steps 01-03 cards have `bg-white shadow-sm` (D-07)
    - Step 04 card has `bg-[var(--color-brand-primary)] text-white` (D-06 navy accent)
    - Offer block uses `rounded-xl` (not rounded-2xl)
    - Gap classes are `gap-4 md:gap-6` (UI-SPEC spacing scale)
    - Steps data array is unchanged (same 4 steps with same icons, numbers, titles, descriptions)
    - UIcon components still use `i-heroicons-*` prefix icons
  </acceptance_criteria>
  <done>SectionMethod refactored to bento grid with white cards for steps 01-03, navy accent card for step 04, rounded-xl corners, and proper responsive breakpoints</done>
</task>

<task type="auto">
  <name>Task 2: Wire app.vue to use SectionAbout and add header top padding</name>
  <files>app/app.vue</files>
  <read_first>
    - app/app.vue
    - app/components/Section/SectionAbout.vue
    - .planning/phases/04-visual-overhaul-navigation/04-CONTEXT.md
  </read_first>
  <action>
    In `app/app.vue`, make two changes per D-23:

    1. Replace `<SectionExpert />` with `<SectionAbout />` in the template. The component is auto-imported by Nuxt (file naming convention). The section order becomes:
    ```
    <SectionHero />
    <SectionAbout />
    <SectionMethod />
    <SectionSocialProof />
    <SectionPrice />
    <SectionFAQ />
    <section id="formulario" ...>
    ```

    2. Add top padding to the `<main>` element to account for the fixed header (64px height). Change:
    ```html
    <main id="main-content">
    ```
    to:
    ```html
    <main id="main-content" class="pt-16">
    ```
    `pt-16` = 64px in Tailwind, matching the header height. Without this, the fixed header overlaps the top of SectionHero.

    Do NOT change the `<script setup>` block (SEO meta, schema.org — all unchanged).
    Do NOT change the `<section id="formulario">` wrapper or `<AppFooter />`.
  </action>
  <verify>
    <automated>grep -q "SectionAbout" app/app.vue && grep -v "SectionExpert" app/app.vue > /dev/null && grep -q 'pt-16' app/app.vue && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - app.vue contains `<SectionAbout />` (NOT `<SectionExpert />`)
    - app.vue does NOT contain the string `SectionExpert` anywhere
    - app.vue contains `pt-16` on the main element (fixed header offset)
    - Section order is: SectionHero, SectionAbout, SectionMethod, SectionSocialProof, SectionPrice, SectionFAQ, formulario section
    - Script setup block unchanged (useSeoMeta, useSchemaOrg calls intact)
    - AppHeader and AppFooter still present in template
  </acceptance_criteria>
  <done>app.vue updated to use SectionAbout replacing SectionExpert, with pt-16 padding compensating for fixed header height</done>
</task>

</tasks>

<verification>
- `grep "SectionAbout" app/app.vue` confirms swap is done
- `grep -c "SectionExpert" app/app.vue` returns 0
- `grep "lg:grid-cols-4" app/components/Section/SectionMethod.vue` confirms bento breakpoints
- `grep "rounded-xl" app/components/Section/SectionMethod.vue` confirms card corners
- `pnpm nuxt build` completes without errors
</verification>

<success_criteria>
- SectionMethod displays as bento grid cards with rounded corners, shadows, and navy accent on step 4
- app.vue renders SectionAbout (company section) where SectionExpert used to be
- Page content is not hidden behind the fixed header (pt-16 offset applied)
- Build succeeds with no hydration warnings
- All sections visible and properly ordered on mobile and desktop viewports
</success_criteria>

<output>
After completion, create `.planning/phases/04-visual-overhaul-navigation/04-03-SUMMARY.md`
</output>

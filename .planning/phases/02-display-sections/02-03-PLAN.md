---
phase: 02-display-sections
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - app/components/Section/SectionPrice.vue
  - app/components/Section/SectionFAQ.vue
  - app/app.vue
autonomous: true
requirements:
  - CTA-02
  - CTA-03

must_haves:
  truths:
    - "Price R$200 is displayed prominently with value anchor 'passagens que custam R$3.000+' visible near the bottom of the page"
    - "FAQ section renders 5 Q&A items in UAccordion expandable format addressing purchase objections"
    - "app.vue assembles all 6 sections (Hero, Expert, Method, SocialProof, Price, FAQ) plus the #formulario stub in correct page-scroll order"
    - "CTA buttons throughout the page scroll to #formulario anchor which exists in the DOM"
    - "nuxt build completes without errors or SSR hydration warnings"
  artifacts:
    - path: "app/components/Section/SectionPrice.vue"
      provides: "Price display with value anchoring and CTA"
      contains: "R$200"
    - path: "app/components/Section/SectionFAQ.vue"
      provides: "UAccordion with 5 FAQ items"
      contains: "UAccordion"
    - path: "app/app.vue"
      provides: "Full page assembly with all sections and #formulario anchor"
      contains: "formulario"
  key_links:
    - from: "app.vue #formulario"
      to: "CTA buttons in SectionHero, SectionExpert, SectionPrice"
      via: "useScroll scrollTo('formulario')"
      pattern: "id=\"formulario\""
    - from: "SectionFAQ.vue UAccordion"
      to: "faqItems array"
      via: ":items prop"
      pattern: ":items=\"faqItems\""
---

<objective>
Build SectionPrice, SectionFAQ, then wire all 6 sections into app.vue with the #formulario anchor stub — completing the full landing page scroll experience.

Purpose: Price anchoring converts hesitant visitors by reframing cost; FAQ removes final objections; app.vue assembly makes the full narrative visible in one scroll.
Output: SectionPrice.vue, SectionFAQ.vue, updated app.vue — Phase 2 complete, page ready for Phase 3 form integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-display-sections/02-CONTEXT.md

<!-- Prior plan outputs needed for assembly -->
@.planning/phases/02-display-sections/02-01-SUMMARY.md
@.planning/phases/02-display-sections/02-02-SUMMARY.md

<interfaces>
<!-- Design tokens (app/assets/css/main.css) -->
--color-brand-primary: #1a3a5c
--color-brand-bg: #f8f9fa
--color-brand-cta: #e67e22
--color-brand-cta-hover: #d35400
--color-brand-text: #1a1a1a
--color-brand-text-muted: #6b7280

<!-- useScroll (from 02-01) -->
const { scrollTo } = useScroll()
// call: scrollTo('formulario')

<!-- R2 pattern -->
const config = useRuntimeConfig()
const r2Base = config.public.r2BaseUrl

<!-- UAccordion usage (Nuxt UI v4.5.1) -->
<UAccordion :items="faqItems" />
// items shape: { label: string, content: string }[]
// Default: single-open, collapsible

<!-- app.vue final assembly (from research pattern) -->
<main id="main-content">
  <SectionHero />
  <SectionExpert />
  <SectionMethod />
  <SectionSocialProof />
  <SectionPrice />
  <SectionFAQ />
  <section id="formulario" aria-label="Formulário de consultoria">
    <!-- TODO: Phase 3 — SectionLeadForm goes here -->
  </section>
</main>

<!-- Alternating backgrounds (D-26) -->
SectionHero        → bg-[var(--color-brand-bg)]  (off-white)
SectionExpert      → bg-white
SectionMethod      → bg-[var(--color-brand-bg)]  (off-white)
SectionSocialProof → bg-white
SectionPrice       → bg-[var(--color-brand-primary)]  (dark blue — high contrast for conversion)
SectionFAQ         → bg-[var(--color-brand-bg)]  (off-white)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build SectionPrice component</name>
  <files>app/components/Section/SectionPrice.vue</files>
  <read_first>
    - app/components/Section/SectionHero.vue (UButton + useScroll pattern)
    - .planning/phases/02-display-sections/02-CONTEXT.md (decisions D-20, D-21, D-22)
  </read_first>
  <action>
Create `app/components/Section/SectionPrice.vue` implementing decisions D-20, D-21, D-22.

**Script:**
```vue
<script setup lang="ts">
const { scrollTo } = useScroll()
</script>
```

**Template structure:**
- `<section id="preco">` with `bg-[var(--color-brand-primary)] py-12 md:py-24 px-6`
  (Dark navy background for maximum contrast and conversion urgency — departure from strict alternating pattern but justified for the pricing moment)
- `max-w-3xl mx-auto text-center`

Content:
1. Pre-heading: `text-[var(--color-brand-cta)] text-sm font-semibold uppercase tracking-widest mb-4` — "Investimento"
2. Value anchor (D-21) — `text-white/70 text-lg mb-2` — "Passagens executivas que custam R$3.000+ emitidas por uma fração do preço"
3. Price display (D-20) — flex layout `items-end justify-center gap-1 mb-2`:
   - `<span class="text-2xl text-white/60 font-light">R$</span>`
   - `<span class="text-7xl md:text-8xl font-bold text-white leading-none">200</span>`
4. Price clarification — `text-white/60 text-sm mt-2 mb-8` — "pagamento único — sem mensalidade, sem surpresas"
5. What's included summary — `grid grid-cols-1 md:grid-cols-3 gap-4 mb-10`:
   Each item: `text-center text-white/80 text-sm`
   - "2 reuniões online"
   - "1 mês acompanhamento"
   - "Plano personalizado"
6. CTA UButton (D-20, D-03) — same label "Quero minha Consultoria", `@click="scrollTo('formulario')"`:
   - `class="bg-[var(--color-brand-cta)] hover:bg-[var(--color-brand-cta-hover)] text-white font-semibold px-10 py-4 rounded-lg text-lg cursor-pointer"`
  </action>
  <verify>
    <automated>grep -n "R\$200\|R\$3\.000\|formulario\|brand-primary\|brand-cta" /home/michel/projects/codebase/agencia201/flyupmilhas/site-flyupmilhas/app/components/Section/SectionPrice.vue</automated>
  </verify>
  <acceptance_criteria>
    - File exists at app/components/Section/SectionPrice.vue
    - Contains "R$200" price display (D-20)
    - Contains "R$3.000" value anchor text (D-21)
    - Contains scrollTo('formulario') on CTA button (D-03, D-22)
    - Uses `var(--color-brand-primary)` as section background for contrast
    - Uses `var(--color-brand-cta)` for CTA button color
    - No `<ClientOnly>` wrapper
  </acceptance_criteria>
  <done>SectionPrice renders R$200 price prominently on dark navy background with value anchor comparing to R$3.000+ passagens and CTA scrolling to form</done>
</task>

<task type="auto">
  <name>Task 2: Build SectionFAQ component</name>
  <files>app/components/Section/SectionFAQ.vue</files>
  <read_first>
    - .planning/phases/02-display-sections/02-CONTEXT.md (decisions D-23, D-24, D-25)
    - .planning/phases/02-display-sections/02-RESEARCH.md (UAccordion code sample, FAQ items)
  </read_first>
  <action>
Create `app/components/Section/SectionFAQ.vue` implementing decisions D-23, D-24, D-25.

**Script with faqItems array (D-24, D-25):**
```vue
<script setup lang="ts">
const faqItems = [
  {
    label: 'Funciona para quem gasta pouco no cartão?',
    content: 'Sim. Mesmo com R$1.500/mês em gastos, é possível acumular milhas suficientes para voos nacionais em menos de 1 ano. Na nossa reunião mapeamos seu perfil e definimos o ritmo ideal para o seu objetivo.',
  },
  {
    label: 'Preciso ter cartão premium para participar?',
    content: 'Não. Trabalho com cartões de diferentes categorias. O objetivo é otimizar o que você já tem e, se fizer sentido, indicar upgrades que compensem financeiramente.',
  },
  {
    label: 'E se eu não gostar da consultoria?',
    content: 'A primeira reunião é um diagnóstico detalhado. Se ao final dela você sentir que não foi o que esperava, conversamos — meu objetivo é que você saia com um plano claro e acionável.',
  },
  {
    label: 'Como funciona a reunião online?',
    content: 'Fazemos via Google Meet ou WhatsApp Video. Dura em média 1 hora. Você compartilha sua tela para vermos os programas juntos, e eu monto sua estratégia em tempo real.',
  },
  {
    label: 'Quanto tempo leva para ver resultado?',
    content: 'Depende do seu ritmo de gastos e objetivo. Resultados em milhas acumuladas aparecem no primeiro mês. Emissão de passagens executivas geralmente acontece entre 3 e 12 meses após a estratégia implementada.',
  },
  // TODO: replace with real Q&A from Marcio — add 1-2 more as needed
]
</script>
```

**Template structure:**
- `<section id="faq">` with `bg-[var(--color-brand-bg)] py-12 md:py-24 px-6`
- `max-w-3xl mx-auto`
- H2 "Perguntas Frequentes" — `text-2xl md:text-3xl font-bold text-[var(--color-brand-primary)] text-center mb-4`
- Subheading — `text-[var(--color-brand-text-muted)] text-center mb-12` — "Tire suas dúvidas antes de decidir"
- UAccordion (D-23):
  ```html
  <UAccordion :items="faqItems" />
  ```

No additional config needed. UAccordion v4 defaults: single-open, collapsible, accessible keyboard navigation. Do not set `type="multiple"` — single open is better for mobile reading flow.
  </action>
  <verify>
    <automated>grep -n "UAccordion\|faqItems\|:items\|TODO" /home/michel/projects/codebase/agencia201/flyupmilhas/site-flyupmilhas/app/components/Section/SectionFAQ.vue</automated>
  </verify>
  <acceptance_criteria>
    - File exists at app/components/Section/SectionFAQ.vue
    - Contains `<UAccordion :items="faqItems" />` (D-23)
    - faqItems array has at least 5 items (D-24)
    - Contains `// TODO: replace with real Q&A from Marcio` comment (D-30)
    - All FAQ answers are 2-3 sentences in first person (D-25)
    - No `<ClientOnly>` wrapper
    - No `$fetch` or `useFetch` calls
  </acceptance_criteria>
  <done>SectionFAQ renders 5 Q&A items in UAccordion with single-open behavior, addressing purchase objections in first person</done>
</task>

<task type="auto">
  <name>Task 3: Assemble all sections in app.vue</name>
  <files>app/app.vue</files>
  <read_first>
    - app/app.vue (MANDATORY — read before any edit; contains existing SEO meta and schema.org setup)
    - app/components/Section/SectionHero.vue (confirm it exists)
    - app/components/Section/SectionExpert.vue (confirm it exists)
    - app/components/Section/SectionMethod.vue (confirm it exists)
    - app/components/Section/SectionSocialProof.vue (confirm it exists)
    - app/components/Section/SectionPrice.vue (just created)
    - app/components/Section/SectionFAQ.vue (just created)
  </read_first>
  <action>
Replace the `<SectionPlaceholder />` in `app/app.vue` with all 6 section components plus the `#formulario` anchor stub.

CRITICAL: Preserve the entire `<script setup>` block unchanged (useSeoMeta + useSchemaOrg — do not touch). Only modify the `<template>` block.

Replace ONLY this in the template:
```vue
<!-- BEFORE -->
<main id="main-content">
  <SectionPlaceholder />
</main>
```

With:
```vue
<!-- AFTER -->
<main id="main-content">
  <SectionHero />
  <SectionExpert />
  <SectionMethod />
  <SectionSocialProof />
  <SectionPrice />
  <SectionFAQ />
  <!-- Phase 3 will fill this section with SectionLeadForm -->
  <section id="formulario" aria-label="Formulário de consultoria" class="py-16 px-6 bg-[var(--color-brand-bg)]">
    <!-- TODO: Phase 3 — SectionLeadForm goes here -->
    <div class="max-w-xl mx-auto text-center">
      <p class="text-[var(--color-brand-text-muted)]">Formulário em breve...</p>
    </div>
  </section>
</main>
```

The `id="formulario"` on that section element is CRITICAL. CTA buttons from SectionHero, SectionExpert, and SectionPrice all call `scrollTo('formulario')`. If the element does not exist in the DOM, the scroll silently fails.

Do NOT remove the `<AppHeader />` and `<AppFooter />` elements.
Do NOT add any imports — Nuxt auto-imports all components in the Section/ directory.
  </action>
  <verify>
    <automated>grep -n "SectionHero\|SectionExpert\|SectionMethod\|SectionSocialProof\|SectionPrice\|SectionFAQ\|id=\"formulario\"" /home/michel/projects/codebase/agencia201/flyupmilhas/site-flyupmilhas/app/app.vue</automated>
  </verify>
  <acceptance_criteria>
    - app.vue contains all 6 section components: SectionHero, SectionExpert, SectionMethod, SectionSocialProof, SectionPrice, SectionFAQ
    - app.vue contains `id="formulario"` on the stub section element
    - SectionPlaceholder is no longer present in the template
    - The script setup block (useSeoMeta + useSchemaOrg) is unchanged
    - AppHeader and AppFooter are still present
    - Sections appear in correct page-scroll order: Hero → Expert → Method → SocialProof → Price → FAQ → formulario
  </acceptance_criteria>
  <done>app.vue renders the full 6-section landing page narrative with the #formulario anchor ready for Phase 3 form integration</done>
</task>

</tasks>

<verification>
After all 3 tasks complete, run the build to confirm Phase 2 is stable:

```bash
# Confirm formulario anchor exists and all sections present
grep -n "formulario\|SectionHero\|SectionFAQ" /home/michel/projects/codebase/agencia201/flyupmilhas/site-flyupmilhas/app/app.vue

# Confirm no SectionPlaceholder remains in app.vue
grep "SectionPlaceholder" /home/michel/projects/codebase/agencia201/flyupmilhas/site-flyupmilhas/app/app.vue && echo "FAIL: placeholder still present" || echo "OK: placeholder removed"

# Count all section files — should be 6 (+ Placeholder kept for reference)
ls /home/michel/projects/codebase/agencia201/flyupmilhas/site-flyupmilhas/app/components/Section/

# Confirm UAccordion in FAQ
grep "UAccordion" /home/michel/projects/codebase/agencia201/flyupmilhas/site-flyupmilhas/app/components/Section/SectionFAQ.vue

# Build check (from project root)
cd /home/michel/projects/codebase/agencia201/flyupmilhas/site-flyupmilhas && npm run build
```

Build MUST complete without errors. Any `document is not defined` or hydration mismatch in build output indicates a missing `import.meta.client` guard — check all new components.
</verification>

<success_criteria>
- SectionPrice.vue: R$200 displayed on dark navy background with R$3.000+ value anchor and CTA
- SectionFAQ.vue: UAccordion with 5 FAQ items (purchase objection topics) and TODO marker for Marcio's Q&A
- app.vue: All 6 sections assembled in correct order, SectionPlaceholder removed, #formulario anchor present
- `npm run build` exits 0 with no SSR errors
- A visitor scrolling the page top-to-bottom sees: Hero → Expert → Method → Social Proof → Price → FAQ → (form stub)
</success_criteria>

<output>
After completion, create `.planning/phases/02-display-sections/02-03-SUMMARY.md` documenting:
- Final file list for Phase 2
- Build result (exit code, any warnings)
- Content gaps requiring Marcio input (photo, real numbers, real testimonials)
- Handoff notes for Phase 3 (formulario anchor confirmed, form fields to implement)
</output>

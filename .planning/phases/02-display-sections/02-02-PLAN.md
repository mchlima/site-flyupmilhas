---
phase: 02-display-sections
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/Section/SectionMethod.vue
  - app/components/Section/SectionSocialProof.vue
autonomous: true
requirements:
  - METD-01
  - METD-02
  - SOCL-01
  - SOCL-02

must_haves:
  truths:
    - "'Como Funciona' section displays exactly 4 labeled steps (Diagnostico, Estrategia, Execucao, Voo) in 4-column desktop / 1-column mobile layout"
    - "Offer details (R$200, 2 reunioes, 1 mes acompanhamento) are visible within or immediately after the method section"
    - "Social proof section shows at minimum 2 testimonial cards with name, city, and specific R$ savings or route + cabin class"
    - "At least one result screenshot is displayed using NuxtImg from R2, with a TODO marker for real content"
  artifacts:
    - path: "app/components/Section/SectionMethod.vue"
      provides: "4-step visual method flow + offer description"
      contains: "Diagnostico"
    - path: "app/components/Section/SectionSocialProof.vue"
      provides: "Testimonial cards + R2 result screenshot"
      contains: "TODO: replace with real data from Marcio"
  key_links:
    - from: "SectionSocialProof.vue NuxtImg"
      to: "R2 bucket"
      via: "useRuntimeConfig().public.r2BaseUrl"
      pattern: "r2Base"
---

<objective>
Build SectionMethod and SectionSocialProof — the middle sections of the landing page that demonstrate the process and build trust through evidence.

Purpose: These two sections answer "how does it work?" and "does it actually work?" — the two questions that move a skeptical visitor toward conversion.
Output: SectionMethod.vue (4-step process + offer block) and SectionSocialProof.vue (testimonial cards + R2 screenshot) — both SSR-safe and mobile-responsive.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-display-sections/02-CONTEXT.md

<interfaces>
<!-- Design tokens (app/assets/css/main.css) -->
--color-brand-primary: #1a3a5c    /* headings, step numbers */
--color-brand-bg: #f8f9fa         /* alternating section background */
--color-brand-cta: #e67e22        /* accent highlights */
--color-brand-text: #1a1a1a       /* body text */
--color-brand-text-muted: #6b7280 /* secondary text, captions */

<!-- R2 image pattern (from SectionPlaceholder.vue) -->
const config = useRuntimeConfig()
const r2Base = config.public.r2BaseUrl

<!-- NuxtImg usage -->
<NuxtImg :src="`${r2Base}/filename.webp`" width="N" height="N" alt="..." />

<!-- Alternating section backgrounds (D-26) -->
SectionHero   → bg-[var(--color-brand-bg)] (off-white)
SectionExpert → bg-white
SectionMethod → bg-[var(--color-brand-bg)] (off-white, alternating)
SectionSocialProof → bg-white

<!-- Available Nuxt UI components -->
UIcon — icon display (i-heroicons-* prefix)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build SectionMethod component</name>
  <files>app/components/Section/SectionMethod.vue</files>
  <read_first>
    - app/components/Section/SectionPlaceholder.vue (section structure pattern)
    - .planning/phases/02-display-sections/02-CONTEXT.md (decisions D-12 through D-15)
  </read_first>
  <action>
Create `app/components/Section/SectionMethod.vue` implementing decisions D-12 through D-15.

**Script:** No reactive state needed. Pure static content.
```vue
<script setup lang="ts">
const steps = [
  {
    icon: 'i-heroicons-magnifying-glass',
    number: '01',
    title: 'Diagnóstico',
    description: 'Analisamos seus cartões, gastos mensais e programas de fidelidade para identificar onde você está deixando milhas na mesa.',
  },
  {
    icon: 'i-heroicons-light-bulb',
    number: '02',
    title: 'Estratégia',
    description: 'Montamos um plano personalizado: quais cartões usar, como transferir pontos e qual programa de milhas faz mais sentido para o seu perfil.',
  },
  {
    icon: 'i-heroicons-rocket-launch',
    number: '03',
    title: 'Execução',
    description: 'Você executa o plano com acompanhamento: dúvidas respondidas via WhatsApp e uma reunião de revisão ao final do primeiro mês.',
  },
  {
    icon: 'i-heroicons-paper-airplane',
    number: '04',
    title: 'Voo',
    description: 'Chegamos juntos ao momento da emissão. Selecionamos a disponibilidade, escolhemos a cabine e você embarca na executiva.',
  },
]
</script>
```

**Template structure:**
- `<section id="como-funciona">` with `bg-[var(--color-brand-bg)] py-12 md:py-24 px-6`
- `max-w-5xl mx-auto`
- Section heading: H2 "Como Funciona" — `text-2xl md:text-3xl font-bold text-[var(--color-brand-primary)] text-center mb-4`
- Optional subheading: "4 passos para voar em executiva" — `text-[var(--color-brand-text-muted)] text-center mb-12`
- Steps grid (D-14): `grid grid-cols-1 md:grid-cols-4 gap-8 mb-16`
  - Each step card: `text-center` with:
    - Step number span: `text-4xl font-bold text-[var(--color-brand-cta)] opacity-30 block mb-2` (e.g., "01")
    - UIcon: `<UIcon :name="step.icon" class="w-10 h-10 text-[var(--color-brand-primary)] mx-auto mb-3" />`
    - H3 title: `text-lg font-semibold text-[var(--color-brand-primary)] mb-2`
    - P description: `text-sm text-[var(--color-brand-text-muted)] leading-relaxed`
- **Offer block (D-15):** After steps grid, a visually distinct card `bg-white rounded-2xl shadow p-8 text-center max-w-2xl mx-auto`:
  - H3: "O que está incluído" — `text-xl font-bold text-[var(--color-brand-primary)] mb-4`
  - Offer list: 3 items with checkmark icon — `flex items-center gap-2 text-[var(--color-brand-text)] mb-2`
    - "R$200 — pagamento único, sem mensalidade"
    - "2 reuniões online (diagnóstico + revisão)"
    - "1 mês de acompanhamento via WhatsApp"
  - Below the list: `text-sm text-[var(--color-brand-text-muted)] mt-4`: "Entregáveis: plano de milhas personalizado, lista de cartões recomendados e suporte na primeira emissão."
  </action>
  <verify>
    <automated>grep -n "Diagnostico\|Estrategia\|Execucao\|Voo\|grid-cols-4\|R\$200\|formulario" /home/michel/projects/codebase/agencia201/flyupmilhas/site-flyupmilhas/app/components/Section/SectionMethod.vue || grep -n "Diagnóstico\|Estratégia\|Execução\|grid-cols-4\|R\$200" /home/michel/projects/codebase/agencia201/flyupmilhas/site-flyupmilhas/app/components/Section/SectionMethod.vue</automated>
  </verify>
  <acceptance_criteria>
    - File exists at app/components/Section/SectionMethod.vue
    - Contains exactly 4 steps: Diagnostico, Estrategia, Execucao, Voo (D-12)
    - Uses `grid-cols-1 md:grid-cols-4` for responsive layout (D-14)
    - Offer block contains "R$200" and "2 reunioes" or similar text (D-15)
    - No `<ClientOnly>` wrapper
    - No `$fetch` or `useFetch` calls
  </acceptance_criteria>
  <done>SectionMethod renders 4-step process flow with icons plus offer block — 4-column on desktop, stacked on mobile</done>
</task>

<task type="auto">
  <name>Task 2: Build SectionSocialProof component</name>
  <files>app/components/Section/SectionSocialProof.vue</files>
  <read_first>
    - app/components/Section/SectionPlaceholder.vue (NuxtImg R2 pattern)
    - .planning/phases/02-display-sections/02-CONTEXT.md (decisions D-16 through D-19)
  </read_first>
  <action>
Create `app/components/Section/SectionSocialProof.vue` implementing decisions D-16 through D-19.

**Script:**
```vue
<script setup lang="ts">
const config = useRuntimeConfig()
const r2Base = config.public.r2BaseUrl

// TODO: replace with real testimonials from Marcio
const testimonials = [
  {
    name: 'Ana Paula',
    city: 'São Paulo, SP',
    text: 'Com a consultoria do Márcio, consegui emitir dois bilhetes executivos para Lisboa por apenas 58.000 milhas cada. Pagaria mais de R$12.000 comprando direto. Valeu cada centavo da consultoria.',
    metric: 'R$12.000 economizados',
    route: 'GRU → LIS, classe executiva',
  },
  {
    name: 'Carlos Eduardo',
    city: 'Curitiba, PR',
    text: 'Não sabia nem que tinha milhas suficientes. O Márcio fez o diagnóstico e em 3 semanas já tinha as passagens emitidas para Miami em business class. Processo todo simples e sem estresse.',
    metric: 'R$8.500 economizados',
    route: 'CWB → MIA, classe executiva',
  },
]
</script>
```

**Template structure:**
- `<section id="depoimentos">` with `bg-white py-12 md:py-24 px-6`
- `max-w-5xl mx-auto`
- Section heading: H2 "Resultados Reais" — `text-2xl md:text-3xl font-bold text-[var(--color-brand-primary)] text-center mb-4`
- Subheading: "O que nossos clientes conseguiram" — `text-[var(--color-brand-text-muted)] text-center mb-12`

**Testimonial cards grid (D-16, D-17, D-19):** `grid grid-cols-1 md:grid-cols-2 gap-8 mb-12`

Each testimonial card — custom div (not UCard, per research recommendation for layout control):
```html
<!-- TODO: replace with real testimonials from Marcio -->
<div class="bg-[var(--color-brand-bg)] rounded-2xl shadow-sm p-8">
  <p class="text-[var(--color-brand-text)] leading-relaxed mb-6 italic">"{{ t.text }}"</p>
  <div class="border-t border-gray-100 pt-4">
    <p class="font-semibold text-[var(--color-brand-primary)]">{{ t.name }}</p>
    <p class="text-sm text-[var(--color-brand-text-muted)]">{{ t.city }}</p>
    <p class="text-sm font-medium text-[var(--color-brand-cta)] mt-1">{{ t.metric }}</p>
    <p class="text-xs text-[var(--color-brand-text-muted)]">{{ t.route }}</p>
  </div>
</div>
```

**Result screenshot block (D-18, D-32):** Below testimonial grid, full-width or centered:
```html
<!-- TODO: replace with real screenshot from Marcio (WhatsApp conversa ou print de passagem) -->
<div class="text-center">
  <p class="text-sm text-[var(--color-brand-text-muted)] mb-4 uppercase tracking-wide font-medium">Comprovante de resultado</p>
  <NuxtImg
    :src="`${r2Base}/resultado-placeholder.webp`"
    width="800"
    height="450"
    alt="Print de passagem executiva emitida com milhas — resultado real de cliente Fly Up Milhas"
    class="rounded-xl shadow-lg mx-auto max-w-2xl w-full"
  />
</div>
```
  </action>
  <verify>
    <automated>grep -n "TODO\|r2Base\|depoimentos\|rounded-2xl\|testimonial" /home/michel/projects/codebase/agencia201/flyupmilhas/site-flyupmilhas/app/components/Section/SectionSocialProof.vue</automated>
  </verify>
  <acceptance_criteria>
    - File exists at app/components/Section/SectionSocialProof.vue
    - Contains at least 2 testimonial entries in the data array (D-17)
    - Each testimonial has name, city, text, and metric fields (D-16)
    - Contains `<!-- TODO: replace with real testimonials from Marcio -->` comment (D-30)
    - Contains NuxtImg for R2 result screenshot using `${r2Base}/resultado-placeholder.webp` (D-18, D-32)
    - Uses rounded-2xl and shadow styling for cards (D-19)
    - No `<ClientOnly>` wrapper
  </acceptance_criteria>
  <done>SectionSocialProof renders 2 testimonial cards with placeholder data (TODO markers) and 1 result screenshot from R2 — cards styled with rounded corners and subtle shadow</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the wave-1 outputs are correct:

```bash
# Confirm both files exist
ls app/components/Section/SectionMethod.vue app/components/Section/SectionSocialProof.vue

# Confirm method has all 4 steps
grep -c "title:" app/components/Section/SectionMethod.vue

# Confirm R2 pattern in SocialProof
grep "r2Base" app/components/Section/SectionSocialProof.vue

# Confirm TODO markers present
grep "TODO" app/components/Section/SectionSocialProof.vue
```
</verification>

<success_criteria>
- SectionMethod.vue: exactly 4 steps with correct labels, responsive grid, offer block with R$200 price
- SectionSocialProof.vue: 2+ placeholder testimonials with TODO markers, NuxtImg R2 screenshot, card styling consistent with brand
- Both sections SSR-safe (no ClientOnly, no $fetch)
- Alternating background: SectionMethod on brand-bg, SectionSocialProof on white (D-26)
</success_criteria>

<output>
After completion, create `.planning/phases/02-display-sections/02-02-SUMMARY.md` documenting:
- Files created and layout choices made
- Placeholder content structure used
- Any deviations from plan
</output>

---
phase: 05-form-polish-footer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/Section/SectionLeadForm.vue
  - app/components/App/AppFooter.vue
  - app/app.vue
autonomous: true
requirements:
  - FORM-01
  - FORM-02
  - FOOT-01

must_haves:
  truths:
    - "Form fields and submit button share consistent horizontal alignment inside a navy card"
    - "Submit button label is vertically and horizontally centered with no clipping"
    - "Footer displays 'Desenvolvido por Agencia 201' in legible text on navy background"
    - "Footer shows WhatsApp contact link on desktop right column"
  artifacts:
    - path: "app/components/Section/SectionLeadForm.vue"
      provides: "Navy card wrapper, 24px field spacing, centered button"
      contains: "bg-[var(--color-brand-primary)]"
    - path: "app/components/App/AppFooter.vue"
      provides: "Two-column footer with credit and WhatsApp link"
      contains: "Agencia 201"
    - path: "app/app.vue"
      provides: "Updated form section wrapper without conflicting background"
      contains: "section id=\"formulario\""
  key_links:
    - from: "app/components/Section/SectionLeadForm.vue"
      to: "app/app.vue"
      via: "section#formulario wrapper classes"
      pattern: "id=\"formulario\""
    - from: "app/components/App/AppFooter.vue"
      to: "WHATSAPP_URL pattern"
      via: "wa.me link in footer"
      pattern: "wa\\.me"
---

<objective>
Polish the lead form with a navy card wrapper, improved spacing, and centered button label. Redesign the footer with Agencia 201 credit and WhatsApp contact link.

Purpose: Complete v1.1 visual refinement — form and footer are the last two areas needing polish before the milestone closes.
Output: Updated SectionLeadForm.vue, AppFooter.vue, and app.vue
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-form-polish-footer/05-CONTEXT.md

<interfaces>
<!-- Current SectionLeadForm.vue structure (key elements): -->
<!-- Line 55: outer div has class="max-w-xl mx-auto px-4 py-12" -->
<!-- Line 58: heading uses text-[var(--color-brand-primary)] -->
<!-- Line 104: UForm has class="space-y-4" (currently 16px gap) -->
<!-- Line 164-171: UButton with class="w-full py-4 bg-[var(--color-brand-cta)]..." -->
<!-- Line 67-90: success state with green-500 icon and brand-primary text -->

<!-- Current AppFooter.vue (5 lines total): -->
<!-- Simple centered text with brand-text-muted color -->

<!-- Current app.vue line 47: -->
<!-- <section id="formulario" aria-label="Formulario de consultoria" class="py-16 px-6 bg-[var(--color-brand-bg)]"> -->

<!-- Design tokens available (from Phase 1): -->
<!-- --color-brand-primary: navy blue -->
<!-- --color-brand-cta: orange -->
<!-- --color-brand-cta-hover: darker orange -->
<!-- --color-brand-bg: #F9FAFB (off-white) -->
<!-- --color-brand-text-muted: muted gray -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Form navy card wrapper, spacing fix, and button centering</name>
  <files>app/components/Section/SectionLeadForm.vue, app/app.vue</files>
  <read_first>
    - app/components/Section/SectionLeadForm.vue
    - app/app.vue
    - app/components/Section/SectionPrice.vue (reference for navy card pattern)
  </read_first>
  <action>
**SectionLeadForm.vue changes:**

1. **Navy card wrapper (per D-01):** Wrap the entire form content (heading + form/success state) in a card div with classes: `bg-[var(--color-brand-primary)] rounded-xl p-8 sm:p-10`. Keep the outer `max-w-xl mx-auto` div but remove its `px-4 py-12` (padding now handled by card + outer section).

2. **White text on navy (per D-02):** Change the h2 heading from `text-[var(--color-brand-primary)]` to `text-white`. Change the subtitle p from `text-[var(--color-brand-text-muted)]` to `text-white/80`.

3. **Form field gap (per D-05):** Change `class="space-y-4"` on the UForm element to `class="space-y-6"` (24px gap).

4. **Label colors for navy background:** Add `text-white` to each UFormField's label styling. Since UFormField renders labels automatically, add a wrapper class or use the `label` slot if needed. If UFormField does not support label color via class, wrap each label text with the `label` slot using `<template #label><span class="text-white">Label text</span></template>`.

5. **Consistent field width (per D-06):** All UInput and USelect already have `class="w-full"`. Ensure the UButton submit also has `w-full`. No additional padding changes needed since the card's `p-8 sm:p-10` provides uniform horizontal padding around all children.

6. **Button centering fix (per D-07):** Replace the UButton with a plain `<button>` element to eliminate UButton internal styling issues. Use these exact classes: `w-full py-4 bg-[var(--color-brand-cta)] hover:bg-[var(--color-brand-cta-hover)] text-white font-semibold rounded-lg text-base text-center flex items-center justify-center gap-2 disabled:opacity-50 transition-colors`. Add a loading spinner span when `isLoading` is true (use a simple CSS spin animation or the text "Enviando..."). The button text when not loading: "Quero minha Consultoria".

7. **Success state contrast (per D-02 context):** In the success state div, change `text-green-500` on the UIcon to `text-green-400` (better on navy). Change the success message p from `text-[var(--color-brand-primary)]` to `text-white`. Change `text-[var(--color-brand-text-muted)]` to `text-white/80`. Change the WhatsApp CTA border from `border-green-500 text-green-700` to `border-white/30 text-white hover:bg-white/10`.

8. **Error banner contrast:** Change error div from `bg-red-50 border-red-200 text-red-700` to `bg-red-500/20 border-red-400/30 text-red-200` for navy background visibility.

9. **WhatsApp CTA below form contrast:** Change the WhatsApp link below the form from `border-green-500 text-green-700 hover:bg-green-50` to `border-white/30 text-white hover:bg-white/10`.

**app.vue change:**

10. **Section wrapper (line 47):** Change `bg-[var(--color-brand-bg)]` to `bg-transparent` on the `section#formulario` element, since the navy card now provides its own background. Keep `py-16 px-6` for outer spacing around the card.
  </action>
  <verify>
    <automated>cd /home/michel/projects/codebase/agencia201/flyupmilhas/site-flyupmilhas && grep -c "bg-\[var(--color-brand-primary)\]" app/components/Section/SectionLeadForm.vue && grep -c "space-y-6" app/components/Section/SectionLeadForm.vue && grep -c "text-white" app/components/Section/SectionLeadForm.vue && grep "bg-transparent" app/app.vue</automated>
  </verify>
  <acceptance_criteria>
    - grep "bg-\[var(--color-brand-primary)\]" in SectionLeadForm.vue returns at least 1 match (navy card)
    - grep "space-y-6" in SectionLeadForm.vue returns 1 match (24px form gap)
    - grep "rounded-xl" in SectionLeadForm.vue returns at least 1 match (card corners)
    - grep "text-white" in SectionLeadForm.vue returns multiple matches (heading, labels, success text)
    - grep "bg-\[var(--color-brand-cta)\]" in SectionLeadForm.vue returns 1 match (orange button preserved)
    - grep "justify-center" on the submit button element in SectionLeadForm.vue returns a match (centered label)
    - grep "bg-transparent" in app.vue returns 1 match on the formulario section
    - No occurrence of "space-y-4" remains in SectionLeadForm.vue UForm
    - `pnpm run build` completes without errors
  </acceptance_criteria>
  <done>Form wrapped in navy card with rounded-xl corners. Fields spaced at 24px. Heading and labels are white on navy. Submit button label centered. Success and error states contrast-adjusted for navy. Orange CTA preserved. app.vue section background transparent.</done>
</task>

<task type="auto">
  <name>Task 2: Footer redesign with Agencia 201 credit and WhatsApp link</name>
  <files>app/components/App/AppFooter.vue</files>
  <read_first>
    - app/components/App/AppFooter.vue
    - app/components/Section/SectionLeadForm.vue (WhatsApp SVG icon reference, lines 85-86 and 182-184)
    - app/components/App/AppHeader.vue (navy background pattern reference)
  </read_first>
  <action>
**Full rewrite of AppFooter.vue (per D-08 through D-12):**

Create a footer component with:

1. **Navy background (per D-08):** `bg-[var(--color-brand-primary)]` with white text, matching the header visual language.

2. **Two-column desktop layout (per D-09):** Use `flex flex-col md:flex-row md:justify-between md:items-center` on the inner container. Left side: copyright line + "Desenvolvido por" line. Right side: WhatsApp link with icon.

3. **Stacked mobile layout (per D-10):** `text-center md:text-left` on the left column. On mobile, the WhatsApp link centers below the credit text with `mt-4 md:mt-0`.

4. **Copyright line:** `<p class="text-white/60 text-sm">&copy; {{ new Date().getFullYear() }} Fly Up Milhas. Todos os direitos reservados.</p>`

5. **Agencia 201 credit (per D-11):** `<p class="text-white/60 text-sm mt-1">Desenvolvido por <a href="https://agencia201.com.br" target="_blank" rel="noopener noreferrer" class="text-white hover:text-white/80 underline underline-offset-2 transition-colors">Agencia 201</a></p>`. Use `https://agencia201.com.br` as the URL with a `<!-- TODO: Confirm Agencia 201 URL -->` comment above it.

6. **WhatsApp link (per D-12):** Use the same inline WhatsApp SVG icon from SectionLeadForm.vue (the path data starting with "M17.472 14.382c..."). The link element: `<a href="https://wa.me/55XXXXXXXXXXX?text=Ola%2C%20quero%20saber%20mais%20sobre%20a%20consultoria%20de%20milhas." target="_blank" rel="noopener noreferrer" class="inline-flex items-center gap-2 text-white/80 hover:text-white transition-colors text-sm">`. Icon size: `w-4 h-4`. Link text: "Fale pelo WhatsApp".

7. **Container:** `max-w-6xl mx-auto px-6 py-8` for the inner wrapper.

Complete template structure:
```vue
<template>
  <footer class="bg-[var(--color-brand-primary)]">
    <div class="max-w-6xl mx-auto px-6 py-8 flex flex-col md:flex-row md:justify-between md:items-center gap-4">
      <!-- Left: copyright + credit -->
      <div class="text-center md:text-left">
        <p>copyright</p>
        <p>desenvolvido por</p>
      </div>
      <!-- Right: WhatsApp -->
      <a>whatsapp link with icon</a>
    </div>
  </footer>
</template>
```
  </action>
  <verify>
    <automated>cd /home/michel/projects/codebase/agencia201/flyupmilhas/site-flyupmilhas && grep "Agencia 201" app/components/App/AppFooter.vue && grep "bg-\[var(--color-brand-primary)\]" app/components/App/AppFooter.vue && grep "wa.me" app/components/App/AppFooter.vue && grep "md:flex-row" app/components/App/AppFooter.vue</automated>
  </verify>
  <acceptance_criteria>
    - grep "Desenvolvido por" in AppFooter.vue returns 1 match
    - grep "Agencia 201" in AppFooter.vue returns at least 1 match
    - grep "bg-\[var(--color-brand-primary)\]" in AppFooter.vue returns 1 match (navy background)
    - grep "wa.me" in AppFooter.vue returns 1 match (WhatsApp link)
    - grep "target=\"_blank\"" in AppFooter.vue returns at least 2 matches (Agencia 201 link + WhatsApp link)
    - grep "md:flex-row" in AppFooter.vue returns 1 match (responsive two-column)
    - grep "text-center md:text-left" in AppFooter.vue returns 1 match (mobile centered, desktop left)
    - grep "M17.472" in AppFooter.vue returns 1 match (WhatsApp SVG icon)
    - `pnpm run build` completes without errors
  </acceptance_criteria>
  <done>Footer has navy background with two-column desktop layout (copyright+credit left, WhatsApp right), stacked centered on mobile. "Desenvolvido por Agencia 201" links to agency site in new tab. WhatsApp link with icon present.</done>
</task>

</tasks>

<verification>
1. `pnpm run build` completes with no errors
2. Form section has navy card with rounded corners, white text headings/labels, 24px field spacing
3. Submit button text is centered (flex items-center justify-center)
4. Footer shows navy background with "Desenvolvido por Agencia 201" and WhatsApp link
5. On narrow viewport (375px), footer stacks to single column centered
</verification>

<success_criteria>
- All 3 requirements (FORM-01, FORM-02, FOOT-01) addressed in a single plan
- Form visually wrapped in navy card matching SectionPrice pattern
- Field spacing increased from 16px to 24px
- Submit button label properly centered with no UButton internal style conflicts
- Footer displays credit and WhatsApp link in responsive two-column layout
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-form-polish-footer/05-01-SUMMARY.md`
</output>

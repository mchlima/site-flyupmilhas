---
phase: 13-structural-template-changes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/Section/SectionSocialProof.vue
  - app/components/Section/SectionProgramContent.vue
autonomous: true
requirements:
  - SOCL-01
  - SOCL-02
  - SOCL-03
  - PROG-01
  - PROG-02

must_haves:
  truths:
    - "SectionSocialProof displays 3 testimonials inside WhatsApp-style green chat bubbles with tail triangles"
    - "Title reads 'Casos reais de quem ja aplicou'"
    - "Numbers and monetary values in testimonials are wrapped in bold"
    - "Bubbles render correctly at 375px mobile width without horizontal overflow"
    - "No occurrence of 'Marcio' in testimonial text"
    - "ProgramContent items display bold keywords via v-html"
    - "New subtitle appears below section title in ProgramContent"
  artifacts:
    - path: "app/components/Section/SectionSocialProof.vue"
      provides: "WhatsApp chat bubble testimonials"
      contains: "DCF8C6"
    - path: "app/components/Section/SectionProgramContent.vue"
      provides: "Bold keywords via v-html + new subtitle"
      contains: "v-html"
  key_links:
    - from: "SectionSocialProof.vue"
      to: "testimonials data array"
      via: "v-html rendering of bold text"
      pattern: "v-html.*\\.text"
    - from: "SectionProgramContent.vue"
      to: "items data array"
      via: "v-html rendering of strong tags"
      pattern: "v-html.*item\\.text"
---

<objective>
Rewrite SectionSocialProof to WhatsApp-style chat bubbles and add bold keywords + subtitle to SectionProgramContent.

Purpose: Social proof must feel authentic (WhatsApp prints are table stakes in BR mentorship niche). ProgramContent needs visual hierarchy with bold keywords.
Output: Two updated Vue components with chat bubble CSS and v-html rendering.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-structural-template-changes/13-CONTEXT.md
@app/components/Section/SectionSocialProof.vue
@app/components/Section/SectionProgramContent.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SectionSocialProof with WhatsApp chat bubbles</name>
  <files>app/components/Section/SectionSocialProof.vue</files>
  <read_first>
    - app/components/Section/SectionSocialProof.vue
    - .planning/phases/13-structural-template-changes/13-CONTEXT.md
  </read_first>
  <action>
Rewrite SectionSocialProof.vue completely. Per D-01 through D-08:

1. **Title**: Change h2 to "Casos reais de quem ja aplicou" (per D-01, SOCL-01). Remove the subtitle paragraph.

2. **Testimonials data** (per D-02, D-04, D-05, D-06, D-07): Replace the 2-item array with 3 testimonials. Use v-html for text so bold tags render. Remove ALL references to "Marcio" — use "a mentoria" or "a Fly Up Milhas" instead. Each testimonial object: `{ name, city, text }` where text contains `<strong>` around numbers/values (per SOCL-03, D-05).

Testimonial 1 (economia em R$):
```
name: 'Ana Paula'
city: 'Sao Paulo, SP'
text: 'Com a mentoria da Fly Up Milhas, consegui emitir dois bilhetes executivos para Lisboa por apenas <strong>58.000 milhas</strong> cada. Pagaria mais de <strong>R$ 12.000</strong> comprando direto. Valeu cada centavo!'
```

Testimonial 2 (rota + classe):
```
name: 'Carlos Eduardo'
city: 'Curitiba, PR'
text: 'Nao sabia nem que tinha milhas suficientes. Em <strong>3 semanas</strong> ja tinha as passagens emitidas para <strong>Miami em business class</strong>. Processo todo simples e sem estresse.'
```

Testimonial 3 (new, experiencia):
```
name: 'Juliana Martins'
city: 'Belo Horizonte, MG'
text: 'Sempre achei que milhas era coisa de quem gasta muito no cartao. Com a mentoria, aprendi a juntar <strong>mais de 40.000 milhas em 2 meses</strong> sem mudar meus habitos. Ja estou planejando minha primeira viagem internacional!'
```

3. **Chat bubble CSS** (per D-03, D-08): Each testimonial renders as a WhatsApp-style bubble:
   - Background: `#DCF8C6` (WhatsApp green tint)
   - Rounded corners: `rounded-xl` (12px)
   - Tail triangle on the left side using CSS `::before` pseudo-element (border trick) pointing left
   - `max-width: min(85%, 400px)` for mobile constraint
   - `word-break: break-word; overflow-wrap: anywhere` for long words
   - Padding: `px-4 py-3`
   - Name and city below the bubble in smaller muted text (outside the green bubble)
   - Stack bubbles vertically with `gap-6`, centered in container

4. **Layout**: Single column of bubbles, NOT a grid. Bubbles alternate slightly left/right alignment for visual variety (odd items `ml-0 mr-auto`, even items `ml-auto mr-0` — like a real chat).

5. **Remove**: Delete the `metric` and `route` fields from data. Delete the old grid card layout. Delete the TODO comments about Marcio.

6. **Scoped styles**: Add a `<style scoped>` block for the bubble tail `::before` pseudo-element since Tailwind cannot do pseudo-element border tricks:
```css
.chat-bubble {
  position: relative;
}
.chat-bubble::before {
  content: '';
  position: absolute;
  left: -8px;
  top: 12px;
  width: 0;
  height: 0;
  border-top: 6px solid transparent;
  border-bottom: 6px solid transparent;
  border-right: 8px solid #DCF8C6;
}
.chat-bubble-right::before {
  left: auto;
  right: -8px;
  border-right: none;
  border-left: 8px solid #DCF8C6;
}
```

7. **Keep**: `id="depoimentos"` on the section element (anchor nav link depends on it).
  </action>
  <verify>
    <automated>cd /home/michel/projects/codebase/agencia201/flyupmilhas/site-flyupmilhas && grep -c "DCF8C6" app/components/Section/SectionSocialProof.vue && grep -c "v-html" app/components/Section/SectionSocialProof.vue && grep -c "strong" app/components/Section/SectionSocialProof.vue && grep "Casos reais" app/components/Section/SectionSocialProof.vue && ! grep "Marcio\|Márcio" app/components/Section/SectionSocialProof.vue && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "DCF8C6" returns at least 2 matches (bubble bg + tail)
    - grep "v-html" returns at least 3 matches (one per testimonial text)
    - grep "strong" returns at least 6 matches (bold values in 3 testimonials)
    - grep "Casos reais de quem" confirms new title
    - grep "Marcio" returns 0 matches
    - grep "Juliana Martins" returns 1 match (new 3rd testimonial)
    - grep "max-width.*min" confirms mobile constraint
    - grep "chat-bubble" confirms scoped CSS class
    - id="depoimentos" preserved on section element
  </acceptance_criteria>
  <done>SectionSocialProof renders 3 WhatsApp-style green chat bubbles with bold numbers, alternating alignment, tail triangles, and mobile-safe max-width. No Marcio references. Title is "Casos reais de quem ja aplicou".</done>
</task>

<task type="auto">
  <name>Task 2: Add bold keywords and subtitle to SectionProgramContent</name>
  <files>app/components/Section/SectionProgramContent.vue</files>
  <read_first>
    - app/components/Section/SectionProgramContent.vue
    - .planning/phases/13-structural-template-changes/13-CONTEXT.md
  </read_first>
  <action>
Modify SectionProgramContent.vue. Per D-14 through D-17:

1. **Switch rendering** (per D-14, PROG-01): Change the `<span>` that renders item text from `{{ item.text }}` to `v-html="item.text"`. The span becomes: `<span class="text-[var(--color-brand-text)]" v-html="item.text"></span>`. This is safe because content is developer-controlled hardcoded strings (per STATE.md pitfall #6).

2. **Add bold keywords** (per D-15, PROG-01): Update the items array to wrap key terms in `<strong>` tags:
```typescript
const items = [
  { text: 'Como funcionam os <strong>principais programas de milhas</strong>', icon: 'i-heroicons-light-bulb' },
  { text: 'Formas inteligentes de <strong>acumular pontos</strong> sem gastar mais', icon: 'i-heroicons-arrow-trending-up' },
  { text: 'Quando vale (ou nao) <strong>transferir pontos</strong>', icon: 'i-heroicons-arrows-right-left' },
  { text: 'Como encontrar <strong>passagens com milhas</strong> pagando muito menos', icon: 'i-heroicons-ticket' },
  { text: '<strong>Erros comuns</strong> que fazem as pessoas perderem milhas', icon: 'i-heroicons-exclamation-triangle' },
  { text: 'Estrategias para <strong>viagens nacionais e internacionais</strong>', icon: 'i-heroicons-globe-americas' },
  { text: 'Como usar o <strong>cartao de credito</strong> a seu favor', icon: 'i-heroicons-credit-card' },
  { text: '<strong>Organizacao e planejamento</strong> de viagens com antecedencia', icon: 'i-heroicons-calendar-days' },
]
```
NOTE: Keep the original Portuguese accented characters from the current file (e.g., "nao" should be "não", "Organizacao" should be "Organização"). The examples above are ASCII-only for the plan — executor must preserve the existing accented text and only add `<strong>` tags around key terms.

3. **Add subtitle** (per D-16, D-17, PROG-02): Insert a `<p>` element between the h2 title and the description paragraph in the left block:
```html
<p class="text-[var(--color-brand-text-muted)] leading-relaxed">
  Sem promessas milagrosas. Um metodo simples e pratico para voce usar milhas do jeito certo e realmente economizar em viagens.
</p>
```
NOTE: Use proper Portuguese accents: "metodo" = "método", "pratico" = "prático", "voce" = "você". This subtitle REPLACES the existing description paragraph (which says "Aqui voce nao recebe promessas irreais..."). Do NOT add a second paragraph — replace the existing one.

4. **Keep everything else**: Same layout, same CTA button, same icons, same section id="conteudo-programatico".
  </action>
  <verify>
    <automated>cd /home/michel/projects/codebase/agencia201/flyupmilhas/site-flyupmilhas && grep -c "v-html" app/components/Section/SectionProgramContent.vue && grep -c "strong" app/components/Section/SectionProgramContent.vue && grep "Sem promessas milagrosas" app/components/Section/SectionProgramContent.vue && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "v-html" returns at least 1 match (template rendering)
    - grep "strong" returns at least 8 matches (one per item)
    - grep "Sem promessas milagrosas" returns 1 match (new subtitle)
    - grep "{{ item.text }}" returns 0 matches (old interpolation removed)
    - id="conteudo-programatico" preserved on section element
    - CTA button still present and functional
  </acceptance_criteria>
  <done>ProgramContent items render bold keywords via v-html with strong tags. New subtitle "Sem promessas milagrosas..." replaces old description. All 8 items have at least one bold keyword.</done>
</task>

</tasks>

<verification>
1. `grep -c "DCF8C6" app/components/Section/SectionSocialProof.vue` returns >= 2
2. `grep -c "v-html" app/components/Section/SectionSocialProof.vue` returns >= 1
3. `grep "Marcio\|Márcio" app/components/Section/SectionSocialProof.vue` returns nothing
4. `grep "v-html" app/components/Section/SectionProgramContent.vue` returns matches
5. `grep "{{ item.text }}" app/components/Section/SectionProgramContent.vue` returns nothing
6. `pnpm run build` completes without errors (if available)
</verification>

<success_criteria>
- SectionSocialProof has 3 WhatsApp-style chat bubbles with green tint, bold values, tail triangles, and mobile-safe width
- SectionProgramContent has v-html rendering with bold keywords and new subtitle
- No Marcio references in SocialProof
- Both sections preserve their id attributes for nav anchors
</success_criteria>

<output>
After completion, create `.planning/phases/13-structural-template-changes/13-01-SUMMARY.md`
</output>

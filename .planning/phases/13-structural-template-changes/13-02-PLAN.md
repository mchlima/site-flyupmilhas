---
phase: 13-structural-template-changes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/Section/SectionForWhom.vue
  - app/components/Section/SectionPrice.vue
autonomous: true
requirements:
  - FORW-01
  - PRCE-01
  - PRCE-02
  - PRCE-03

must_haves:
  truths:
    - "SectionForWhom shows 3 negative qualification cards with red X icons below the positive cards"
    - "A separator heading 'Essa mentoria nao e para todos' divides positive and negative cards"
    - "SectionPrice displays 'R$ 299,90' as a hard-coded string, never computed"
    - "SectionPrice displays 'ate 10x no cartao' as visible text"
    - "A guarantee block reads 'Garantia de 7 dias' with explanation text"
    - "SectionPrice uses a split layout with benefits left and price card right"
  artifacts:
    - path: "app/components/Section/SectionForWhom.vue"
      provides: "Negative qualification cards with red X icons"
      contains: "x-mark"
    - path: "app/components/Section/SectionPrice.vue"
      provides: "Split layout with real pricing and guarantee"
      contains: "299,90"
  key_links:
    - from: "SectionForWhom.vue"
      to: "negativeCards data array"
      via: "v-for rendering"
      pattern: "v-for.*negativeCard"
    - from: "SectionPrice.vue"
      to: "benefits array + price strings"
      via: "template literals"
      pattern: "R\\$.*299"
---

<objective>
Add "Para quem NAO e" negative cards to SectionForWhom and rewrite SectionPrice with split layout, real pricing, and guarantee block.

Purpose: Negative qualification increases lead quality (filters tire-kickers). Visible pricing with guarantee is legally expected (CDC Art. 49) and builds trust.
Output: Two updated Vue components with negative cards and real pricing layout.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-structural-template-changes/13-CONTEXT.md
@app/components/Section/SectionForWhom.vue
@app/components/Section/SectionPrice.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add negative qualification cards to SectionForWhom</name>
  <files>app/components/Section/SectionForWhom.vue</files>
  <read_first>
    - app/components/Section/SectionForWhom.vue
    - .planning/phases/13-structural-template-changes/13-CONTEXT.md
  </read_first>
  <action>
Modify SectionForWhom.vue. Per D-09 through D-13, FORW-01:

1. **Add negative cards data** (per D-09, D-10): Add a new array below the existing `cards` array:
```typescript
const negativeCards = [
  { text: 'Nao e pra quem quer dinheiro facil', icon: 'i-heroicons-x-mark' },
  { text: 'Nao e pra quem nao usa cartao', icon: 'i-heroicons-x-mark' },
  { text: 'Nao e pra quem nao vai aplicar', icon: 'i-heroicons-x-mark' },
]
```
NOTE: Use proper Portuguese accents — "Não é pra quem quer dinheiro fácil", "Não é pra quem não usa cartão", "Não é pra quem não vai aplicar". The plan uses ASCII for readability.

2. **Add separator** (per D-12): Below the existing 2-column grid (image + positive cards), add a separator heading:
```html
<div class="mt-12 mb-6 text-center">
  <p class="text-lg font-semibold text-[var(--color-brand-primary)]">
    Essa mentoria nao e para todos
  </p>
</div>
```
(Again, use proper accents: "não é")

3. **Render negative cards** (per D-09, D-11, D-13): Below the separator, add a grid of 3 negative cards. Use the same grid pattern as positive cards but with red visual treatment:
```html
<div class="grid grid-cols-1 md:grid-cols-3 gap-4">
  <div
    v-for="card in negativeCards"
    :key="card.text"
    class="rounded-xl bg-red-50 border border-red-200 p-5 flex items-center gap-4"
  >
    <UIcon :name="card.icon" class="w-7 h-7 text-red-500 shrink-0" />
    <p class="text-[var(--color-brand-text)] leading-relaxed text-sm md:text-base">{{ card.text }}</p>
  </div>
</div>
```

Key visual differences from positive cards (per D-11, D-13):
- Background: `bg-red-50` instead of `bg-white`
- Border: `border-red-200` instead of `border-gray-100`
- Icon color: `text-red-500` instead of `text-[var(--color-brand-cta)]`
- Layout: 3-column grid on desktop (md:grid-cols-3) since there are only 3 items

4. **Keep everything else**: Same section id, same image, same positive cards, same overall structure. The negative section is appended AFTER the existing grid, not inside it.
  </action>
  <verify>
    <automated>cd /home/michel/projects/codebase/agencia201/flyupmilhas/site-flyupmilhas && grep -c "x-mark" app/components/Section/SectionForWhom.vue && grep -c "negativeCards" app/components/Section/SectionForWhom.vue && grep "red-500" app/components/Section/SectionForWhom.vue && grep "para todos" app/components/Section/SectionForWhom.vue && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "negativeCards" returns at least 2 matches (array definition + v-for)
    - grep "x-mark" returns at least 1 match (icon name)
    - grep "red-500" returns at least 1 match (icon color)
    - grep "red-50" returns at least 1 match (card background)
    - grep "para todos" returns 1 match (separator text)
    - grep "dinheiro" returns 1 match (first negative card)
    - Existing 5 positive cards array unchanged
    - id="para-quem-e" preserved on section element
  </acceptance_criteria>
  <done>SectionForWhom shows 5 positive cards (unchanged) with a separator heading, followed by 3 negative qualification cards with red X icons and red-tinted backgrounds.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite SectionPrice with split layout, pricing, and guarantee</name>
  <files>app/components/Section/SectionPrice.vue</files>
  <read_first>
    - app/components/Section/SectionPrice.vue
    - .planning/phases/13-structural-template-changes/13-CONTEXT.md
    - .planning/STATE.md
  </read_first>
  <action>
Rewrite SectionPrice.vue completely. Per D-18 through D-22, PRCE-01 through PRCE-03:

1. **Benefits data** (per D-19): Update the benefits array with the 4 items from client PDF:
```typescript
const benefits = [
  { text: '3 encontros estrategicos (inicio, meio e fim)', icon: 'i-heroicons-video-camera' },
  { text: 'Suporte direto via WhatsApp', icon: 'i-heroicons-chat-bubble-left-right' },
  { text: 'Material pratico para aplicacao imediata', icon: 'i-heroicons-document-text' },
  { text: 'Plano personalizado para seu perfil', icon: 'i-heroicons-user-circle' },
]
```
NOTE: Use proper Portuguese accents — "estratégicos", "início", "prático", "aplicação". Icons are Claude's discretion per CONTEXT.md.

2. **Split layout** (per D-18, PRCE-03): Replace the centered single-column layout with a 2-column grid:
```html
<div class="max-w-5xl mx-auto grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-12 items-start">
```
- LEFT column: benefits list with check icons (like current but left-aligned in its own column)
- RIGHT column: price card + guarantee + CTA

3. **Left column — Benefits** (per D-19):
```html
<div class="flex flex-col gap-6">
  <h2 class="text-2xl md:text-3xl font-bold text-white">
    Tudo o que voce recebe na mentoria
  </h2>
  <div class="space-y-4">
    <div v-for="b in benefits" :key="b.text" class="flex items-start gap-3">
      <UIcon :name="b.icon" class="w-6 h-6 text-[var(--color-brand-cta)] shrink-0 mt-0.5" />
      <span class="text-white/90 text-base">{{ b.text }}</span>
    </div>
  </div>
</div>
```

4. **Right column — Price card** (per D-20, PRCE-01): A white card on the navy background:
```html
<div class="bg-white rounded-2xl shadow-xl p-8 flex flex-col items-center text-center">
  <p class="text-[var(--color-brand-text-muted)] text-sm font-medium mb-2">
    Comece hoje sua jornada com milhas
  </p>
  <p class="text-4xl md:text-5xl font-bold text-[var(--color-brand-primary)] mb-1">
    R$ 299,90
  </p>
  <p class="text-[var(--color-brand-text-muted)] text-base mb-6">
    no PIX ou ate 10x no cartao
  </p>
  <!-- CTA button per D-22 -->
  <button
    class="w-full bg-[var(--color-brand-cta)] hover:bg-[var(--color-brand-cta-hover)] text-white font-semibold px-8 py-4 rounded-lg text-lg cursor-pointer transition-colors"
    @click="scrollTo('formulario')"
  >
    Quero dar o primeiro passo
  </button>
</div>
```

CRITICAL: "R$ 299,90" and "ate 10x no cartao" MUST be string literals in the template. NEVER use JS variables, computed properties, or arithmetic for price values. Per STATE.md pitfall #3: JS arithmetic on 299.90 produces floating-point errors.

5. **Guarantee block** (per D-21, PRCE-02): Below the price card, still inside the right column:
```html
<div class="mt-6 flex items-center gap-3 text-white/80">
  <UIcon name="i-heroicons-shield-check" class="w-8 h-8 text-[var(--color-brand-cta)] shrink-0" />
  <div>
    <p class="font-semibold text-white">Garantia de 7 dias</p>
    <p class="text-sm text-white/70">Se nao fizer sentido pra voce, devolvemos 100% do valor.</p>
  </div>
</div>
```
(Use proper accents: "não", "você")

6. **Keep**: `id="preco"` on section. Navy background `bg-[var(--color-brand-primary)]`. `useScroll()` import and `scrollTo` usage.

7. **Remove**: The old "Condicoes especiais disponiveis" text. The old centered single-column layout.
  </action>
  <verify>
    <automated>cd /home/michel/projects/codebase/agencia201/flyupmilhas/site-flyupmilhas && grep "299,90" app/components/Section/SectionPrice.vue && grep "10x" app/components/Section/SectionPrice.vue && grep "Garantia de 7 dias" app/components/Section/SectionPrice.vue && grep "lg:grid-cols-2" app/components/Section/SectionPrice.vue && grep "shield-check" app/components/Section/SectionPrice.vue && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "R\$ 299,90" returns exactly 1 match (hard-coded string in template, not a JS variable)
    - grep "10x" returns at least 1 match (installment text)
    - grep "Garantia de 7 dias" returns exactly 1 match
    - grep "100% do valor" returns 1 match (guarantee explanation)
    - grep "lg:grid-cols-2" returns at least 1 match (split layout)
    - grep "shield-check" returns at least 1 match (guarantee icon)
    - grep "scrollTo" returns at least 1 match (CTA still scrolls to form)
    - grep "toFixed\|Number\|parseFloat\|parseInt" returns 0 matches (no JS arithmetic on price)
    - id="preco" preserved on section element
  </acceptance_criteria>
  <done>SectionPrice shows a split layout with 4 benefit items on the left and a white price card on the right displaying "R$ 299,90 no PIX" and "ate 10x no cartao" as hard-coded strings. Guarantee block with shield icon reads "Garantia de 7 dias". CTA button scrolls to form.</done>
</task>

</tasks>

<verification>
1. `grep "negativeCards" app/components/Section/SectionForWhom.vue` returns matches
2. `grep "299,90" app/components/Section/SectionPrice.vue` returns exactly 1 match
3. `grep "Garantia de 7 dias" app/components/Section/SectionPrice.vue` returns 1 match
4. `grep "lg:grid-cols-2" app/components/Section/SectionPrice.vue` returns match
5. `grep -c "toFixed\|Number(\|parseFloat\|parseInt" app/components/Section/SectionPrice.vue` returns 0
6. `pnpm run build` completes without errors (if available)
</verification>

<success_criteria>
- SectionForWhom has 3 negative cards with red X icons below a separator heading
- SectionPrice has split layout with benefits left, white price card right
- Price displayed as hard-coded "R$ 299,90" string — no JS arithmetic
- Guarantee block with shield icon and 7-day promise
- Both sections preserve their id attributes
</success_criteria>

<output>
After completion, create `.planning/phases/13-structural-template-changes/13-02-SUMMARY.md`
</output>

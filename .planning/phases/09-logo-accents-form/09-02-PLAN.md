---
phase: 09-logo-accents-form
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/Section/SectionLeadForm.vue
  - app/app.vue
autonomous: true
requirements:
  - FORM-03

must_haves:
  truths:
    - "O formulario nao tem card navy com rounded-xl envolvendo os campos"
    - "O fundo azul vem da section#formulario em app.vue, nao de um wrapper interno"
    - "Os campos do formulario flutuam diretamente sobre o fundo navy da section"
    - "Labels permanecem brancas, CTA permanece laranja, heading permanece branco"
    - "O formulario mantem max-w-xl mx-auto para constraining"
  artifacts:
    - path: "app/components/Section/SectionLeadForm.vue"
      provides: "Form without card wrapper"
    - path: "app/app.vue"
      provides: "Section#formulario with navy background"
      contains: "bg-[var(--color-brand-primary)]"
  key_links:
    - from: "app/app.vue"
      to: "app/components/Section/SectionLeadForm.vue"
      via: "section#formulario wrapper provides navy bg"
      pattern: "id=\"formulario\".*bg-.*brand-primary"
---

<objective>
Redesign the lead form by removing the heavy navy card wrapper and letting the section background provide the navy color, creating a lighter, more integrated design.

Purpose: FORM-03 requires a cleaner form that feels part of the page flow rather than a boxed-in element.
Output: Form fields float directly on section navy background, no card wrapper.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-logo-accents-form/09-CONTEXT.md

<interfaces>
<!-- Current SectionLeadForm.vue structure (relevant lines): -->

Line 57: `<div class="bg-[var(--color-brand-primary)] rounded-xl p-8 sm:p-10">` — THIS IS THE CARD TO REMOVE (per D-10)
Line 55: `<div class="max-w-xl mx-auto">` — outer constraint (KEEP per D-16)
Line 209: `</div>` — closing the card wrapper

<!-- Current app.vue section wrapper (line 48): -->
`<section id="formulario" aria-label="Formulario de mentoria" class="py-16 px-6 bg-transparent">`
— bg-transparent must change to bg-[var(--color-brand-primary)] (per D-11)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove card wrapper and update section background (FORM-03)</name>
  <files>app/components/Section/SectionLeadForm.vue, app/app.vue</files>
  <read_first>app/components/Section/SectionLeadForm.vue, app/app.vue</read_first>
  <action>
**app.vue** (per D-11):
Change line 48 section wrapper from:
```html
<section id="formulario" aria-label="Formulario de mentoria" class="py-16 px-6 bg-transparent">
```
to:
```html
<section id="formulario" aria-label="Formulário de mentoria" class="py-16 px-6 bg-[var(--color-brand-primary)]">
```
(Also fix accent: "Formulario" → "Formulário" per TEXT-01/D-09)

**SectionLeadForm.vue** (per D-10, D-12, D-13, D-14, D-15, D-16, D-17):

1. Remove the card wrapper div on line 57. Change:
```html
<div class="bg-[var(--color-brand-primary)] rounded-xl p-8 sm:p-10">
```
to just remove this div entirely (and its closing `</div>` on line 209). The content inside stays.

2. The outer `<div class="max-w-xl mx-auto">` (line 55) stays — it provides max-width constraint per D-16.

3. Add padding to the outer max-w-xl div to replace the card padding that was removed. Change:
```html
<div class="max-w-xl mx-auto">
```
to:
```html
<div class="max-w-xl mx-auto py-4">
```
(Light vertical padding; horizontal padding comes from the section in app.vue)

4. All other elements remain unchanged:
   - Heading "Comece sua mentoria agora" stays white text (per D-14)
   - Labels stay white via `<template #label>` slot (per D-13)
   - UInput/USelect keep `class="w-full bg-white"` with border styling (per D-12)
   - CTA button keeps orange bg (per D-15)
   - Success state keeps white text (per D-17 — already white on navy, no card dependency)
   - WhatsApp CTA keeps existing styling (per D-17)

The net effect: remove 1 opening div and 1 closing div, change outer div class, update app.vue section bg.
  </action>
  <verify>
    <automated>grep -c "rounded-xl p-8" app/components/Section/SectionLeadForm.vue | grep "0" && grep -c "bg-\[var(--color-brand-primary)\]" app/app.vue && echo "FORM REDESIGN OK"</automated>
  </verify>
  <acceptance_criteria>
- grep "rounded-xl p-8 sm:p-10" in SectionLeadForm.vue returns 0 matches (card wrapper removed)
- grep "bg-\[var(--color-brand-primary)\]" in app.vue section#formulario confirms navy bg on section
- grep "max-w-xl mx-auto" in SectionLeadForm.vue returns 1 match (constraint preserved)
- grep "bg-white" in SectionLeadForm.vue still finds input field styling
- grep "text-white" in SectionLeadForm.vue still finds label/heading styling
- grep "color-brand-cta" in SectionLeadForm.vue still finds CTA button
- `pnpm run build` succeeds without errors
  </acceptance_criteria>
  <done>Form card wrapper removed, section provides navy background, fields float on section bg, all styling preserved (white labels, white heading, orange CTA, white inputs with borders)</done>
</task>

</tasks>

<verification>
1. `grep "rounded-xl p-8" app/components/Section/SectionLeadForm.vue` — should return nothing (card removed)
2. `grep "bg-\[var(--color-brand-primary)\]" app/app.vue` — section has navy bg
3. `grep "max-w-xl" app/components/Section/SectionLeadForm.vue` — constraint preserved
4. `pnpm run build` — no build errors
</verification>

<success_criteria>
- No card wrapper div with rounded-xl in SectionLeadForm.vue
- Section#formulario in app.vue has navy background color
- Form fields, labels, heading, CTA, success state all render correctly on the section bg
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-logo-accents-form/09-02-SUMMARY.md`
</output>

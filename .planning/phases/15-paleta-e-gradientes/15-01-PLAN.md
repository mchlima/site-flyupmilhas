---
phase: 15-paleta-e-gradientes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/assets/css/main.css
  - app/components/App/AppHeader.vue
  - app/components/App/AppFooter.vue
  - app/components/Section/SectionHero.vue
  - app/components/Section/SectionPrice.vue
  - app/components/Section/SectionMethod.vue
  - app/components/Section/SectionAbout.vue
  - app/app.vue
autonomous: true
requirements: [PAL-01, PAL-02, PAL-03, PAL-04]

must_haves:
  truths:
    - "O azul primario visivel em headings e icones e #1D4ED8 — o navy #1a3a5c nao aparece como cor de texto/acento"
    - "Todos os botoes CTA exibem cor cyan — o laranja #e67e22 nao aparece em nenhum botao"
    - "Header e secao formulario usam fundo escuro #0F172A, nao o azul vibrante #1D4ED8"
    - "Hero, Price e formulario exibem gradientes diagonais em tons de azul"
    - "Method e About exibem gradientes sutis de accent azulado"
    - "Footer usa fundo #020617 (slate-950)"
    - "Texto branco sobre fundos escuros e gradientes e legivel (WCAG AA)"
  artifacts:
    - path: "app/assets/css/main.css"
      provides: "Updated brand tokens + gradient @utility definitions"
      contains: "--color-brand-dark"
    - path: "app/components/App/AppHeader.vue"
      provides: "Dark header background + blue hover on nav links"
      contains: "color-brand-dark"
    - path: "app/components/Section/SectionHero.vue"
      provides: "Gradient overlay on hero"
      contains: "gradient-hero"
    - path: "app/components/Section/SectionPrice.vue"
      provides: "Gradient background on price section"
      contains: "gradient-price"
    - path: "app/app.vue"
      provides: "Gradient background on formulario section"
      contains: "gradient-form"
    - path: "app/components/Section/SectionMethod.vue"
      provides: "Light accent gradient on method section"
      contains: "gradient-section-method"
    - path: "app/components/Section/SectionAbout.vue"
      provides: "Light accent gradient on about section"
      contains: "gradient-section-about"
  key_links:
    - from: "app/assets/css/main.css"
      to: "all 13 components"
      via: "CSS custom property cascade through var(--color-brand-*)"
      pattern: "var\\(--color-brand-"
    - from: "app/assets/css/main.css"
      to: "SectionHero, SectionPrice, app.vue, SectionMethod, SectionAbout"
      via: "@utility gradient-* classes applied in templates"
      pattern: "gradient-(hero|price|form|section-method|section-about)"
---

<objective>
Update the entire LP color palette from the old navy/orange scheme to vibrant blue/cyan with diagonal gradients on key sections.

Purpose: Establish the v1.6 premium visual identity — vibrant blue (#1D4ED8) as brand accent, cyan CTA, dark slate backgrounds, and subtle aviation-themed gradients.
Output: Updated main.css tokens + gradient utilities, 7 component files with new background/gradient classes, all passing WCAG AA contrast.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-paleta-e-gradientes/15-CONTEXT.md
@.planning/phases/15-paleta-e-gradientes/15-RESEARCH.md

@app/assets/css/main.css
@app/components/App/AppHeader.vue
@app/components/App/AppFooter.vue
@app/components/Section/SectionHero.vue
@app/components/Section/SectionPrice.vue
@app/components/Section/SectionMethod.vue
@app/components/Section/SectionAbout.vue
@app/app.vue

<interfaces>
<!-- Current token block in main.css (lines 5-17) — the single source of truth for all brand colors -->
```css
@theme {
  --color-brand-primary: #1a3a5c;
  --color-brand-bg: #F9FAFB;
  --color-brand-cta: #e67e22;
  --color-brand-cta-hover: #d35400;
  --color-brand-text: #1a1a1a;
  --color-brand-text-muted: #6b7280;
  --color-brand-footer: #0f2039;
  --font-family-sans: 'Inter', system-ui, ...;
}
```

<!-- Components using bg-[var(--color-brand-primary)] as BACKGROUND (need swap to brand-dark or gradient): -->
<!-- AppHeader.vue line 42: header bg -->
<!-- AppHeader.vue line 90: mobile overlay bg -->
<!-- SectionPrice.vue line 15: section bg -->
<!-- app.vue line 48: formulario section bg -->
<!-- SectionMethod.vue line 33: section bg uses --color-brand-bg -->
<!-- SectionMethod.vue line 66: step card "04" uses --color-brand-primary as bg -->

<!-- SectionAbout.vue line 6: section root is <section id="sobre" class="bg-white py-12 ..."> -->
<!-- SectionHero.vue line 18: overlay is <div class="absolute inset-0 bg-black/60" /> -->

<!-- AppFooter.vue line 33: Agencia 201 link uses hover:text-white/80 -->
<!-- AppFooter.vue line 42-43: WhatsApp link uses text-white/80 hover:text-white -->
<!-- AppFooter.vue line 53: Instagram link uses text-white/80 hover:text-white -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update design tokens and add gradient utilities in main.css</name>
  <files>app/assets/css/main.css</files>
  <read_first>
    - app/assets/css/main.css (current token values and structure)
    - .planning/phases/15-paleta-e-gradientes/15-RESEARCH.md (complete main.css code example in "Code Examples" section)
  </read_first>
  <action>
    Replace the entire `@theme {}` block and add `@utility` gradient definitions. Per D-01 through D-06:

    **Token changes in `@theme {}`:**
    - `--color-brand-primary`: change from `#1a3a5c` to `#1D4ED8` (vibrant blue-700, per D-01)
    - Add NEW token `--color-brand-dark`: `#0F172A` (slate-900, per D-03 — this splits the old primary into "accent blue" and "dark background")
    - `--color-brand-bg`: keep `#F9FAFB` (per D-05)
    - `--color-brand-cta`: change from `#e67e22` to `#0891B2` (cyan-700, per D-02 intent + PAL-04 WCAG compliance — D-02 specified #06B6D4 but white text on #06B6D4 achieves only 2.5:1 ratio which fails WCAG AA even for large text; #0891B2 achieves 3.5:1 which passes the 3:1 large-text threshold for the semibold 18px+ CTA buttons)
    - `--color-brand-cta-hover`: change from `#d35400` to `#0E7490` (cyan-800, darker hover state)
    - `--color-brand-text`: keep `#1a1a1a` (per D-06)
    - `--color-brand-text-muted`: keep `#6b7280` (per D-06)
    - `--color-brand-footer`: change from `#0f2039` to `#020617` (slate-950, per D-04)
    - `--font-family-sans`: keep unchanged (font change is Phase 16)

    **Add gradient `@utility` blocks after the `@theme {}` block but before the `html {}` rule:**

    ```css
    @utility gradient-hero {
      background: linear-gradient(135deg, #0F172A 0%, #1D4ED8 100%);
    }

    @utility gradient-price {
      background: linear-gradient(135deg, #1E3A8A 0%, #1D4ED8 100%);
    }

    @utility gradient-form {
      background: linear-gradient(135deg, #0F172A 0%, #1E3A8A 100%);
    }

    @utility gradient-section-method {
      background: linear-gradient(135deg, #F9FAFB 0%, #EFF6FF 100%);
    }

    @utility gradient-section-about {
      background: linear-gradient(135deg, #FFFFFF 0%, #F0F7FF 100%);
    }
    ```

    Per D-10: all gradients use 135deg diagonal, max 2 color stops.
    Per D-11: hero, price, form use strong dark-to-blue gradients.
    Per D-12: method, about use subtle light-to-blue-tint gradients.

    **Keep all existing rules unchanged:** `html {}`, `section[id] {}`.
    Update the comment on `--color-brand-primary` to say `/* vibrant blue-700 — headings, text accents, icons */`.
    Add comment on `--color-brand-dark`: `/* slate-900 — header bg, form section bg, dark overlays */`.
  </action>
  <verify>
    <automated>grep -c "color-brand-dark" app/assets/css/main.css | grep -q "1" && grep -c "gradient-hero" app/assets/css/main.css | grep -q "1" && grep -c "#1D4ED8" app/assets/css/main.css | grep -q "" && grep -c "#0891B2" app/assets/css/main.css | grep -q "" && grep -c "#e67e22" app/assets/css/main.css | grep -q "0" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - main.css contains `--color-brand-primary: #1D4ED8`
    - main.css contains `--color-brand-dark: #0F172A`
    - main.css contains `--color-brand-cta: #0891B2`
    - main.css contains `--color-brand-cta-hover: #0E7490`
    - main.css contains `--color-brand-footer: #020617`
    - main.css contains `@utility gradient-hero`
    - main.css contains `@utility gradient-price`
    - main.css contains `@utility gradient-form`
    - main.css contains `@utility gradient-section-method`
    - main.css contains `@utility gradient-section-about`
    - main.css does NOT contain `#e67e22` or `#d35400` or `#1a3a5c` or `#0f2039`
    - main.css still contains `--color-brand-bg: #F9FAFB` (unchanged)
    - main.css still contains `--color-brand-text: #1a1a1a` (unchanged)
    - main.css still contains `--font-family-sans` (unchanged)
    - All 5 gradient utilities use `135deg`
  </acceptance_criteria>
  <done>All 8 brand tokens updated (7 existing + 1 new --color-brand-dark), 5 gradient @utility blocks registered, old hex values removed, file structure preserved</done>
</task>

<task type="auto">
  <name>Task 2: Apply new tokens and gradients to all affected components</name>
  <files>
    app/components/App/AppHeader.vue
    app/components/App/AppFooter.vue
    app/components/Section/SectionHero.vue
    app/components/Section/SectionPrice.vue
    app/components/Section/SectionMethod.vue
    app/components/Section/SectionAbout.vue
    app/app.vue
  </files>
  <read_first>
    - app/components/App/AppHeader.vue (lines 42, 60, 90, 100 — bg and hover classes)
    - app/components/App/AppFooter.vue (lines 33, 42, 53 — link hover classes)
    - app/components/Section/SectionHero.vue (line 18 — overlay div)
    - app/components/Section/SectionPrice.vue (line 15 — section bg)
    - app/components/Section/SectionMethod.vue (line 33 — section bg, line 66 — step card "04" bg)
    - app/components/Section/SectionAbout.vue (line 6 — section root element)
    - app/app.vue (line 48 — formulario section)
  </read_first>
  <action>
    Apply 7 targeted edits across 7 files. Each edit is specific:

    **1. AppHeader.vue — 3 changes (per D-03, D-08):**
    - Line 42: change `bg-[var(--color-brand-primary)]` to `bg-[var(--color-brand-dark)]` (header background)
    - Line 60: change `hover:text-white` to `hover:text-[var(--color-brand-primary)]` (desktop nav links — blue hover on dark bg, per D-08)
    - Line 90: change `bg-[var(--color-brand-primary)]` to `bg-[var(--color-brand-dark)]` (mobile overlay)
    - Line 100: mobile nav links stay `text-white text-xl font-bold` — no hover change needed (tap targets, not hover)

    **2. AppFooter.vue — 2 changes (per D-09):**
    - Line 33 (Agencia 201 link): change `hover:text-white/80` to `hover:text-[var(--color-brand-primary)]` (blue hover on dark footer, per D-09)
    - Lines 42-43 (WhatsApp link): change `hover:text-white` to `hover:text-[var(--color-brand-primary)]`
    - Line 53 (Instagram link): change `hover:text-white` to `hover:text-[var(--color-brand-primary)]`

    **3. SectionHero.vue — 1 change (per D-11):**
    - Line 18: replace `<div class="absolute inset-0 bg-black/60" />` with `<div class="absolute inset-0 gradient-hero opacity-90" />`
    - The gradient-hero utility provides the diagonal dark-to-blue overlay. opacity-90 ensures text readability over the background image.

    **4. SectionPrice.vue — 1 change (per D-11):**
    - Line 15: replace `bg-[var(--color-brand-primary)]` with `gradient-price` in the section class
    - Full line becomes: `class="gradient-price py-12 md:py-24 px-6"`

    **5. SectionMethod.vue — 2 changes (per D-12):**
    - Line 33: replace `bg-[var(--color-brand-bg)]` with `gradient-section-method` in the section class
    - Full line becomes: `class="gradient-section-method py-12 md:py-24 px-6"`
    - Line 66 (step card "04"): change `bg-[var(--color-brand-primary)]` to `bg-[var(--color-brand-dark)]` — the "Autonomia" card should use dark background, not vibrant blue, to match the dark section aesthetic

    **6. SectionAbout.vue — 1 change (per D-12):**
    - Line 6: replace `bg-white` with `gradient-section-about` in the section class
    - Full line becomes: `<section id="sobre" class="gradient-section-about py-12 md:py-24 px-6">`

    **7. app.vue — 1 change (per D-11):**
    - Line 48: replace `bg-[var(--color-brand-primary)]` with `gradient-form` in the formulario section class
    - Full line becomes: `<section id="formulario" aria-label="Formulario de mentoria" class="gradient-form py-16 px-6">`

    **Do NOT change:**
    - SectionSocialProof.vue — #DCF8C6 WhatsApp green stays (per D-14)
    - SectionProgramContent.vue, SectionForWhom.vue, SectionFAQ.vue — no gradient (per D-13)
    - BackToTop.vue — uses CTA token, auto-updates
    - SectionLeadForm.vue — uses CTA token for submit button, auto-updates
    - Any text content, layout, or structure
  </action>
  <verify>
    <automated>grep -c "color-brand-dark" app/components/App/AppHeader.vue | grep -q "2" && grep -c "gradient-hero" app/components/Section/SectionHero.vue | grep -q "1" && grep -c "gradient-price" app/components/Section/SectionPrice.vue | grep -q "1" && grep -c "gradient-form" app/app.vue | grep -q "1" && grep -c "gradient-section-method" app/components/Section/SectionMethod.vue | grep -q "1" && grep -c "gradient-section-about" app/components/Section/SectionAbout.vue | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - AppHeader.vue contains `bg-[var(--color-brand-dark)]` (2 occurrences: header + mobile overlay)
    - AppHeader.vue contains `hover:text-[var(--color-brand-primary)]` on desktop nav links
    - AppHeader.vue does NOT contain `bg-[var(--color-brand-primary)]` (no longer used as background)
    - AppFooter.vue contains `hover:text-[var(--color-brand-primary)]` on link elements (3 occurrences)
    - SectionHero.vue contains `gradient-hero opacity-90` and does NOT contain `bg-black/60`
    - SectionPrice.vue contains `gradient-price` and does NOT contain `bg-[var(--color-brand-primary)]`
    - SectionMethod.vue contains `gradient-section-method` and does NOT contain `bg-[var(--color-brand-bg)]` on the section element
    - SectionMethod.vue step card "04" uses `bg-[var(--color-brand-dark)]` instead of `bg-[var(--color-brand-primary)]`
    - SectionAbout.vue contains `gradient-section-about` and does NOT contain `bg-white` on the section element
    - app.vue formulario section contains `gradient-form` and does NOT contain `bg-[var(--color-brand-primary)]`
    - No file contains `#e67e22` (old orange CTA)
    - SectionSocialProof.vue still contains `#DCF8C6` (unchanged, per D-14)
  </acceptance_criteria>
  <done>All 7 component files updated with new token references and gradient classes. Dark sections use brand-dark or gradients. Light sections use subtle accent gradients. Nav hover states use blue accent per D-08/D-09. No old color references remain in modified files.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full palette change:

1. **No old colors remain:**
   - `grep -r "#1a3a5c" app/` should return 0 results
   - `grep -r "#e67e22" app/` should return 0 results
   - `grep -r "#d35400" app/` should return 0 results
   - `grep -r "#0f2039" app/` should return 0 results

2. **New tokens present:**
   - `grep "#1D4ED8" app/assets/css/main.css` returns match
   - `grep "#0F172A" app/assets/css/main.css` returns match
   - `grep "#0891B2" app/assets/css/main.css` returns match
   - `grep "#020617" app/assets/css/main.css` returns match

3. **Gradients applied:**
   - `grep -r "gradient-hero" app/` returns SectionHero.vue + main.css
   - `grep -r "gradient-price" app/` returns SectionPrice.vue + main.css
   - `grep -r "gradient-form" app/` returns app.vue + main.css
   - `grep -r "gradient-section-method" app/` returns SectionMethod.vue + main.css
   - `grep -r "gradient-section-about" app/` returns SectionAbout.vue + main.css

4. **WhatsApp green preserved:**
   - `grep "#DCF8C6" app/components/Section/SectionSocialProof.vue` returns matches

5. **Build check:**
   - `cd /home/michel/projects/codebase/agencia201/flyupmilhas/site-flyupmilhas && npx nuxi build 2>&1 | tail -5` succeeds without errors
</verification>

<success_criteria>
- All 8 brand tokens in main.css reflect the v1.6 palette (PAL-01, PAL-02)
- 5 gradient @utility classes registered and applied to 5 sections (PAL-03)
- CTA buttons use #0891B2 (cyan-700) which passes WCAG AA 3:1 for large text with white text (PAL-04)
- White text on all dark backgrounds (#0F172A, #020617, gradients) passes WCAG AA (PAL-04)
- #1D4ED8 on white/off-white backgrounds passes WCAG AA at 6.3:1 (PAL-04)
- No old palette hex values (#1a3a5c, #e67e22, #d35400, #0f2039) appear anywhere in app/
- #DCF8C6 WhatsApp green is preserved in SectionSocialProof.vue
- Nuxt build succeeds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-paleta-e-gradientes/15-01-SUMMARY.md`
</output>

---
phase: 08-new-sections
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - app/app.vue
  - app/components/App/AppHeader.vue
autonomous: false
requirements:
  - SEC-01
  - SEC-02

must_haves:
  truths:
    - "SectionProgramContent is visible on the page in a logical reading-flow position"
    - "SectionForWhom is visible on the page in a logical reading-flow position"
    - "Both new sections render correctly on mobile (375px) and desktop (1280px)"
    - "Both sections are reachable via anchor navigation"
  artifacts:
    - path: "app/app.vue"
      provides: "Section wiring in correct reading flow order"
      contains: "SectionProgramContent"
    - path: "app/app.vue"
      provides: "Section wiring in correct reading flow order"
      contains: "SectionForWhom"
  key_links:
    - from: "app/app.vue"
      to: "SectionProgramContent.vue"
      via: "component import (auto-resolved by Nuxt)"
      pattern: "SectionProgramContent"
    - from: "app/app.vue"
      to: "SectionForWhom.vue"
      via: "component import (auto-resolved by Nuxt)"
      pattern: "SectionForWhom"
    - from: "AppHeader.vue navLinks"
      to: "section IDs"
      via: "anchor scroll navigation"
      pattern: "conteudo-programatico|para-quem-e"
---

<objective>
Wire both new sections into app.vue at optimal reading-flow positions and update AppHeader navigation.

Purpose: Make the new sections visible to visitors and navigable from the header.
Output: Complete page integration with both sections in the reading flow.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-new-sections/08-CONTEXT.md
@.planning/phases/08-new-sections/08-01-SUMMARY.md
@app/app.vue
@app/components/App/AppHeader.vue

<interfaces>
<!-- Current app.vue section order -->
```
SectionHero
SectionAbout
SectionMethod
SectionSocialProof
SectionPrice
SectionFAQ
SectionLeadForm (in formulario wrapper)
```

<!-- AppHeader navLinks array -->
```typescript
const navLinks = [
  { label: 'Sobre', anchor: 'sobre' },
  { label: 'Como Funciona', anchor: 'como-funciona' },
  { label: 'Depoimentos', anchor: 'depoimentos' },
  { label: 'FAQ', anchor: 'faq' },
]
```

<!-- New section IDs from Plan 01 -->
SectionProgramContent: id="conteudo-programatico"
SectionForWhom: id="para-quem-e"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire sections into app.vue and update AppHeader nav</name>
  <files>app/app.vue, app/components/App/AppHeader.vue</files>
  <read_first>
    - app/app.vue (current section order)
    - app/components/App/AppHeader.vue (current navLinks array)
    - app/components/Section/SectionProgramContent.vue (verify component exists and has id)
    - app/components/Section/SectionForWhom.vue (verify component exists and has id)
  </read_first>
  <action>
    **app.vue — Add sections per D-13 (Claude decides position):**

    Optimal reading flow rationale:
    - Hero: hook attention
    - About: what is this mentorship
    - **ProgramContent: what you will learn (natural follow-up to "what is this")**
    - **ForWhom: is this for me (qualification after seeing the content)**
    - Method: how it works (for those who passed qualification)
    - SocialProof: proof it works
    - Price: investment
    - FAQ: remaining objections
    - LeadForm: conversion

    Update the `<template>` section to insert:
    - `<SectionProgramContent />` after `<SectionAbout />`
    - `<SectionForWhom />` after `<SectionProgramContent />`

    Final order in template:
    ```
    <SectionHero />
    <SectionAbout />
    <SectionProgramContent />
    <SectionForWhom />
    <SectionMethod />
    <SectionSocialProof />
    <SectionPrice />
    <SectionFAQ />
    <section id="formulario" ...>
      <SectionLeadForm />
    </section>
    ```

    **AppHeader.vue — Update navLinks per D-14/D-16 (Claude's discretion):**

    The nav currently has 4 links. Adding 2 more would make 6 which is acceptable on desktop but could crowd mobile. Add 1 link for "Conteudo" (covers program content) since it's a high-value navigation target. Skip "Para quem e" from nav to avoid overcrowding — it's immediately below ProgramContent so users will scroll into it naturally.

    Update navLinks array to:
    ```typescript
    const navLinks = [
      { label: 'Sobre', anchor: 'sobre' },
      { label: 'Conteudo', anchor: 'conteudo-programatico' },
      { label: 'Como Funciona', anchor: 'como-funciona' },
      { label: 'Depoimentos', anchor: 'depoimentos' },
      { label: 'FAQ', anchor: 'faq' },
    ]
    ```

    Insert "Conteudo" as the second item (after Sobre, before Como Funciona) to match page reading order.
  </action>
  <verify>
    <automated>cd /home/michel/projects/codebase/agencia201/flyupmilhas/site-flyupmilhas && grep -c "SectionProgramContent" app/app.vue && grep -c "SectionForWhom" app/app.vue && grep -c "conteudo-programatico" app/components/App/AppHeader.vue</automated>
  </verify>
  <acceptance_criteria>
    - app.vue contains `<SectionProgramContent />` (grep returns 1+)
    - app.vue contains `<SectionForWhom />` (grep returns 1+)
    - SectionProgramContent appears after SectionAbout in template
    - SectionForWhom appears after SectionProgramContent in template
    - SectionMethod appears after SectionForWhom (existing sections not reordered relative to each other)
    - AppHeader navLinks contains entry with anchor "conteudo-programatico" (grep returns 1+)
    - NavLinks order matches page flow: Sobre, Conteudo, Como Funciona, Depoimentos, FAQ
  </acceptance_criteria>
  <done>Both sections wired into app.vue in optimal reading-flow position, AppHeader nav updated with Conteudo link.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify new sections render correctly</name>
  <what-built>Two new sections (Conteudo Programatico and Para Quem E) integrated into the landing page with header navigation</what-built>
  <how-to-verify>
    1. Run `pnpm dev` if not already running
    2. Open http://localhost:3000 in browser
    3. Scroll down the page — verify SectionProgramContent appears after SectionAbout:
       - Desktop (1280px+): Two columns — left block with title/description/CTA, right block with 8 items with icons
       - Mobile (375px): Single column — title/description/CTA stacked above 8 items list
    4. Continue scrolling — verify SectionForWhom appears next:
       - Desktop: 2x2 grid of cards + 1 centered card below
       - Mobile: 5 cards stacked vertically
    5. Click "Conteudo" in the header nav — page scrolls to Conteudo Programatico section
    6. Click the CTA "Quero dar o primeiro passo" in the ProgramContent section — page scrolls to the form
    7. Verify all 8 learning items and 5 audience cards display their icons correctly
  </how-to-verify>
  <resume-signal>Type "approved" or describe any layout/content issues</resume-signal>
</task>

</tasks>

<verification>
Both new sections visible on page, correctly positioned in reading flow, navigable from header, responsive on mobile and desktop.
</verification>

<success_criteria>
- Page shows all sections in correct order: Hero > About > ProgramContent > ForWhom > Method > SocialProof > Price > FAQ > Form
- "Conteudo" nav link scrolls to the correct section
- Both sections render without layout breaks at 375px and 1280px
- All 8 learning items and 5 audience cards visible with icons
</success_criteria>

<output>
After completion, create `.planning/phases/08-new-sections/08-02-SUMMARY.md`
</output>

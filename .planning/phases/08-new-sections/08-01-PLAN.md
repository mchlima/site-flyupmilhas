---
phase: 08-new-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/Section/SectionProgramContent.vue
  - app/components/Section/SectionForWhom.vue
autonomous: true
requirements:
  - SEC-01
  - SEC-02

must_haves:
  truths:
    - "SectionProgramContent displays all 8 learning items from old site with individual icons"
    - "SectionProgramContent has a left block with title, description, and CTA on desktop"
    - "SectionForWhom displays 5 audience fit cards with icons"
    - "Both sections are responsive: single-column mobile, multi-column desktop"
  artifacts:
    - path: "app/components/Section/SectionProgramContent.vue"
      provides: "Programmatic content section with 8 learning items"
      contains: "conteudo-programatico"
    - path: "app/components/Section/SectionForWhom.vue"
      provides: "Audience qualification section with 5 cards"
      contains: "para-quem-e"
  key_links:
    - from: "SectionProgramContent.vue"
      to: "useScroll composable"
      via: "CTA button scrollTo('formulario')"
      pattern: "scrollTo.*formulario"
---

<objective>
Create the two new Vue section components: SectionProgramContent (8 learning items) and SectionForWhom (5 audience cards).

Purpose: Visitors can see exactly what they will learn and whether the mentorship fits them.
Output: Two standalone section components ready to wire into app.vue.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-new-sections/08-CONTEXT.md
@docs/OLD_SITE_CONTENT.md
@app/app.vue
@app/components/Section/SectionAbout.vue
@app/components/Section/SectionMethod.vue
@app/composables/useScroll.ts
@app/assets/css/main.css

<interfaces>
<!-- Established patterns the executor must follow -->

Section structure pattern (from SectionAbout.vue):
```html
<section id="..." class="bg-[...] py-12 md:py-24 px-6">
  <div class="max-w-5xl mx-auto">
    <h2 class="text-2xl md:text-3xl font-bold text-[var(--color-brand-primary)] text-center mb-4">
```

Card pattern (from SectionAbout.vue):
```html
<div class="rounded-xl bg-white shadow-sm border border-gray-100 p-6">
  <UIcon name="i-heroicons-..." class="w-8 h-8 text-[var(--color-brand-primary)] mb-3" />
```

CTA button pattern:
```html
<button class="bg-[var(--color-brand-cta)] hover:bg-[var(--color-brand-cta-hover)] text-white font-semibold px-8 py-4 rounded-lg cursor-pointer transition-colors" @click="scrollTo('formulario')">
  Quero dar o primeiro passo
</button>
```

useScroll composable:
```typescript
const { scrollTo } = useScroll()
// scrollTo(id: string) — smooth scrolls to element by ID
```

CSS variables:
```css
--color-brand-primary: #1a3a5c;
--color-brand-bg: #F9FAFB;
--color-brand-cta: #e67e22;
--color-brand-cta-hover: #d35400;
--color-brand-text: #1a1a1a;
--color-brand-text-muted: #6b7280;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SectionProgramContent component</name>
  <files>app/components/Section/SectionProgramContent.vue</files>
  <read_first>
    - docs/OLD_SITE_CONTENT.md (Section 2 for description text, Section 3 for 8 items)
    - app/components/Section/SectionAbout.vue (card and section patterns)
    - app/composables/useScroll.ts (CTA scrollTo usage)
  </read_first>
  <action>
    Create SectionProgramContent.vue implementing SEC-01 with these specifics:

    **Section wrapper:** `<section id="conteudo-programatico" class="bg-[var(--color-brand-bg)] py-12 md:py-24 px-6">` per D-15. Use off-white background to alternate with surrounding white sections.

    **Layout per D-01/D-02:** Two-block side-by-side on desktop, stacked on mobile.
    - Use `<div class="max-w-5xl mx-auto grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-12 items-start">`
    - Left block: title, description paragraph, CTA button
    - Right block: 8-item list

    **Left block per D-05/D-06/D-07:**
    - Heading: "O que voce vai aprender na pratica" (per D-07) — use `text-2xl md:text-3xl font-bold text-[var(--color-brand-primary)]`
    - Description paragraph from old site section 2: "Aqui voce nao recebe promessas irreais. Voce aprende o que funciona, quando funciona e como aplicar na sua realidade." (per D-05)
    - CTA: plain `<button>` with `bg-[var(--color-brand-cta)]` classes, text "Quero dar o primeiro passo", `@click="scrollTo('formulario')"` (per D-06)

    **Right block — 8 items per D-03/D-04:**
    Define a `const items` array with these exact 8 entries from old site section 3, each with a relevant heroicons icon:
    1. "Como funcionam os principais programas de milhas" — icon: `i-heroicons-light-bulb`
    2. "Formas inteligentes de acumular pontos sem gastar mais" — icon: `i-heroicons-arrow-trending-up`
    3. "Quando vale (ou nao) transferir pontos" — icon: `i-heroicons-arrows-right-left`
    4. "Como encontrar passagens com milhas pagando muito menos" — icon: `i-heroicons-ticket`
    5. "Erros comuns que fazem as pessoas perderem milhas" — icon: `i-heroicons-exclamation-triangle`
    6. "Estrategias para viagens nacionais e internacionais" — icon: `i-heroicons-globe-americas`
    7. "Como usar o cartao de credito a seu favor" — icon: `i-heroicons-credit-card`
    8. "Organizacao e planejamento de viagens com antecedencia" — icon: `i-heroicons-calendar-days`

    Render with `v-for` as flex column items, each item as a flex row with:
    `<UIcon :name="item.icon" class="w-6 h-6 text-[var(--color-brand-cta)] shrink-0 mt-0.5" />`
    `<span class="text-[var(--color-brand-text)]">{{ item.text }}</span>`
    Gap between items: `gap-4`. Gap between icon and text: `gap-3`.

    **Script setup:** Import `useScroll` composable: `const { scrollTo } = useScroll()`
  </action>
  <verify>
    <automated>cd /home/michel/projects/codebase/agencia201/flyupmilhas/site-flyupmilhas && grep -c "conteudo-programatico" app/components/Section/SectionProgramContent.vue && grep -c "scrollTo" app/components/Section/SectionProgramContent.vue && grep -c "i-heroicons-" app/components/Section/SectionProgramContent.vue</automated>
  </verify>
  <acceptance_criteria>
    - File exists at app/components/Section/SectionProgramContent.vue
    - Contains `id="conteudo-programatico"` (grep returns 1+)
    - Contains `scrollTo('formulario')` for CTA button (grep returns 1+)
    - Contains exactly 8 different heroicons icon references (grep returns 8+)
    - Contains "O que voce vai aprender na pratica" heading text
    - Contains "Quero dar o primeiro passo" CTA text
    - Uses grid layout with lg:grid-cols-2 for desktop side-by-side
    - Uses bg-[var(--color-brand-bg)] section background
  </acceptance_criteria>
  <done>SectionProgramContent renders 8 learning items with icons in a two-column desktop layout with left info block + CTA and right items list, single column on mobile.</done>
</task>

<task type="auto">
  <name>Task 2: Create SectionForWhom component</name>
  <files>app/components/Section/SectionForWhom.vue</files>
  <read_first>
    - docs/OLD_SITE_CONTENT.md (Section 4 for 5 audience criteria)
    - app/components/Section/SectionAbout.vue (card pattern: rounded-xl bg-white shadow-sm)
  </read_first>
  <action>
    Create SectionForWhom.vue implementing SEC-02 with these specifics:

    **Section wrapper:** `<section id="para-quem-e" class="bg-white py-12 md:py-24 px-6">` per D-15. Use white background to alternate with off-white SectionProgramContent.

    **Heading per D-11:** "Para quem e essa mentoria?" — centered, using `text-2xl md:text-3xl font-bold text-[var(--color-brand-primary)] text-center mb-10`

    **Card grid per D-08/D-10:**
    - Grid: `grid grid-cols-1 md:grid-cols-2 gap-4 md:gap-6 max-w-4xl mx-auto`
    - Per D-12 (Claude's discretion): 5th card centered below the 2x2 grid using `md:col-span-2 md:max-w-sm md:mx-auto` to center it

    **5 cards per D-09** from old site section 4, each with a relevant heroicons icon:
    Define a `const cards` array:
    1. "Pessoas que querem viajar mais gastando menos" — icon: `i-heroicons-paper-airplane`
    2. "Quem esta comecando do zero em milhas" — icon: `i-heroicons-rocket-launch`
    3. "Quem ja tem pontos, mas nao sabe usar bem" — icon: `i-heroicons-puzzle-piece`
    4. "Quem quer aprender sem depender de dicas soltas" — icon: `i-heroicons-academic-cap`
    5. "Quem busca clareza, seguranca e estrategia" — icon: `i-heroicons-shield-check`

    **Card markup per D-10:**
    ```html
    <div class="rounded-xl bg-white shadow-sm border border-gray-100 p-6 flex items-start gap-4">
      <UIcon :name="card.icon" class="w-8 h-8 text-[var(--color-brand-cta)] shrink-0" />
      <p class="text-[var(--color-brand-text)] leading-relaxed">{{ card.text }}</p>
    </div>
    ```

    No script setup needed (no interactivity). Use `<script setup lang="ts">` with only the cards array.
  </action>
  <verify>
    <automated>cd /home/michel/projects/codebase/agencia201/flyupmilhas/site-flyupmilhas && grep -c "para-quem-e" app/components/Section/SectionForWhom.vue && grep -c "rounded-xl" app/components/Section/SectionForWhom.vue && grep -c "i-heroicons-" app/components/Section/SectionForWhom.vue</automated>
  </verify>
  <acceptance_criteria>
    - File exists at app/components/Section/SectionForWhom.vue
    - Contains `id="para-quem-e"` (grep returns 1+)
    - Contains 5 heroicons icon references (grep returns 5+)
    - Contains "Para quem e essa mentoria" heading text
    - Uses rounded-xl bg-white shadow-sm card pattern (grep returns 5+)
    - Uses grid with md:grid-cols-2 layout
    - 5th card has md:col-span-2 for centered layout
    - Uses bg-white section background
  </acceptance_criteria>
  <done>SectionForWhom renders 5 audience qualification cards in a 2-column grid on desktop (5th centered) and single column on mobile, matching the bento card pattern.</done>
</task>

</tasks>

<verification>
Both components created with correct section IDs, content from old site, heroicons icons, responsive layouts, and established CSS patterns.
</verification>

<success_criteria>
- SectionProgramContent.vue exists with 8 items, two-column layout, CTA button
- SectionForWhom.vue exists with 5 cards, grid layout, centered 5th card
- Both use established patterns (CSS variables, UIcon, rounded-xl cards)
- Both have anchor IDs for navigation
</success_criteria>

<output>
After completion, create `.planning/phases/08-new-sections/08-01-SUMMARY.md`
</output>

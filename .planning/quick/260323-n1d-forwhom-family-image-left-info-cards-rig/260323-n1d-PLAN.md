---
type: quick
id: "260323-n1d"
description: "ForWhom: family image LEFT, info cards RIGHT, remove professional image, harmonize layout"
status: complete
---

# Quick Task 260323-n1d

## Task
Restructure SectionForWhom to use family image on left, info cards on right. Remove professional image. Harmonize layout spacing.

## Changes
1. Remove `young-professional-planning.png` import — only family image used
2. Swap columns: image LEFT, cards RIGHT
3. Image: `aspect-[4/5]` crop, `shadow-lg`, `rounded-2xl`
4. Cards: tighter gap (`gap-3`), smaller padding (`p-5`), `items-center` vertical alignment
5. Grid: `items-center` to vertically center both columns
6. Text: `text-sm md:text-base` for better proportion next to image

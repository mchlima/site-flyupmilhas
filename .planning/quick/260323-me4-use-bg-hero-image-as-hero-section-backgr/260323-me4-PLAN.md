---
type: quick
task_count: 1
files_modified:
  - app/components/Section/SectionHero.vue
autonomous: true
---

<objective>
Add bg-hero.png (airplane wing at sunset) as the Hero section background image with a dark overlay for text contrast, switching all text to white/light variants.

Purpose: Create a visually striking hero with the aviation photography backdrop instead of the flat brand-bg color.
Output: SectionHero.vue with background image, dark overlay, and light text.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@app/components/Section/SectionHero.vue
@app/assets/img/bg-hero.png
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add background image with dark overlay and switch text to light</name>
  <files>app/components/Section/SectionHero.vue</files>
  <action>
Modify SectionHero.vue to use bg-hero.png as a full-bleed background with a dark overlay:

1. On the outer `<section>` element:
   - Remove `bg-[var(--color-brand-bg)]`
   - Add `relative overflow-hidden` to the existing classes

2. Add a background image div as the FIRST child inside `<section>`, BEFORE the content div:
   ```html
   <div
     class="absolute inset-0 bg-cover bg-center bg-no-repeat"
     :style="{ backgroundImage: `url(${bgHero})` }"
   />
   ```
   Import the image in script setup: `import bgHero from '~/assets/img/bg-hero.png'`

3. Add a dark overlay div as the SECOND child inside `<section>`, after the bg div:
   ```html
   <div class="absolute inset-0 bg-black/60" />
   ```
   Use 60% opacity black — the image is very bright (golden hour sunset), needs strong overlay for white text readability. Adjust to bg-black/55 or bg-black/65 if needed during verification.

4. On the inner content div (max-w-3xl), add `relative z-10` so content sits above the overlay.

5. Switch text colors to light variants:
   - Pain point chips: change `text-[var(--color-brand-text-muted)]` to `text-white/90` and `border-[var(--color-brand-primary)]/30` to `border-white/30`
   - H1: change `text-[var(--color-brand-primary)]` to `text-white`
   - Subtitle paragraph: change `text-[var(--color-brand-text-muted)]` to `text-white/80`
   - CTA button: keep as-is (`bg-[var(--color-brand-cta)]` orange on dark looks great)

6. Keep all other classes unchanged (min-h-screen, flex layout, padding, font sizes, etc.)
  </action>
  <verify>
    <automated>cd /home/michel/projects/codebase/agencia201/flyupmilhas/site-flyupmilhas && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Hero section renders bg-hero.png as full-bleed background
    - Dark overlay (bg-black/60) covers entire section for contrast
    - All text is white/light and readable over the dark overlay
    - CTA button remains orange and stands out
    - Build succeeds with no errors
  </done>
</task>

</tasks>

<verification>
- `pnpm build` completes without errors
- Visual check: hero shows airplane wing sunset photo behind dark overlay with white text
</verification>

<success_criteria>
- bg-hero.png visible as hero background
- Text fully readable (white on dark overlay)
- CTA button orange, clearly visible
- No layout shifts or broken styling
- Build passes
</success_criteria>
